# Отчёт сессии 24–25.09.2026 — МФ-UI закрыта, переход к Ф9а

Файл для перехода в новый чат. Состояние: main = origin = VPS, на stage `a017028`. Все решения пользователя записаны в `TZ_2026-09-21_PLAN.md` §1 «Решения 24.09 (вечер)».

## 1. Что сделано за сессию (по порядку)

### 1.1. Решения пользователя (записаны в PLAN §1, п. 1–12)
1. Сначала интерфейс, потом код Ф9а («интерфейс должен согласовываться со всеми функциями ТЗ»).
2. Меню CRM — только самое нужное. 3. Главная — доска «Парковка».
4. «Отменить»/исправление закрытой брони — только владелец (код — у Ф9а).
5–6. Роли по реальному штату: владелец, 3 администратора, охрана (отдельно!), водитель, парковщик; **водитель и парковщик входят сами** (отменяет «логинов нет» от 22.09), видят только свой экран, денег и броней не видят.
7. Деньги у ворот принимает администратор, не охрана.
8. DRIVER и PARKER — две отдельные роли (не общая «сотрудник»).
9. Парковщику кнопок «Заехал»/«Выехал» не даём — только смотрит.
10. «Дашборд» + «Отчёты» = один пункт меню. 11. Сессии 30 дней у всех.
12. Мелкие вопросы — по рекомендации проверяющего (админ и охрана сами отмечают смену — в Ф13; звонок водителю — нет, пока).
Плюс правила сессии: **не спрашивать подтверждений, работать на stage свободно, параллелить процессы** (M4 Pro, 12 ядер — 3–4 фоновых агента нормально). Записано в память `feedback_autonomous_execution`.

### 1.2. Сверка всего UI с ТЗ — `docs/ui_check_2026-09-24/`
Четыре параллельных агента, ~150 пунктов ТЗ построчно:
- `A_bookings_notifications.md` — ТЗ §1–2 (36 строк: ✅7 🟡12 ⬜17) + 9 разрывов без фазы + 8 противоречий между фазами.
- `B_boards_occupancy.md` — ТЗ §3–4 + разгадка «Среда 23-го»: врали часы в шапке (пояс браузера, чинит Ф9б), не заголовок.
- `C_money.md` — ТЗ §5, §7 (63 строки: ✅6 🟡13 ⬜44) + матрица денежных прав по 5 ролям.
- `D_roles_menu_shifts.md` — роли/меню/смены: инвентарь всех экранов и действий, матрица доступов, что ломает «логины есть» (главное — **Ф13 написана под «логинов нет», переписать до её кода**), предложение меню.
Разрывы разнесены по фазам; противоречия между фазами (окно 24ч Ф3↔Ф9б, сторно Ф10↔Ф11, ключ отправщика МФ-1↔Ф4 и др.) — в отчётах, брать оттуда при работе над соответствующими фазами.

### 1.3. Ф9а — документ доведён (кода НЕТ)
`PHASE_09A_MANUAL_TIME.md`: §12 — ответы на вопросы 1–11 (таблицей), §13 — сводка интерфейса по всем экранам, SQL-проверка stage с двумя условиями; §7.9 сведён с §13 (противоречие «тост Отменить» устранено — верна версия §13: тост с кнопкой у закрытых статусов только владельцу).

### 1.4. МФ-UI — полный цикл (архитектура → критика → код → 2 ревью → подтверждающее → stage)
Документ: `docs/phases/PHASE_MFUI_MENU_ROLES.md` (в конце — протоколы критики и ревью).
- **Критика архитектуры**: 14 находок (1 блокер — карта не знала о фазе), все внесены; `MAP_DEPENDENCIES.md` обновлена: **МФ-UI = шаг 0**, владельцы файлов переставлены, «дописывать NAV в конец» заменено на `nav.ts`.
- **Код** (3 параллельных потока):
  - Ядро: `enum Role` + DRIVER/PARKER (миграция `20260923193343_driver_parker_roles`), `roleHome()` в `src/lib/crm/roles.ts` (Record — новая роль не соберётся без адреса), guard/login/today на него, `next` только внутри раздела роли, `/admin/today` = `requireUser(ALL)`, запрос «Сегодня» вынесен в `src/server/services/today.ts` (перенос дословный, побайтно сверено ревью), сиды driver/parker.
  - Экраны поля: `/admin/parking-lot` (ParkingLotScreen: «Скоро выезд» + «Ожидаем», поиск с клавиатурой КПП, перестой — только сутки без денег) и `/admin/transfers` (TransferScreen: имя+номер+время, без телефона); «← CRM» в шапке GuardScreen для не-охраны.
  - Меню/навигация: `nav.ts` (группы; `ready:false` = серый пункт «скоро» — **Ф11/Ф13 включают одной строкой здесь**), Sidebar группами, `MobileNav.tsx` (нижняя панель Парковка·Сегодня·+·Клиенты·Ещё + лист «Ещё» с поиском/Экраном охраны/Выйти; фокус в диалог и назад; слушает `p24:quick-booking` — двух диалогов не бывает), «Выйти» в Topbar, `GlobalSearch inSheet`, QuickBookingDrawer поднят на `z-[60]`.
- **Ревью №1 (безопасность)**: 0 блокеров, 3 важно закрыты — `markNoticesReadAction` → STAFF; **щиты `requireUser(STAFF)` на clients/boards/search/occupancy** (RSC-запрос сегмента идёт мимо layout — Next 16 docs это подтверждают); пароли сидов на stage через переменные ДО первого деплоя (upsert не перезапишет). Мелочь: roleHome → Record.
- **Ревью №2 (интерфейс)**: 0 блокеров, 2 важно закрыты — тосты подняты над нижним баром (3 места); фокус-менеджмент листа «Ещё». 8 мелочей закрыты (шапка КПП на 390, BAR из NAV, overscroll, e2e-надёжность, E2E_OWNER_PASSWORD, стиль подвала). Не правилось: quirk z-контекста NoticeBell (владелец Ф2б, не пересекается с баром).
- **Подтверждающее (раунд 2): ЧИСТО.** Остаточное (говорящая ошибка pick) закрыто `a017028`.

### 1.5. Выкатка на stage — `a017028`
По процедуре PLAN §3: бэкап `backups/pre-mfui-2026-09-24.dump` → **в stage `.env` добавлены `SEED_DRIVER_PASSWORD=Pitstop-Driver-2026`, `SEED_PARKER_PASSWORD=Pitstop-Parker-2026` до пересборки** → rsync с исключениями → compose build → `seed ok`, минутный тик включён → все 6 e2e-наборов на stage зелёные (site-lead, crm-pipeline — admin; autoconfirm, scheduler, overstay, **roles 22/22** — owner + E2E_*-пароли) → cleanup.sql → `DEPLOYED_COMMIT` → rsync --checksum: различий нет.
Проверки: локально юнит 92/92, e2e 118/118, tsc 0, build чистый; eslint — только 3 старые ошибки Ф9б (Clock, QuickBookingDrawer ×2).

### 1.6. Wazzup (не код)
Пользователь передал API-ключ реального аккаунта заказчика (workspace 5154-8781). Сохранён в памяти (`reference_wazzup_parkovka24`), в git не попадает, в чат повторно не печатать. Живой платный аккаунт — API не трогать до Ф14 и явной команды. При Ф14: `WAZZUP_API_KEY` только в `.env`.

## 2. Коммиты сессии
`f626bf5` решения 24.09 + Ф9а §12–13 → `0f0b065` сверка UI (4 отчёта) → `4cdd583` архитектура МФ-UI + PLAN → `f6dcd78` критика применена + карта → `69888c1` код МФ-UI → `ccd4237` правки по 2 ревью → `a017028` pick fail-fast (**= stage**) → `49be991` фаза закрыта, HANDOFF.

## 3. Следующий шаг — код Ф9а (убрать ручное время)
Документ полностью готов: `PHASE_09A_MANUAL_TIME.md` — §7 файлы, §12 решения, §13 сводка интерфейса, критика в конце. Помнить: «Отменить»/исправление закрытой брони — только владелец (реш. 24.09 п.4, в коде ещё НЕ выполнено — это Ф9а); `KanbanBoard` берёт версию §13, не §7.9. После Ф9а — параллельно свободные дорожки (Ф2б, Ф4ш0, МФ-2, МФ-1, МФ-3а). **Перед кодом Ф13 — переписать её документ под «логины есть»** (устаревшие места: отчёт D §4).

## 4. Стартовый промпт для нового чата

```
Продолжаем Parkovka24. Прочитай ~/Parking24/HANDOFF.md (блок СТАРТ),
docs/SESSION_2026-09-24_MFUI_REPORT.md, docs/phases/MAP_DEPENDENCIES.md
и docs/phases/PHASE_09A_MANUAL_TIME.md целиком (включая §12–13 и критику).
Проверь git status и stage (/opt/parking24/DEPLOYED_COMMIT).
Затем: пишем код Ф9а по документу. Не спрашивай подтверждений,
параллель процессы, дальше по порядку из карты.
```
