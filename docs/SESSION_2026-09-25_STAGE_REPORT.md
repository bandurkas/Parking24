# Переход в новый чат — 25.09.2026 (ночь): всё из дорожек в main и на stage, Ф6 в работе

Заменяет `SESSION_2026-09-24_LANES_REPORT.md` (там §3–§4 выполнены, §2 устарел). Читать вместе с блоком СТАРТ в `HANDOFF.md`.

> **⚠️ ГЛАВНОЕ ПРАВИЛО ПРОЕКТА (пользователь, 24.09): не усложнять там, где не нужно.** Только то, что просит ТЗ и решения пользователя, самым простым способом; из ревью чиним блокеры и то, что ломает деньги/данные/безопасность, остальное — строкой «на заметку» в документ фазы.

**Приоритет пользователя:** коммуникация с клиентами до показа заказчику → потом касса и табель (код обеих уже на stage).

## 1. Где что сейчас

| Где | Коммит | Что внутри |
|---|---|---|
| **main** = origin | `68d34cf` | всё готовое: МФ-UI, Ф9а, МФ-1, Ф2б, Ф10, МФ-2, Ф4ш0, Ф8, Ф3, цены сайт↔CRM, **Ф14 (Wazzup, на заглушке), Ф13 (табель), Ф11 (касса), Ф5 (сообщения по статусам, номер договора)** |
| **stage** (`/opt/parking24/DEPLOYED_COMMIT`) | `68d34cf` | = main. Выкачено 25.09 ночью: бэкап `backups/pre-lanes3-2026-09-25.dump`, 6 миграций (Ф14 ×2, Ф11, Ф13 ×2, Ф5), `seed ok`, тик включён, `cleanup.sql` выполнен, сверка rsync `-n --checksum` — различий и лишних файлов нет |
| дорожка `l8-comm` | ветка `lane/l8-comm` от `68d34cf` | **Ф6 (изменение брони: сообщение клиенту) в работе**, база `parking24_l8`, порт 3108 |

**Тесты.** main локально: tsc 0, юнит 355/355, полный `npm run test:e2e` — 19 наборов, 0 провалов (с заглушкой Wazzup на dev-сервере). Stage: 18 наборов с exit 0 — сайт 12, конвейер 29, планировщик 10, перестой 50, роли 22, Ф9а 123, МФ-1 4, Ф2б 35, Ф10 99, МФ-2 68, Ф8 18, Ф14 4, табель 72, касса 60, Ф5 8; автоподтверждение, Ф4-отправщик и Ф3 на stage без базы — штатный «Пропуск» (счастливые пути проверены локально). `prices-sync` на stage **не гоняли** (временно меняет цену на публичном сайте; кода цен в пакете не было).

**Состояние данных stage после выкатки:** 14 броней (№105 «test» и №41 — ручные проверки, не трогали), открытых смен кассы нет, табель пуст (сотрудников и должностей нет — заводит владелец), номеров договоров нет, очередь сообщений пуста.

## 2. Что должен знать тот, кто будет показывать заказчику

- **Отправщик сообщений выключен** (`messaging.senderEnabled = false`), автоподтверждение выключено, скан перестоя — «выкл» (`scheduler.scans = {overstay: off}`). Так и оставить до ответов на вопросы §4.
- **Четыре правила Ф5 выключены** (`on_rejected_no_space`, `on_rejected_other`, `on_checked_in`, `on_checked_out`) — владелец включает в «Настройки → Автоматизации». «Автомобиль принят» и «Спасибо» — черновики, тексты меняются на странице «Шаблоны» без выкатки; включать «Спасибо» после ссылки на отзывы (Яша).
- **Wazzup подключён к stage и проверен настоящим путём (25.09, ключ дан пользователем повторно):** ключ, секрет вебхука и `WAZZUP_WEBHOOK_BASE` — в `/opt/parking24/.env` (копия прежнего — `backups/env.pre-wazzup-2026-09-25.bak`); в CRM выбран провайдер **Wazzup**; каналы по API: **WhatsApp …0408 работает**, **Telegram …6444 «не оплачен»** (в колокольчике `CHANNEL_DOWN`), MAX нет; вебхук аккаунта был пуст (чужих интеграций нет) — зарегистрирован кнопкой владельца. **Входящее** (сообщение «Тест» с +7 987 271-50-04 в WhatsApp …0408) дошло до CRM: запись в ленте + колокольчик. **Исходящее**: тестовая заявка с сайта на этот номер → в очереди `on_new_lead` (WhatsApp) → режим «Включено», «только на номера из списка» → отправщик отправил, Wazzup принял (`providerMessageId` записан), **пользователь подтвердил получение**. Статусы «доставлено/прочитано» отдельно не проверяли. После теста: отправщик снова **«Выключено»**, в списке разрешённых остался +7 987 271-50-04 (режим «только по списку» включён), тестовая заявка №199, клиент и входящие тестовые записи удалены (на stage снова 14 броней, 13 клиентов). Окно чатов не включали.
- Перед показом: `cleanup.sql` на stage и снять отметку «Включено вручную» у правила `before_checkin_24h` (след e2e МФ-2).
- Табель: должности и сотрудников заводит владелец — «Табель» → «Настроить» (`/admin/staff/people`); без них сетка пустая.
- Касса: смена не открыта; e2e кассы на stage открывает и закрывает свою тестовую смену и убирает за собой.

## 3. Что дальше (по приоритету пользователя)

1. **Ф6** (агент в `l8-comm`) → два независимых ревьюера → правки → подтверждающее → слить в main → e2e → stage.
2. **Ф7** (сообщения по времени: напоминание за 24 ч, «не приехали») — после Ф6 (общие файлы `dispatcher.ts`, `seed.ts`, `transition.ts`); архитектура и критика — `PHASE_07_TIMED_MESSAGES.md`, критику с блокерами сначала разрешить и **урезать под ТЗ**.
3. Показ заказчику (нужны ответы §4 п. 1–5).
4. После показа: Ф9б (табло), Ф12 (финансовый отчёт), МФ-3а (бэкапы), МФ-3б (подготовка к бою) — по `MAP_DEPENDENCIES.md`.

Порядок работы с фазой: агент дорожки (код + тесты, без ревью) → 2 независимых ревьюера параллельно (сервер/деньги; интерфейс/роли/ТЗ) → правки блокеров → подтверждающий ревьюер (дифф + слияние с main) → `ff` в main → `lane-full` на main → выкатка (§5).

## 4. Вопросы пользователю (ждут ответа; до ответа — по рекомендации, как сделано)

**Коммуникация (нужно для показа):**
1. ~~Wazzup: подключить аккаунт к stage~~ — **сделано 25.09** (см. §2); чужого вебхука на аккаунте не было. Осталось: ответить, тот ли это аккаунт заказчика и не нужно ли что-то отключить.
2. Каналы видны через API: **WhatsApp …0408 работает, Telegram …6444 не оплачен, MAX нет**. Осталось: какой тариф (в API его нет; при «Max без лимита» порог 500 диалогов даст ложные предупреждения — `wazzup.dialogLimit`), оплачивать ли Telegram.
3. ~~Тестовые номера~~ — тест отправки сделан на +7 987 271-50-04 (номер пользователя, остался в списке разрешённых). Нужны ещё номера — какие и в каких мессенджерах? Номер канала …0408 сам себе писать не может.
4. Тексты «при заезде» (Влад) и ссылки на отзывы (Яша) не пришли — для показа наши черновики; заказчик заменит в настройках?
5. Клиент выбрал Telegram, а канала нет — писать ли ему в WhatsApp? (рек.: нет, сообщение ждёт)

**Ф5:** нумерация договоров с 001 или продолжить бумажную (рек.: с 001; стартовое значение — одна строка при выкатке); нужен ли номер договора на экране охраны (рек.: нет); включать ли «Автомобиль принят» до текста Влада (рек.: после показа) и «Спасибо» до ссылки Яши (рек.: после ссылки); задержка «спасибо» 2 ч — подтвердить. **Заметка:** «не беспокоить» не режет «Спасибо и отзыв» (`MARKETING_RULES` Ф4 знает только `before_checkout_2d`, `after_checkout_7d`) — считать ли просьбу об отзыве рекламой?

**МФ-1:** блокировать включение автоподтверждения, пока нет отправщика и текста отказа (рек.: да); при перегрузке базы бронь «Новая» с пояснением (рек.: да); уведомлять ли админа, когда заявка осталась «Новой» при полной стоянке (рек.: нет); антибот — создавать заявку «возможен бот» вместо молчаливой потери (рек.: создавать).
**Ф10:** досрочный выезд при ступени тарифа — «не дороже плана» (рек.: оставить) или по цене суток брони.
**Ф2б:** фура «по запросу», уехавшая из перестоя — отдельный сигнал админу (рек.: нет).
**Ф11 (касса):** закрыть открытую смену может любой администратор, не только владелец (рек.: да — пересменка); наличные без открытой смены сейчас проходят и не попадают ни в одну смену — запрещать? (рек.: пока нет, выключатель добавить, когда заказчик начнёт вести кассу в системе).
**Ф13 (табель, сделано по рекомендациям):** часы смен день 08–20, ночь 20–08, сутки охраны с 08:00, до 02:00 — вчерашняя ночь; график на будущее не ведём, только факт (ТЗ 6.2 говорит «контролировать график» — подтвердить); администратор не ставит и не снимает смены себе.

## 5. Выкатка на stage (как делали 25.09, работает)

1. Бэкап: `ssh root@168.231.118.173 'cd /opt/parking24 && docker exec parking24-db pg_dump -U parking24 -Fc parking24 > backups/<имя>.dump'`.
2. `cd ~/Parking24` → rsync **с исключениями и БЕЗ `--delete`**: `--exclude node_modules --exclude .next --exclude .git --exclude .env --exclude '.env.*' --exclude backups --exclude .claude --exclude tsconfig.tsbuildinfo`; сначала `-n --itemize-changes`.
3. На сервере `cd /opt/parking24 && nohup docker compose up -d --build web > /tmp/p24-deploy.log 2>&1 &` → `docker logs parking24-web`: миграции применены, `seed ok`, `[scheduler] минутный тик включён`.
4. e2e на stage (bash-скрипт, пароли `E2E_*_PASSWORD` и секрет крона из `/opt/parking24/.env`; сценарии — `tests/README.md`), затем `docker exec -i parking24-db psql -U parking24 -d parking24 < tests/e2e/cleanup.sql` (запускать на сервере из `/opt/parking24`).
5. `DEPLOYED_COMMIT` (`<хэш> <UTC время> <фазы>`), сверка `rsync -azn --checksum` и `--delete -n` — пусто.

## 6. Подводные камни (узнали 25.09)

- **Слияние схемы «объединить обе стороны» — только с `npx prisma validate` сразу после**: при разрешении конфликта потерялась закрывающая `}` модели `CashCollection`, а устаревший Prisma-клиент скрыл это (tsc упал только из-за `staff.ts`). Скрипты проверок дорожек теперь падают на `PRISMA INVALID`.
- **Имя миграции = порядок применения.** Миграция Ф5 шла раньше уже влитых миграций Ф13 — переименована в `20260925020000_…` до слияния (на stage не применялась). Новые миграции — только с временем после последней в `prisma/migrations`.
- `sed -i` на macOS требует `sed -i ''`; в zsh `for x in "a b"` не делит на слова — циклы гонять `bash -c`.
- `tests/e2e/cleanup.*` удаляет клиентов с номерами `+7999…` — реальный диапазон мобильных; на бою с живыми клиентами cleanup не запускать (смена префикса — задача МФ-3б).
- Старые дорожки (`integration`, `l1-notices`, `l1b-autoconfirm`, `l2-occupancy`, `l4-send`, `l4b-wazzup`, `l5-settings`, `l6-money`, `l7-timesheet`, `l3-ui`, `qa`) полностью в main, их можно удалить (`git worktree remove`), базы `parking24_l*` — тоже.
- Лимит сессии API: около 16:45 по Джакарте агенты падали с 429; после сброса — продолжать `SendMessage` агенту (контекст сохраняется).

## 7. Стартовый промпт нового чата

```
Продолжаем Parkovka24. Прочитай ~/Parking24/docs/SESSION_2026-09-25_STAGE_REPORT.md целиком и блок СТАРТ в
~/Parking24/HANDOFF.md. ГЛАВНОЕ ПРАВИЛО: не усложнять. Проверь git status main и дорожки l8-comm
(~/Parking24-lanes), stage (/opt/parking24/DEPLOYED_COMMIT). Затем: доведи Ф6 (ревью → слияние → stage),
потом Ф7 (сначала разреши блокеры критики и урежь под ТЗ), затем подготовь показ заказчику.
Не спрашивай подтверждений, параллель процессы, вопросы ко мне — из §4.
```
