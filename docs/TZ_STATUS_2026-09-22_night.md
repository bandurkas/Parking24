# Что не доделано по ТЗ, по фазам (сверка 22.09.2026, поздний вечер, код `ff68709`)

Повторная сверка перед переходом на параллельные дорожки. Работали 4 сверщика, 4 критика и сводчик. Каждая строка проверена по коду на `ff68709`; после него в `main` есть только коммит `3a13cc5`, в нём одни документы. Документы использовались, только чтобы понять намерение. Критики подтвердили все строки сверщиков и ни одного статуса не поменяли. Они поправили два подсчёта: в разделах 3–4 строк 45, а не 43, в разделах 5–7 — 82, а не 76. Ещё они добавили пункты, из которых в таблицы вошли 12 новых строк. Прежние таблицы остались в `docs/tz_check_2026-09-22/`, это отправная точка.

**Вводная задачи устарела: СП уже на stage.** `/opt/parking24/DEPLOYED_COMMIT` = `ff68709`, выкачено 2026-09-22 15:05 UTC. Подтверждающее ревью раунда 3 прошло чисто (`PHASE_SP_URGENT_FIXES.md` §13, коммит `3a13cc5`). Сводчик на чтение проверил stage (только SELECT и `cat`) и получил:
- в Outbox PENDING 5, CANCELLED 2;
- все 6 правил активны, включая `before_checkout_2d` и `after_checkout_7d`;
- `parking.autoConfirm = false`;
- ни у одной из 13 броней `paidAmount` не расходится с суммой платежей;
- броней парковки без типа машины 0;
- в crontab VPS2 нет строк parking24;
- `http://168.231.118.173:8500/admin/login` отвечает 200 по HTTP.

Статусы: ✅ done, 🟡 partial, ⬜ missing.

## 1. Итог в цифрах

| Раздел ТЗ | Пунктов | ✅ | 🟡 | ⬜ | Было 22.09 вечер |
|---|---|---|---|---|---|
| 1–2. Бронирования и уведомления | 67 | 21 | 19 | 27 | 49: 12 / 14 / 23 |
| 3–4. Интерфейс администратора и занятость | 47 | 14 | 20 | 13 | 38: 13 / 12 / 13 |
| 5–7. Финансы, табель, касса | 86 | 12 | 8 | 66 | 71: 6 / 3 / 62 |
| **Всего по ТЗ** | **200** | **47** | **47** | **106** | **158: 31 / 29 / 98** |
| Сквозные хвосты (раздел 8, ревью, аудит 05.09, HANDOFF, готовность) | 103 | 16 | 16 | 71 | 86: 10 / 7 / 69 |

**Что изменилось и почему.**
- **Код.** С `9f9fe7c` менялась только СП (`fcb9ac7`, `020a018`, `ff68709`). Остальные хвосты не тронуты, в `bookings.ts` сдвинулись номера строк: `createBooking` :27-92, `transition` :125-205, `correctStatus` :208-254, `addPayment` :260-337, `decideRecalc` :358-378, `extendStay` :382-401, `waiveOverstay` :404-420, `updateBooking` :427-490.
- **Стали done по коду (СП).** Причина возврата (5.3 п.3, было ⬜), наличные по умолчанию (п.4, было 🟡), «не больше оплаченного» (⬜), «нет ложного долга» (⬜). Новые строки по решениям 22.09 вечера сразу done: выезд в перестое текущими сутками (в 1.1 и в 3.3), льготный час, снятие начисления, запрет правок в перестое, возврат по статусу, ответ 1а.
- **Стали partial.** «Ручное время двигает деньги» (3.3): в перестое дыра закрыта, вне перестоя нет. «Оформить возврат», «поправить сумму», «путь без баннера»: форма теперь подставляет переплату. Без изменений кода переоценены с ⬜ на 🟡: 3.1 п.2, 4.2 п.5, эталон 1/404/405. Шкала «расходится = partial».
- **Почему пунктов стало на 42 больше.** Сверщики разбили строки: видео, поле ссылки, плановый момент, «держит ли место Новая заявка», предоплата. Критики добавили входящие сообщения, предохранители отправщика, «фуры вручную» ✅, `userId = null` ✅, счётчик «заезд», забытый заезд, полный возврат до заезда, 4 способа оплаты в снимке смены, журнал платежа, каскад `Payment`. СП породила хвосты: «Продлить» против «бронь изменена», правки «Заехал» до перестоя, «Исправить статус» открывает закрытую бронь, сторно, снятие начисления при открытом баннере, забытый выезд.
- **Сквозные.** Закрытых стало 16 против 10: закрыты три дыры в деньгах (§2 п.1–3), сама СП и блок СТАРТ в HANDOFF (проверено по stage и `3a13cc5`). Открытых 71 против 69, новые:
  - сайт обещает «оплату после подтверждения»;
  - порт 8500 открыт наружу;
  - бэклог СП;
  - P2 аудита;
  - сверка STAGE2.

**Главное одной строкой:**
- Клиенту не уходит ни одно сообщение: отправщика нет, `SCANS = []` (Ф4, Ф5, Ф7).
- Касса, табель и отчёт не начаты (Ф11–Ф13).
- В CRM нет потолка 405, «занято сейчас» считается шестью способами (Ф3).
- Ручное время вне перестоя всё ещё двигает деньги: предлагаю Ф9а раньше всех.

## 2. Срочно: дыры на stage сейчас

**Выкатка СП на stage уже прошла и закрыла:**
1. Ручное время «Выехал» больше не снимает долг за перестой. Время не позже «сейчас» (`bookings.ts:133-136`), в перестое выезд только текущими сутками (`:146-149`, `lib/overstay.ts:54-56`).
2. Возврат больше оплаченного отклоняется. Проверки идут до `payment.create` под блокировкой строки (`bookings.ts:265-279`, `refund.ts:11`).
3. Ложного долга после возврата нет (`refund.ts:13`, `bookings.ts:304-307`).
4. Кроме того, на stage уже работают льготный час, снятие начисления с причиной (`bookings.ts:404-420`) и «Изменить бронь» в перестое без дат, суммы и типа (`:447-456`).

**Осталось и нашлось, по важности:**
1. **Ручное время вне перестоя двигает деньги.** Раньше в срочном стоял только вариант с перестоем. У времени выезда нет нижней границы, его можно поставить даже раньше заезда (`bookings.ts:133-136`). Сюда же попадает окно 00:00–01:00: машина с `dateTo = вчера` ещё не в перестое.

   Цепочка: выезд задним числом → меньше `actualDays` (`:158-160`) → «Пересчитать по факту» без причины (`decideRecalc` :358-378, `STAFF`) → переплата → администратор возвращает её наличными (`refund.ts:18-20`, причина нужна только у возврата). `transitionAction` принимает `at` и от охраны (`actions/bookings.ts:44-48`, `requireActor(ALL)`).

   Решение уже есть: PLAN §1, «ручное время убираем совсем». **Предлагаю Ф9а первой в дорожке Д2.** Это три файла и правка e2e, вопросов не нужно.
2. **Порт 8500 открыт наружу по HTTP.**
   - `docker-compose.yml:26` публикует `"8500:3000"` на всех интерфейсах, ufw на VPS2 выключен. Вход в CRM идёт открытым HTTP.
   - Cookie ставится без `secure`, признак берётся из заголовка `x-forwarded-proto` (`session.ts:26-30`).
   - Лимит заявок обходится подделкой `X-Forwarded-For` (`api/public/lead/route.ts:23-24`).

   Данные на stage тестовые. Место в МФ-3б, но строка `127.0.0.1:8500:3000` меняется в репозитории, а не на сервере: вопрос пользователю, можно ли сейчас.
3. **Автоподтверждение можно включить без предохранителя** (`actions/settings.ts:12-38`, `CapacityForm.tsx:72`). На stage оно выключено. Если владелец его включит:
   - автоотказ клиенту уходит молча, правила на REJECTED нет;
   - колокольчик гасит непоказанные отказы;
   - при наплыве заявок сервер отдаёт 500 (`autoconfirm.ts:31`).

   До МФ-1 логин владельца заказчику не давать.
4. **Сайт обещает то, чего нет.** «Подтверждение придёт в …», «мы напишем вам сами» (`BookingCalculator.tsx:180,185,202,395`), а сообщения не уходят. «Оплата после подтверждения, пришлём способ оплаты» (`:186`, `:395`) противоречит решению 3 «предоплаты нет, платят на въезде». Заказчик stage видит. Тексты — Ф4 (тот же блок экрана), отправка — Ф4.
5. **Данные stage для Ф4.** Висят 5 PENDING со старыми ключами, активны правила со скидкой 10 % (`seed.ts:98-99,109`, seed флаг `isActive` не трогает). Пока отправки нет, вреда нет. Убрать в шаге 0 Ф4.
6. **Бэкапов по крону нет.** `docker/backup.sh:2` описывает крон только в комментарии, в crontab VPS2 parking24 нет. Дампы делаются только вручную перед выкаткой. Нужна МФ-3а, её можно начать сейчас, если пользователь разрешит крон на VPS2.
7. VPS2 (ufw, root по паролю) по решению не трогаем, это МФ-3б на боевом сервере.

Проверено, дыр нет: расхождений `paidAmount` с суммой платежей после старого `Math.max(0,…)` на stage 0 из 13 броней.

## 3. Подробно по фазам

### Ф2б. Перестой: уведомления, колокольчик, режимы сканов (архитектура одобрена, `PHASE_02` §4.6–4.8)
- ⬜ 1.1, реш. 22.09: администратору один раз на перестой. `NoticeKind` без `OVERSTAY` (`schema.prisma:53-58`), у `AdminNotice` нет `dedupKey` (`:489-502`), `SCANS = []` (`scheduler.ts:21`).
- ⬜ §4.6 п.24 «выехал, не оплачено N ₽»: в ветке «Выехал» уведомления нет (`bookings.ts:143-164`).
- ⬜ Колокольчик гасит все уведомления: `NoticeBell.tsx:18` → `notices.ts:23-27`, показывается только 20 (`:10`). Нет проверки роли (`actions/settings.ts:48`). Бейдж без опроса (`AdminShell.tsx:9`, `/api/admin/notices` нет). `revalidatePath("/admin","layout")` бьёт мимо группы (`actions/settings.ts:50`, `actions/bookings.ts:24`, `actions/clients.ts:145,158`).
- ⬜ Режимы сканов в «Планировщике»: `setScanModeAction` нет.
- 🟡 1.2 «отклонена: нет мест» администратору: часть про колокольчик (перенос в транзакцию — МФ-1).
- Даёт другим фазам: `notify(…, {tx, key})` (нужен МФ-1 и Ф11), реестр сканов с режимами (нужен Ф7).

### МФ-1. Автоподтверждение: надёжность
- ⬜ Настройки читаются вторым соединением изнутри транзакции, у транзакции нет таймаута (`autoconfirm.ts:31` → `settings.ts:32-34`; `bookings.ts:35-36`).
- ⬜ Нет условия «цена > 0» (`autoconfirm.ts:32-35`). Сумма считается в `bookings.ts:33` и в решение не передаётся.
- ⬜ Дубль заявки ищется вне блокировки (`leads.ts:45-55`); при выключенном АП блокировки нет вовсе (`autoconfirm.ts:32`).
- ⬜ Уведомление об отказе создаётся после коммита, в тексте сырые ISO-даты (`leads.ts:100-102`).
- ⬜ Предохранитель: АП не включается без правила на «Отклонена» и отправщика (`actions/settings.ts:12-38`). Проверку можно написать до Ф4 и Ф5: она просто не даст включить АП.
- ⬜ Антибот опирается на часы клиента (`lead/route.ts:42`): при спешащих часах заявки нет, а сайт пишет «принята».
- 🟡 Сводная строка 1.1 «блокировка без дыр» закрывается пунктами выше.
- Экран результата сайта (`BookingCalculator.tsx:196`, в TZ_STATUS — Ф9) предлагаю перенести в Ф4: это тот же блок, что «Открыть бота».

### Ф3. Занятость по ТЗ и потолок 405 в CRM
- 🟡 4.1: категории 60/30/10/5 из `CapacityConfig` (`seed.ts:33-36`, `occupancy.ts:27-32`). У «Груз» /5 против 10 (`settings.ts:9`). Строки краснеют при свободном пуле (`occupancy/page.tsx:89-92`).
- ⬜ 4.1: флаг «заглушка» гаснет при любом сохранении (`actions/settings.ts:34`).
- 🟡 4.1: полоса «занято» без «Ожидает оплаты» и по /60… (`occupancy.ts:154-166`, `TodayStrip.tsx:16-31`, `TodayBoard.tsx:34-44`).
- 🟡 4.2 п.2 «заезды за 24 ч»: две календарные даты (`occupancy.ts:131`). Всего три разных числа: доска `boards/[kind]/page.tsx:36` (с «Новой», только сегодня), «Прибытие» `today/page.tsx:27`, панель.
- 🟡 4.2 п.3 «выезды»: на панели `dateTo ≤ завтра` (`occupancy.ts:133`), на «Сегодня» и доске `≤ сегодня` (`today/page.tsx:28`, `boards/[kind]/page.tsx:38`).
- 🟡 4.2 п.4 «свободно» = 415 − брони (`occupancy.ts:135,137,143`). 🟡 п.5 «всего» 415 (`ParkingSummary.tsx:23`). 🟡 Эталон 1/404/405 выдаёт 1/414/415, e2e нет. 🟡 Порог на панели (`ParkingSummary.tsx:24-25,62-66`).
- 🟡 «Одно определение занято», сейчас их шесть: `occupancyToday`, `poolLoad`, `held` на панели, `held` на странице ёмкости (`settings/capacity/page.tsx:13,23`, `CapacityForm.tsx:79`), дашборд с комнатами (`dashboard/page.tsx:20`), сетка категорий с «Ожидает оплаты» (`occupancy.ts:45-55`).
- ⬜ Потолок при создании в CRM: `createBooking` без проверки (`bookings.ts:27-92`). Быстрая бронь сразу в «Ожидает оплаты» или «Подтверждена» (`validation/booking.ts:22`, `actions/bookings.ts:37`).
- ⬜ Потолок при «Подтвердить место» и возврате из «Отклонена» (`transition` :125-205; `labels.ts:55,62`). Резерв 10 мест можно переполнить вручную.
- ⬜ **Не записано нигде:** «Исправить статус» в «Ожидает оплаты» или «Подтверждена» из «Отклонена» или «Отменена» (`bookings.ts:208-254`). Проверки мест нет, автоматизации не запускаются, подтверждение клиенту не встанет.
- ⬜ Потолок при правке дат (`updateBooking` :427-490); сообщение об изменении — Ф6.
- ⬜ Подсказка «свободно N» по категориям (`actions/bookings.ts:161-175`, `QuickBookingDrawer.tsx:225-229`, `EditBooking.tsx:26,70`).
- ⬜ Вместимость сохраняется без сверки с занятостью (`actions/settings.ts:23-26`): опечатка 45 закроет приём заявок.
- ⬜ `plannedMoment` зависит от пояса процесса (`dates.ts:81-88`) и нигде не вызывается. Нужен для окон 24 ч (Ф3, Ф7, Ф9).
- ⬜ `fits` при `to < from` (`occupancy-math.ts:50-52`). ⬜ Нет сервисных тестов выборки: в tests нет импорта `services/occupancy`.
- 🟡 Старые брони без типа машины входят в `held`, но не в пул (`occupancy.ts:135` против `:92-94`). На stage таких 0.
- 🟡 Трактовка 1.1 «менее 395»: сейчас проверяется только пик на датах заявки (`autoconfirm.ts:39-41`). 🟡 Держит ли место «Новая заявка» (`occupancy.ts:8`) — вопрос 3.
- 🟡 Ранний заезд: потолок здесь, предупреждение — в Ф9б.
- e2e: `overstay.mjs:62,90` читают подпись «занято бронями», переписать вместе с панелью.

### Ф4. Telegram и отправщик
**Шаг 0, без токена:**
- ⬜ Отменить 5 старых PENDING на stage.
- ⬜ Выключить `before_checkout_2d` и `after_checkout_7d`; seed должен создавать их выключенными (`seed.ts:98-99,109`).
- ⬜ Строгая проверка ЧЧ:ММ (`validation/booking.ts:4`, `dates.ts:75,83`, `leadSchema` :32-33): «25:00» попадает в сообщение.
- ⬜ Громкий сбой seed (`docker/entrypoint.sh:7`). От seed зависят done-строки: `dedupGroup`, тексты ред. 3.

**Основная часть:**
- ⬜ Отправщик Outbox и бот. Нет `chat_id` (`Client.telegram` — строка, `schema.prisma:246`), нет токена для `/start`, вебхука, кнопки «Открыть бота» на экране результата (`BookingCalculator.tsx:175-237`).
- ⬜ Предохранители (PLAN §6): не слать сообщения старше N часов, на время теста только разрешённые номера.
- 🟡 Канал: по умолчанию WHATSAPP (`dispatcher.ts:40`), «не беспокоить» не проверяется (`schema.prisma:255`), заявка без телефона ставится без клиента (`dispatcher.ts:32`).
- ⬜ Честный статус очереди в карточке: `SKIPPED_NO_PROVIDER` не ставится, время «запланировано» по UTC (`bookings/[id]/page.tsx:208`, `dates.ts:49-52`). Всю строку :208 делает Ф4, а не Ф9.
- 🟡 1.1 усл.3, 2 абз.1, 2.1: подтверждение пока только встаёт в очередь (`bookings.ts:89,201,333`).
- ⬜ Входящие (PLAN этап 2а): вебхук пишет входящие в ленту брони и в колокольчик. Окно чатов — Ф14.
- ⬜ e2e: Outbox после автоподтверждения, оживление отменённой записи (`dispatcher.ts:31,45`). Тест на `dedupGroup` уже есть (`crm-pipeline.mjs:67,137`).
- ⬜ Экран результата сайта: заголовок по `state` (`BookingCalculator.tsx:196`), честные обещания (`:180-202`, `:395`), без «оплаты после подтверждения» (`:186`).
- Новые, предлагаю сюда:
  - ⬜ перенос канала у PENDING при смене мессенджера в карточке клиента (сейчас переносит только дубль с сайта, `leads.ts:61`);
  - ⬜ `admin.notifyPhone`, уведомление администратору в мессенджер (ключа нет, `settings.ts:7-18`), последним шагом, при письменном «да» заказчика.

### МФ-2. Настройки CRM
- ⬜ Страница «Сообщения»: вкл/выкл, текст, предпросмотр, признак ручной правки вместо `sync: true` (`seed.ts:92`). Ссылки `settings/page.tsx:13-14` ведут на 404.
- 🟡 Ссылки маршрута и отзыва задаются только в коде (`settings.ts:15-18,61`); у отзыва нет запасного значения, строка выпадает (`render.ts:79`). ⬜ Поле ссылки на видео (само видео — от заказчика).
- ⬜ Пользователи и смена пароля, тарифы, политика отмены (`settings/page.tsx:9-11` → 404).
- Нужна не позже Ф5.

### Ф5. Сообщения по статусам
- ⬜ 1.2 абз.2 и 2.4: два текста отказа (нет мест / отклонено). Шаблонов и правила на REJECTED нет (`templates.ts:59-68`, `seed.ts:95-102`). Выбирать по `rejectKind` нечем: диспетчер фильтрует только `status` и `source` (`dispatcher.ts:17-19`). При подтверждении из резерва отказ в очереди не отменяется (`bookings.ts:200`).
- ⬜ 2.3: сообщение при «Заехал» (ждём текст Влада).
- ⬜ 2.3: номер договора 001…. `Booking.contractNumber` нет (`schema.prisma:288-349`), `dispatcher.ts:33-34` его не передаёт (`render.ts:35`). Что с номером после «Исправить статус», не решено (`bookings.ts:218`).
- ⬜ 2.7: «спасибо + отзыв» через 2 ч. Вместо него `thanks_discount` через 7 дней (`templates.ts:64`, `seed.ts:99`); `links.review` пустой.
- 🟡 2: тексты семи сообщений, есть 4 (вместе с Ф6 и Ф7).
- 🟡 Фура с ценой 0: в сообщении нет «оплачено» (`render.ts:48-51`). Автопереход при оплате — Ф10.

### Ф6. Изменение брони
- ⬜ 1.3 и 2.6: «бронь изменена». `updateBooking` автоматизаций не вызывает (`bookings.ts:427-490`), в `AutomationTrigger` нет `BOOKING_CHANGED` (`schema.prisma:116-121`).
- 🟡 Смена только времени или типа пишется в ленту как «данные брони» (`bookings.ts:480-486`).
- ⬜ **Новое после СП, дописать в состав:** «Продлить» (`extendStay` :382-401) — единственный путь продления в перестое. Решить, уходит ли клиенту «изменена». Начисление при выезде двигает `dateTo` (`:152`) и сообщения давать не должно.
- 🟡 Бэклог СП §12: у «Заехал» до перестоя можно продлить без роста суммы и сменить тип машины без причины (`bookings.ts:453-456`).
- ⬜ Неотправленные сообщения по старым датам остаются: текст рендерится при постановке (`dispatcher.ts:35`).
- ⬜ Ключ дедупликации без даты (`dispatcher.ts:28`): второе «изменено» или напоминание не встанет (общее с Ф7).
- ⬜ Клиент меняет бронь с сайта (`BookingCalculator.tsx:236`, `leads.ts:45-63`) — вопрос 4.
- ⬜ Проверка реальной доставкой в Telegram — после Ф4.

### Ф7. Сообщения по времени
- ⬜ 2.2: напоминание за 24 ч. Правило `before_checkin_24h` никто не читает (`seed.ts:97`, `dispatcher.ts:13`, `SCANS = []`).
- ⬜ 2.5: «не воспользовались» через 24 ч, нет `AFTER_PLANNED_CHECKIN`.
- 🟡 Автопереход в «Не приехал» через 48 ч: переход есть (`labels.ts:56`), но `autoNoShowAfterHours` = 24 (`schema.prisma:412`, `seed.ts:87`) и скана нет.
- ⬜ Холодный старт сканов: `startFrom`, лимит на тик, оплаченные не трогать. Обратный переход «Не приехал» → «Ожидает оплаты» (`labels.ts:61`).
- ⬜ Повтор после смены дат — ключ с датой, общее с Ф6.
- Проверить решение «тихих часов нет». Сверить разборы `STAGE2_DESIGN` с кодом (Ф4–Ф7).

### Ф8. «Не смогли попасть»
- 🟡 1.2: сегмент. Данные сохраняются (`bookings.ts:38,65-66`), фильтра нет, `NO_SPACE` читается только в `bookings/[id]/page.tsx:62`.
- ⬜ Согласие на рекламные рассылки: сайт ставит только `consentPersonalAt` (`leads.ts:103-106`), `consentMarketingAt` меняется вручную (`actions/clients.ts:119`) — вопрос 5.
- Для истории отказов нужны сохранённые отметки отклонения, это Ф10.

### Ф9а. Убрать ручное время (предлагаю вынести из Ф9 первой в Д2)
- ⬜ 3.3: форма `datetime-local` (`TransitionButtons.tsx:31,57-80`), `window.prompt` на доске (`KanbanBoard.tsx:81-89`), параметр `at` (`actions/bookings.ts:44-48` → `bookings.ts:133-136`).
- 🟡 3.3 «система сама фиксирует время» и 🟡 «ручное время двигает деньги» — см. §2 п.1.
- e2e: `crm-pipeline.mjs:73`, `overstay.mjs:54,128,187` ищут поле времени. Проверку отказа «не текущими сутками» придётся заменить.

### Ф9б. Табло, фильтр, московское время, мелочи
- ⬜ 3.1: отдельное табло «Планируют выезд», окно 24 ч от текущего момента (`TodayBoard.tsx:55`, `today/page.tsx:28`), нужен `plannedMoment` из Ф3.
- 🟡 3.1 п.2: «Отправление» печатает время заезда (`TodayBoard.tsx:101`), у охраны нет времени (`GuardScreen.tsx:94`). Время выезда есть только в карточке (`bookings/[id]/page.tsx:118`).
- 🟡 3.1 п.4: статус скрыт на экранах уже 1280 px (`TodayBoard.tsx:116`), у охраны статуса нет.
- 🟡 3.1 у охраны: вкладка «Выезд» = все «Заехал» (`GuardScreen.tsx:24-25`).
- ⬜ 3.2: фильтр «Планируют выезд» (`BookingsTable.tsx:17-29,50-67`; в `KanbanItem` нет `timeTo`, `KanbanBoard.tsx:16-21`, `boards/[kind]/page.tsx:42-63`).
- 🟡 3.3 треб.1: московское время в журнале (`audit/page.tsx:21`) и в часах (`Clock.tsx:13-14`). Платёж (`PaymentPanel.tsx:83`) делает Ф10, очередь — Ф4, TZ контейнера — МФ-3б.
- ⬜ Решение 3 против подписей: главная кнопка «Подтвердить» ведёт в CONFIRMED (`labels.ts:67-70`, `TransitionButtons.tsx:11`). Короткий статус CONFIRMED — «Оплачена» (`labels.ts:17`) даже без денег. «Подтвердить» в быстрой брони ставит CONFIRMED без оплаты (`QuickBookingDrawer.tsx:285,318`).
- ⬜ Дата по умолчанию в быстрой брони по UTC (`QuickBookingDrawer.tsx:28-32`). ⬜ У охраны «Заехал» активна для «Новой» (`GuardScreen.tsx:82`). 🟡 Ранний заезд без предупреждения (`bookings.ts:139`).
- ⬜ 3 ошибки eslint (`Clock.tsx:8`, `QuickBookingDrawer.tsx:117`). ⬜ Канбан не помещается в 1440 px (`KanbanBoard.tsx:24,150`). ⬜ На клавиатуре охраны из букв только «А» (`GuardScreen.tsx:42`).

### Ф10. Возврат при досрочном выезде и «Исправить статус»
- 🟡 5.3 и 8.5: расчёт возврата.
  - Баннер считает `actualDays × цена суток` (`PriceTools.tsx:13`), действие — `quote(actualDays)` (`bookings.ts:366`); от 30 суток суммы расходятся.
  - Пересчёт по тарифу теряет ручную цену и скидку (`Booking.discount` не используется).
  - Поздний заезд без перестоя показывает «Переплату» (`:161-163`).
- 🟡 «Оформить возврат» с готовой суммой (`PriceTools.tsx:26-27`, `PaymentPanel.tsx:50`). 🟡 Если вернуть меньше переплаты, остаток висит на брони (`PaymentPanel.tsx:48-50`). 🟡 Возврат, когда баннера нет (`page.tsx:71`, `bookings.ts:364,162-163,275-278`).
- ⬜ Бэклог СП §13: снятие начисления при открытом баннере (`waiveOverstay` :404-420).
- ⬜ Отдельное поле `Payment.reason`: сейчас причина пишется в `note` (`bookings.ts:292`).
- ⬜ «Исправить статус» открывает закрытую бронь, «Выехал» → «Заехал» (`bookings.ts:208-228`), и обходит `assertMoneyEditable` (`:103-105`). Переход в «Отменена» из перестоя не пишет «не начислен» (`:241`). ⬜ Сторно ошибочного платежа.
- 🟡 Отметки отклонения стираются (`bookings.ts:176-180`, `:222`), отчёт поменяется задним числом. В TZ_STATUS это Ф10, в part4 — Ф12; беру Ф10.
- 🟡 5.3 п.6: время платежа по Москве (`PaymentPanel.tsx:83`).
- Новые, предлагаю сюда:
  - ⬜ полный возврат до заезда оставляет «Подтверждена»: статус повышается только при оплате (`bookings.ts:328`);
  - ⬜ фура с ценой 0 становится «Подтверждена» от любой оплаты (`:328`);
  - ⬜ забытый заезд или выезд через «Исправить статус»: ставится время исправления (`:224-225`), `actualDays` не считается; при исправлении сразу в «Выехал» `checkedInAt` = null.

### МФ-3а. Бэкапы
- ⬜ Крон, копия вне VPS, проверенное восстановление (`docker/backup.sh:2`; в crontab VPS2 parking24 нет). Не позже Ф11, на боевом сервере повторить.

### Ф11. Касса
- ⬜ Раздел 7 целиком.
  - 7.1: учёт по сменам, имя, время открытия, начальный остаток из прошлой смены с расхождением при приёме.
  - 7.1 абз.2 (🟡): автор платежа есть, смены нет. `Payment.cashShiftId` нет (`schema.prisma:351-368`).
  - Касса одна: частичный уникальный индекс в SQL или блокировка.
  - Наличные вне смены: ключа `cash.requireShift` нет (`settings.ts:7-18`), `addPayment` смену не проверяет.
- ⬜ 7.2: форма «Закрыть смену» со всеми полями, «Скопировать отчёт», повторное подтверждение, пересменка, закрытие чужой смены владельцем, рубли вместо «Rp». Выход смену не закрывает, при открытой смене — напоминание (`login/actions.ts:34-39`, `Sidebar.tsx:58-62`, `GuardScreen.tsx:56`).
- ⬜ 7.3: формула с юнит-тестом. Фактический остаток и уведомление: `CASH_MISMATCH` есть в схеме (`schema.prisma:56`), но не используется. В формуле только возвраты наличными, в снимке возвраты по способам.
- ⬜ 7.4: инкассация (сумма, время, кто забрал, «кто передал» текстом, смена), уменьшает остаток, история, отмена ошибочной.
- ⬜ 7.5: снимок при закрытии, привязка к дате смены (ночная — вопрос 8), администратор видит свои смены.
- ⬜ 5.3 абз.3: возврат уменьшает наличные и привязан к смене. ⬜ Права на кассу. ⬜ Индекс `Payment(kind, status, paidAt)` (`:367`). 🟡 Модели `CashShift` и `CashCollection` (значения `NoticeKind` уже есть).
- Новые, предлагаю сюда:
  - ⬜ снимок смены по всем 4 способам, включая TRANSFER и ONLINE (`schema.prisma:78-83`);
  - 🟡 журнал `CREATE Payment` хранит id брони и не хранит способ (`bookings.ts:325`);
  - ⬜ `Payment → Booking onDelete: Cascade` заменить на Restrict (`schema.prisma:364`).

### Ф12. Финансовый отчёт
- ⬜ 5: раздел `/admin/reports` и пункт меню (`Sidebar.tsx:12-21`), права.
- ⬜ 5.1: период с 1-го числа, произвольный период, границы по Москве. Помощника в `dates.ts` нет; дашборд считает месяц по UTC (`dashboard/page.tsx:13,16-19`), его перевести на тот же помощник.
- ⬜ 5.2 п.1–6: заехавшие по `checkedInAt` с фильтром вида брони; оплаты, наличные, карты; все 4 способа со сверкой с итогом; всего заявок; отклонённые.
- ⬜ Возвраты отдельной строкой: дашборд вычитает их из выручки (`:17,27`). ⬜ Список смен за период. ⬜ Выгрузка и печать: сейчас есть только CSV броней (`BookingsTable.tsx:34-42`). ⬜ Охват — вопрос 7.
- 🟡 Дашборд §11: конверсия по источникам, средний чек, повторные.

### Ф13. Табель
- ⬜ Раздел 6 целиком: календарь смен, должности, дата смены, число смен за период, слоты день/ночь/сутки с часами в записи, табель, отчёт за месяц или период, данные для зарплаты с печатью, ставка на момент смены, снятие ошибочной отметки с журналом.
- ⬜ Схема: справочник должностей, `Employee` (ставка, связь с `User`), `WorkShift` со снимком ставки, уникальность «сотрудник + дата + слот».
- ⬜ Открытие кассы само отмечает смену администратора; после Ф11 дозаполнить отметки из истории касс.
- Противоречия, нужны решения:
  - «сотрудник сам отмечает смену» против «логины только у администраторов», при этом у охраны логин есть (`seed.ts:13`);
  - 3 должности против 5;
  - DAY/NIGHT против FULL.

### Ф14. WhatsApp через Wazzup, окно чатов
- ⬜ Отправка в WhatsApp. ⬜ Окно чатов и ответ из CRM (PLAN этап 2б). ⬜ MAX (`tariffs.ts:33,38`, выбор выключен) — вопрос 6.

### МФ-3б. Подготовка к бою
- ⬜ Демо-брони только при `SEED_DEMO=1` (`seed.ts:115-116`), пароли без умолчаний (`seed.ts:7,11-13`).
- ⬜ `TZ=Europe/Moscow` в compose и Dockerfile.
- ⬜ Блокировка перебора паролей (`login/actions.ts:23-25`). ⬜ Заголовки безопасности и `poweredByHeader` (`next.config.ts:3-6`, `proxy.ts:40`).
- ⬜ CSV броней только владельцу, с записью EXPORT в журнал (`BookingsTable.tsx:34-40,70`). ⬜ 2FA по желанию (`totpSecret`, `schema.prisma:169`). ⬜ Мониторинг.
- ⬜ Домен, robots (`robots.ts:6`), sitemap, canonical. ⬜ Боевой WhatsApp 79055250660 (`tariffs.ts:30`). ⬜ 152-ФЗ и юрист. ⬜ ufw и ssh на боевом сервере.
- Новое, сюда: ⬜ порт только на 127.0.0.1 (`docker-compose.yml:26`); IP клиента брать только от прокси (`lead/route.ts:23-24`); `secure` у cookie не из заголовка (`session.ts:26-30`).

### Пункты «новая»: куда положить

| Пункт | Где в коде | Предлагаю |
|---|---|---|
| Экран результата: обещания и «оплата после подтверждения» | `BookingCalculator.tsx:180-202,395`, `templates.ts:19` | Ф4 |
| Перенос канала у PENDING при смене мессенджера | `leads.ts:61` (только дубль с сайта) | Ф4 |
| `admin.notifyPhone` | ключа нет | Ф4, последний шаг |
| Фура 0 ₽ становится «Подтверждена» от любой оплаты | `bookings.ts:328` | Ф10 |
| Полный возврат до заезда, статус остаётся «Подтверждена» | `bookings.ts:328` | Ф10 (решение) |
| Забытый заезд или выезд: время исправления, нет `actualDays` | `bookings.ts:224-225` | Ф10 (решение) |
| «Исправить статус» в «Ожидает оплаты»/«Подтверждена» без проверки мест | `bookings.ts:208-254` | Ф3 |
| «Продлить» и начисление против «бронь изменена» | `bookings.ts:152,382-401` | Ф6 |
| Журнал платежа, каскад `Payment` | `bookings.ts:325`, `schema.prisma:364` | Ф11 |
| Порт 8500, `X-Forwarded-For`, `secure` у cookie | см. МФ-3б | МФ-3б (порт — раньше, если разрешат) |
| MAX | `tariffs.ts:33` | Ф14, если «да» |
| Заявка с `/rooms` в CRM (P1-7) | `rooms/page.tsx:110` | вне ТЗ 21.09, решение пользователя; отдельная МФ после Ф6 |
| Страницы `/tariffs`, `/faq`, `/contacts`, JSON-LD (P1-8) | файлов нет | вне ТЗ, в МФ-3б (SEO) по решению |
| P2 аудита: карточка v2, кампании, ЮKassa, PWA, шифрование ПД | — | после запуска |

## 4. Зависимости и пересечения для параллельной работы

| Фаза | Зависит от (что раньше и почему) | Трогает (по коду) | Внешние блокеры |
|---|---|---|---|
| Ф2б | — | `schema` (`NoticeKind`, `AdminNotice.dedupKey`, 2 миграции), новый `automations/scans/overstay.ts`, `lib/overstay.ts`, `tick-core.ts`/реестр, `scheduler.ts`, `bookings.ts` (`transition`, «Выехал»), `notices.ts`, `settings.ts`, `actions/settings.ts`, новый `api/admin/notices`, `NoticeBell`, `AdminShell`, `SchedulerCard`; e2e `overstay`, `scheduler` | — |
| МФ-1 | Ф2б (`notify(tx, key)`) | `autoconfirm.ts`, `leads.ts`, `settings.ts` (`parkingSettings(tx)`), `bookings.ts` (`createBooking`), `actions/settings.ts` (`saveCapacityAction`), `CapacityForm`, `lead/route.ts`; e2e `autoconfirm`, `site-lead` | — |
| Ф8 | МФ-1 (`leads.ts`); мягко Ф10 (отметки отклонения) | `leads.ts`, `lead/route.ts`, `validation/booking.ts` (`leadSchema`), `BookingCalculator` (форма), `clients/page.tsx`, `services/clients.ts`; `Client` (согласие) | вопрос 5; юрист (текст согласия) |
| Ф9а | — | `TransitionButtons`, `KanbanBoard`, `actions/bookings.ts` (`transitionAction`), `bookings.ts` (`transition`, `at`); e2e `crm-pipeline:73`, `overstay:54,128,187` | — (решение есть) |
| Ф3 | Ф9а (тот же `transition`), МФ-1 (`createBooking`, `saveCapacityAction`) | `occupancy.ts`, `occupancy-math.ts`, `dates.ts` (`plannedMoment`), `bookings.ts` (`createBooking`, `transition`, `correctStatus`, `updateBooking`), `actions/bookings.ts` (`quoteAction`), `actions/settings.ts`, `ParkingSummary`, `occupancy/page`, `settings/capacity/page`, `CapacityForm`, `QuickBookingDrawer`, `EditBooking`, `dashboard/page`, `TodayStrip`, `today/page`, `boards/[kind]/page`, `seed.ts` (`CapacityConfig`); e2e `overstay:62,90`, `autoconfirm` | вопрос 3, трактовка 395 |
| Ф9б | Ф3 (`plannedMoment`, окна 24 ч, счётчики) | `today/page`, `TodayBoard`, `GuardScreen`, `BookingsTable`, `KanbanBoard`, `boards/[kind]/page`, `labels.ts`, `QuickBookingDrawer`, `audit/page`, `Clock` | вопрос 9 |
| Ф4 | шаг 0 — ни от чего; `BookingCalculator` после МФ-1 | новый `automations/sender.ts`, новый `api/telegram/*`, `dispatcher.ts` (канал), `scheduler.ts` (отправщик в тике), `schema` (`Client` chat_id, токен привязки), `settings.ts` (ключи бота), `BookingCalculator` (экран результата), `bookings/[id]/page:208`, `validation/booking.ts:4`, `dates.ts:75,83`, `seed.ts:109`, `entrypoint.sh:7`; e2e `site-lead` | **токен бота**; для notifyPhone — «да» заказчика |
| Ф14 | Ф4 (интерфейс отправщика) | `sender.ts` (адаптер), вебхук Wazzup, окно чатов (новые страницы), `schema` (диалоги), `tariffs.ts:33` | **аккаунт Wazzup**, вопрос 6 |
| МФ-2 | — | новые `settings/{templates,automations,users,tariffs,policy}`, `settings/page.tsx`, `settings.ts` (`links.*`), `actions/settings.ts`, `seed.ts` (sync → признак правки), `templates.ts`, `schema` (`MessageTemplate`: признак правки) | видео (только поле) |
| Ф5 | МФ-2 (тексты и ссылки из базы), Ф4 (проверка доставкой) | `templates.ts`, `seed.ts` (правила REJECTED/CHECKED_IN/CHECKED_OUT), `dispatcher.ts` (`rejectKind`, `contract`), `render.ts`, `schema` (`contractNumber`), `bookings.ts` (`transition` → «Заехал», `correctStatus`) | **текст Влада, ссылка Яши** |
| Ф6 | Ф5 (шаблоны), Ф3 (потолок в `updateBooking`) | `bookings.ts` (`updateBooking`, `extendStay`), `dispatcher.ts` (ключ с датой, отмена старых), `schema` (`BOOKING_CHANGED`), `templates.ts`, `seed.ts`, `leads.ts` + `BookingCalculator:236` (изменение с сайта), `EditBooking` | вопрос 4 |
| Ф7 | Ф2б (реестр и режимы сканов), Ф3 (`plannedMoment`), Ф6 (ключ с датой) | новые `scans/*`, `scheduler.ts`, `dispatcher.ts`, `schema` (`AFTER_PLANNED_CHECKIN`), `labels.ts:61`, `seed.ts:87,97`, `templates.ts` | — |
| Ф10 | Ф9а (честное время), Ф3 (`correctStatus`, `transition` — порядок в файле) | `bookings.ts` (`decideRecalc`, `correctStatus`, `waiveOverstay`, `addPayment`, `transition` — отметки), `refund.ts`, `PriceTools`, `PaymentPanel`, `bookings/[id]/page`, `actions/bookings.ts`, `schema` (`Payment.reason`, сторно); e2e `crm-pipeline`, `overstay` | решения по новым пунктам Ф10 |
| МФ-3а | — | `docker/backup.sh`, crontab, копия вне сервера, runbook | «да» на крон на VPS2 |
| Ф11 | Ф10 (та же модель `Payment`), МФ-3а (до выкатки) | `schema` (`CashShift`, `CashCollection`, `Payment.cashShiftId`, индекс, Restrict), новый `services/cash.ts`, страницы кассы, `bookings.ts` (`addPayment`), `settings.ts` (`cash.requireShift`), `notices.ts`, `login/actions.ts`, `Sidebar`, `GuardScreen:56`, `PaymentPanel` | вопрос 8 (дата ночной смены) |
| Ф12 | Ф11 (список смен), Ф10 (отметки отклонения, причина), Ф9а (честный `checkedInAt`) | новые `reports` (страница и сервис), `dates.ts` (диапазон по Москве), `dashboard/page`, `Sidebar` | вопрос 7 |
| Ф13 | ответы; для отметки из кассы — Ф11 | `schema` (должности, `Employee`, `WorkShift`), новые страницы `staff`, сервис, `Sidebar` | вопрос 8, «парковщик = охрана?», объём по договору |
| МФ-3б | всё остальное (последняя) | `seed.ts`, `docker-compose.yml`, `Dockerfile`, `next.config.ts`, `proxy.ts`, `login/actions.ts`, `session.ts`, `lead/route.ts`, `BookingsTable` (CSV), `robots.ts`, `tariffs.ts:30` | домен и DNS, хостинг в РФ, юрист |

### Дорожки (7)

| Дорожка | Фазы по порядку | Когда старт |
|---|---|---|
| **Д1** Уведомления и заявки | Ф2б → МФ-1 → Ф8 | сразу |
| **Д2** Занятость и экраны | Ф9а → Ф3 → Ф9б | Ф9а сразу; Ф3 после ответа на вопрос 3 |
| **Д3** Транспорт сообщений | Ф4 (шаг 0 → бот) → Ф14 | шаг 0 сразу, бот после токена |
| **Д4** Тексты и события | МФ-2 → Ф5 → Ф6 → Ф7 | сразу. Критический путь: Ф5 ждёт Ф4 и тексты заказчика. Нет ответа на вопрос 4 — Ф7 раньше Ф6 |
| **Д5** Деньги | Ф10 → Ф11 → Ф12 | после ответов по Ф10 |
| **Д6** Табель | Ф13 | после вопроса 8 и ответа заказчика |
| **Д7** Инфраструктура | МФ-3а → (пауза) → МФ-3б | МФ-3а сразу после «да» на крон; МФ-3б в конце |

**Шаг 0 оркестратора до старта дорожек:**
1. **Механически разрезать `src/server/services/bookings.ts`** (524 строки, его трогают 9 фаз) на `create` / `transition` / `correct` / `payments` / `edit`. Поведение не меняется, отдельный коммит, прогон юнит 91 и e2e 97.
2. **Миграции** только добавляющие, новое значение enum — отдельной миграцией. Оркестратор 1 при слиянии перенумеровывает папки миграций в порядке слияния и сверяет схему с цепочкой миграций через `prisma migrate diff`.
3. **e2e:** у каждой фазы свой файл `tests/e2e/<фаза>.mjs`. Общие (`overstay`, `crm-pipeline`, `site-lead`, `autoconfirm`, `scheduler`, `lib`) правятся только там, где меняется поведение. `package.json`, `Sidebar.tsx`, `settings/page.tsx` — только дописывать в конец.

**Общие файлы между дорожками и кто первый:**

| Пара | Общие файлы | Как сводить |
|---|---|---|
| Д1 × Д2 | `bookings.ts` (Ф2б и Ф9а — `transition`; МФ-1 и Ф3 — `createBooking`), `actions/settings.ts` и `CapacityForm` (МФ-1 — выключатель; Ф3 — вместимость и флаг заглушки), `settings.ts`, e2e `autoconfirm`, `overstay` | Ф9а и Ф2б в любом порядке (разные куски). **МФ-1 раньше Ф3.** Ф3 доделывает `saveCapacityAction` поверх предохранителя |
| Д1 × Д3 | `notices.ts` (Ф4: входящие в колокольчик), `leads.ts` (Ф4: токен привязки), `BookingCalculator` (Ф8 — форма, Ф4 — экран результата), `lead/route.ts`, `Client` в схеме | **Ф2б и МФ-1 раньше Ф4** по `notices` и `leads`. `BookingCalculator`: у Ф4 экран результата, у Ф8 форма, блоки не пересекаются |
| Д1 × Д4 | `leads.ts` и `BookingCalculator:236` (Ф6 — изменение с сайта), реестр сканов и `SchedulerCard` (Ф7) | МФ-1 раньше Ф6. Ф2б раньше Ф7 (Ф7 от него зависит) |
| Д1 × Д5 | `notices.ts` (Ф11: касса), `bookings.ts` (разные функции), отметки отклонения (Ф8 читает, Ф10 меняет) | Ф2б раньше Ф11. Отметки меняет только Ф10 |
| Д2 × Д3 | `bookings/[id]/page.tsx:208`, `dates.ts` (проверка времени против `plannedMoment`), `validation/booking.ts` | Строку :208 целиком делает Ф4. Строгое время — одним помощником в шаге 0 Ф4 (он раньше), Ф3 доделывает поверх |
| Д2 × Д4 | `bookings.ts` (`updateBooking`: Ф3 потолок, Ф6 сообщения и «Заехал»), `dates.ts` (`plannedMoment` → Ф7), `labels.ts` (Ф9б подписи, Ф7 переход), `EditBooking`, `seed.ts` (`CapacityConfig` против шаблонов) | **Ф3 раньше Ф6 и Ф7.** `labels.ts` и `seed.ts` — разные блоки, сливаются без ручной работы |
| Д2 × Д5 | `bookings.ts` (`transition`, `correctStatus`), `PaymentPanel`, `bookings/[id]/page`, `dashboard/page`, e2e `crm-pipeline`, `overstay` | **Ф9а → Ф3 → Ф10** по `bookings.ts`. `PaymentPanel:83` (время) делает Ф10. Дашборд: Ф3 раньше Ф12 |
| Д2 × Д7 | `BookingsTable` (фильтр Ф9б против CSV МФ-3б) | Ф9б раньше |
| Д3 × Д4 | `dispatcher.ts`, `seed.ts`, `templates.ts`, `scheduler.ts`, `settings.ts`, `schema` (`Client` против `Booking`/`AutomationTrigger`) | **Шаг 0 Ф4 — самым первым** (`seed.ts:109`, `entrypoint`). Ф4 выносит отправку в новый `sender.ts` и в `enqueue` меняет только выбор канала. Фильтры правил, ключи и шаблоны — Д4. Ф4 сливается до тестирования Ф5 |
| Д3 × Д7 | `seed.ts`, `entrypoint.sh`, `tariffs.ts` (MAX против номера WhatsApp) | МФ-3б последней |
| Д4 × Д7 | `seed.ts` | МФ-3б последней |
| Д5 × Д6 | `schema` (новые модели), `Sidebar`, привязка «касса → смена» | Ф11 раньше. Ф13 добавляет привязку и дозаполняет историю касс |
| Д5 × Д7 | МФ-3а до выкатки Ф11; `login/actions.ts` (Ф11 — напоминание, МФ-3б — перебор паролей) | МФ-3а раньше Ф11. Ф11 раньше МФ-3б |
| Д1 × Д7 | `lead/route.ts` (антибот против IP от прокси) | МФ-1 раньше |

Д6 с остальными пересекается только в `Sidebar` и в схеме, новыми моделями.

## 5. Вопросы пользователю, без которых фазы не начать, и что ждём от заказчика

**До запуска дорожек:**
1. Утвердить раскладку на 7 дорожек и шаг 0 оркестратора (разрезать `bookings.ts`).
2. Разделить Ф9 на Ф9а «убрать ручное время», первой в Д2, и Ф9б.
3. Экран результата сайта сделать в Ф4, а не в Ф9 или МФ-1.
4. До МФ-1 не давать заказчику логин владельца: у него в руках выключатель АП.

**По фазам:**
- **Ф3.**
  - Вопрос 3: держит ли место «Новая заявка».
  - Трактовка 395: «на парковке сейчас < 395» или «пик на датах заявки < 395» (сейчас второе).
  - Считать ли фуры в «Авто на парковке».
  - Сверх 405 вручную: жёсткий запрет или можно с явным подтверждением.
- **Ф4.**
  - Токен бота.
  - При неизвестном канале не отправлять (SKIPPED) или слать в WhatsApp.
  - Включить ли `admin.notifyPhone` в Ф4.
- **Ф6.**
  - Вопрос 4: повторная заявка с сайта на другие даты — менять бронь или создавать новую.
  - Уходит ли клиенту «бронь изменена» после «Продлить».
- **Ф8.** Вопрос 5: галочка согласия на рекламные рассылки на сайте.
- **Ф9б.** Вопрос 9: имя обязательно и в CRM.
- **Ф10.**
  - После полного возврата до заезда возвращать бронь в «Ожидает оплаты»?
  - Какое время ставить при забытом заезде или выезде через «Исправить статус», если ручной ввод запрещён: дата без времени, время исправления с пометкой или другое.
  - «Исправить статус» из «Выехал» — только владелец?
  - Сторно ошибочного платежа: кто делает и как это видно в кассе.
  - Что делать с остатком переплаты, если вернули меньше.
- **Ф11 и Ф13.** Вопрос 8: к какой дате относится ночная смена. Ставки и зарплата — в договоре или отдельно. 3 должности или 5.
- **Ф12.** Вопрос 7: только парковка или вместе с комнатами; фуры отдельно?
- **Ф14.** Вопрос 6: MAX через Wazzup. Аккаунт Wazzup.
- **МФ-3а.** Можно ли поставить крон бэкапа parking24 на VPS2 (решение «VPS2 не трогать»), или только на боевом сервере?
- **МФ-3б.** Можно ли сейчас опубликовать порт 8500 только на 127.0.0.1 (правка `docker-compose.yml`, stage продолжит работать через прокси)?
- **Общее.** Вопрос 11: удалить заявку №41 «Sergeyyy»? Вопрос 12: новый срок сдачи.

**Ждём от заказчика:**
- текст сообщения при заезде (Влад) — Ф5;
- ссылку на отзывы (Яша) — Ф5 и МФ-2;
- видео «как проехать» — МФ-2;
- подтверждение порога 395;
- письменно:
  - освобождение места через 48 ч;
  - уведомления администратору в мессенджер;
  - логины только у администраторов (6.1);
  - «парковщик» — это охрана?
- как клиент добирается от терминала и цену платного трансфера (меньше 4 суток);
- ссылку на MAX;
- домен и DNS;
- хостинг в РФ или письменное согласие (152-ФЗ);
- юриста: политика, оферта, согласие на рассылки;
- номер для АТС.

**От пользователя:** токен Telegram-бота (@BotFather) для Ф4 и аккаунт Wazzup для Ф14.