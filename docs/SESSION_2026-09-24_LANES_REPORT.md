# Переход в новый чат — 24.09.2026 (вечер): Ф9а на stage, дорожки параллельно

> **⚠️ УСТАРЕЛ (25.09.2026):** все дорожки слиты в main и выкачены на stage (`68d34cf`) — актуальное состояние, вопросы и стартовый промпт: `docs/SESSION_2026-09-25_STAGE_REPORT.md`. Ниже — история.

Файл для старта нового чата. Читать целиком вместе с блоком СТАРТ в `HANDOFF.md`.

> **⚠️ ГЛАВНОЕ ПРАВИЛО ПРОЕКТА (пользователь, 24.09): не усложнять там, где не нужно.** Только то, что просит ТЗ и решения пользователя (PLAN §1), самым простым рабочим способом. Из находок ревью чиним только блокеры и то, что ломает деньги/данные/безопасность; улучшения — одной строкой в документ фазы. Записано: `docs/TZ_2026-09-21_PLAN.md` §3 первым пунктом.

**Приоритет пользователя (24.09):** сначала довести **коммуникацию с клиентами** до показа заказчику и всё протестировать, потом **касса и табель**. Работать максимально параллельно (M4 Pro, 12 ядер, 24 ГБ — до 7 дорожек, dev-сервер дорожки поднимать только на время e2e).

## 1. Где что сейчас

| Где | Коммит | Что внутри |
|---|---|---|
| **stage** (`/opt/parking24/DEPLOYED_COMMIT`) | `7c88efe` | МФ-UI, Ф9а + **МФ-1** (автоподтверждение: надёжность), **Ф2б** (уведомления перестоя, колокольчик, режимы сканов), **Ф10** (возвраты, сторно, пересчёт), **МФ-2** (настройки CRM). Выкачено 24.09 вечером: бэкап `backups/pre-lanes-2026-09-24.dump`, 4 миграции, `seed ok`, e2e на stage **452/452** (11 наборов), `cleanup.sql` выполнен. На stage 14 броней: №105 «test» (+7 111 111-11-10, 24.09 11:43 МСК) и №41 — ручные проверки, не трогали |
| **main** = origin = VPS | `7c88efe` | = stage. Локально: юнит 206, e2e 519/519. Сверено rsync `--checksum`: различий и лишних файлов нет |
| рабочее дерево `~/Parking24` | чистое | слияние Ф4ш0 было начато и **откачено** (`git merge --abort`) — см. §3 |

## 2. Дорожки (`~/Parking24-lanes/<папка>`, у каждой своя ветка `lane/<папка>`, база и порт)

| Папка | База · порт | Фаза | Состояние | Последний коммит | Что осталось |
|---|---|---|---|---|---|
| `l4-send` | `parking24_l4` · 3104 | **Ф4 шаг 0 — отправщик** | ✅ готово: 3 раунда ревью, последнее ЧИСТО; e2e 315 | `e0494eb` | **слить в main** (§3) |
| `l4b-wazzup` | `parking24_l4b` · 3114 | **Ф14 — адаптер Wazzup, окно чатов** (на заглушке, реальный аккаунт НЕ трогали) | ✅ готово: 2 раунда, ЧИСТО; e2e 325 | `cd0cbc0` | слить **после** Ф4ш0 (§3); проверка на реальном аккаунте — только с разрешения пользователя (чек-лист в `PHASE_14_WAZZUP.md`, раздел «Код») |
| `l1-notices` | `parking24_l1` · 3101 | **Ф8 — сегмент «не смогли к нам попасть»** (урезано: список для ручного обзвона + CSV владельцу, без рассылок и галочки согласия) | ревью №1: сервер ЧИСТО, интерфейс 1 важно — **исправлено в WIP**; e2e с правками и подтверждающее ревью не сделаны | `a950b7d` (WIP) | e2e → подтверждающее ревью → слить |
| `l7-timesheet` | `parking24_l7` · 3107 | **Ф13 — табель** (документ переписан под «логины есть», критика внесена, код написан) | ревью №1: 1 важно (дата в журнале снятия отметки) — **исправлено в WIP**; подтверждающее не сделано | `c70934a` (WIP) | e2e → подтверждающее ревью → слить. ⚠️ Пользователь велел урезать до ТЗ 6.1–6.2 — при ревью выкидывать лишнее |
| `l2-occupancy` | `parking24_l2` · 3102 | **Ф3 — занятость по ТЗ, потолок 405 в CRM** | код закоммичен (38 файлов), **ревью не проводилось** | `2ae6bd1` | 2 раунда ревью → слить (конфликты с Ф10 в correct/payments/edit/shared — свести) |
| `l6-money` | `parking24_l6` · 3106 | **Ф11 — касса и смены** (база — main с Ф10) | код закоммичен (27 файлов), **ревью не проводилось** | `19ae0aa` | 2 раунда ревью → слить **после Ф13** (у Ф11 временный `src/lib/workshift.ts` только с `shiftDateOf` — при слиянии взять версию Ф13) |
| `l5-settings` | `parking24_l5` · 3105 | **Синхронизация цен сайт ↔ CRM** (пользователь: «да, делать сейчас») | прервано на середине, **не проверено** | `26e6def` (WIP) | доделать: сайт берёт цены из `Tariff` (одна чистая функция расчёта на калькулятор и `quote()`), `revalidatePath` при сохранении тарифа, e2e «цена в CRM → сайт → бронь», 1 раунд ревью |
| `l1b-autoconfirm` | `parking24_l1b` · 3111 | МФ-1 | ✅ слита в main | `3dffaba` | свободна |
| `integration`, `l3-ui`, `qa` | — | не использовались (стоят на старом `3a13cc5`) | — | — | — |

Каждая дорожка перед работой: `git -C <папка> merge main` (или rebase), `npx prisma generate && npx prisma migrate deploy && npx prisma db seed` в папке.

## 3. Порядок слияния и как разрешать конфликты

**Порядок:** Ф4ш0 → Ф14 → (Ф8, Ф13 по готовности) → Ф3 → Ф11 → цены. После каждого: `npx prisma migrate deploy` на `parking24_dev`, `tsc`, `npm test`, полный e2e (скрипт ниже), push.

**Ф4ш0 → main — 8 конфликтов, решения (начато и откачено, повторить):**
- `scheduler.ts`: импорты обоих (`overstayScan` и `senderStep`); `const SCANS = [overstayScan]` (коммент Ф2б про scan-registry) **и** `const STEPS: Step[] = [senderStep(() => db())]`.
- `tick-core.ts`: оставить `SCAN_MODES`, `isScanMode`, `withScanMode` (Ф2б); `mergeMode` отправщика — тонкая обёртка: `return parseModes(withScanMode(value, code, mode));` (одно правило).
- `actions/settings.ts`: импорты HEAD (`isScanMode`, `isScanCode`, `saveScanMode`, `autoConfirmGate`, `gateBlockers/gateChecks`) + `MESSAGING_KEYS` из `sender-core`.
- `settings/page.tsx`: объединить (реестр сканов Ф2б + пункты МФ-2 + карточка отправщика Ф4).
- `prisma/schema.prisma`: `NoticeKind` — объединение значений (OVERSTAY, UNPAID_CHECKOUT от Ф2б + MESSAGE_FAILED от Ф4); `Outbox` — поля Ф4.
- `prisma/seed.ts`: цикл правил МФ-2 (`planRuleSync`, `isActive` при обновлении не пишется) + `OFF_BY_DECISION` Ф4 **после** `planRuleSync` — он сильнее ручного включения (так договорились МФ-2 и Ф4).
- `package.json`: все e2e-наборы, `f4-sender.mjs` в конец. `tests/README.md`: оба раздела.
- **После слияния Ф4ш0:** `leads.ts:61` перевести перенос канала на `moveClientPendingChannel`; фикстуру МФ-1 `tests/e2e/autoconfirm-fixture.mjs` (ставит `messaging.senderEnabled = true`) перевести в режим «пробно», иначе локальный e2e может реально отправить; тексты МФ-2 про «отправка в настройках» сверить с карточкой отправщика.

**Ф14 → main (после Ф4ш0):** миграция Ф14 `NoticeKind` += `CLIENT_MESSAGE`, `MESSAGE_FAILED` уже с `IF NOT EXISTS` — идёт после миграции Ф4; `src/server/messaging/types.ts` — взять версию Ф4 + поле `uncertain?: boolean` в неуспешном `SendResult`; подключение — строка `wazzup` в `PROVIDERS` реестра Ф4 (`registry.ts`); `notifyOnce` (`wazzup/notify.ts`) → тело на `notify(kind, text, bookingId, { key })`; `DeliveryMark` из блока «Сообщения клиенту» перенести в `OutboxList` Ф4; **удалить тестовую ручку `/api/dev/wazzup-send`**.

**Ф3 → main:** `notify("CAPACITY_OVER", …)` в `guardCapacity` перевести на `{ tx, key }`; `isHHMM` — взять версию Ф4 (`src/lib/periods.ts`); в `correct.ts`/`payments.ts`/`edit.ts` — поверх Ф10 (порядок замков `lockOccupancy → lockBooking`).

**Скрипт полного e2e на main:** `/private/tmp/claude-501/-Users-styserg/296b0b84-1fc1-4853-a57e-1414d77db85d/scratchpad/e2e-main.sh` (поднимает dev на 3100, `npm run test:e2e`, cleanup, гасит сервер) — если scratchpad пропал, повторить: dev на 3100 → `npm run test:e2e` → `node tests/e2e/cleanup.mjs`. Гонять **bash**-скриптом: в zsh `$t` в цикле не делится на слова.

## 4. Выкатка на stage (следующая — пакетом после Ф4ш0 + Ф14)

По PLAN §3 / HANDOFF §3: бэкап `docker exec parking24-db pg_dump … > backups/<имя>.dump` → rsync **с исключениями и БЕЗ `--delete`** (24.09 `--delete-after` снёс `DEPLOYED_COMMIT`) → `docker compose up -d --build web` (nohup) → `seed ok`, тик → e2e на stage (bash-скрипт: `scratchpad/e2e-stage.sh`, пароли `E2E_*_PASSWORD`, секрет крона из `/opt/parking24/.env`) → `cleanup.sql` → `DEPLOYED_COMMIT` → сверка rsync `-n --checksum`.
Особенности пакета: 6+ миграций (Ф2б ×2, Ф10, МФ-2, Ф4 ×3, Ф14 ×2) — все добавляющие; **seed теперь падает громко** (`SEED_STRICT=0` — запасной выход); после выкатки на stage нажать «Убрать устаревшие» в карточке отправщика (старые PENDING 08–22.09), **отправщик оставить «Выключено»**, скан перестоя — «выкл → пробно → вкл»; перед показом заказчику — `cleanup.sql` и снять отметку «Включено вручную» у правила `before_checkin_24h` (след e2e МФ-2).

## 5. Решения, принятые в этой сессии (оркестратор и пользователь)

- **Пользователь:** главное правило «не усложнять»; приоритет — коммуникация, затем касса и табель; максимум параллельности; **синхронизировать цены сайта с CRM — да, сейчас**.
- Дата смены — одна для кассы, отчёта и табеля: `shiftDateOf(момент открытия)` в `src/lib/workshift.ts` (граница 02:00 МСК).
- e2e табеля на stage не трогает логины заказчика (занятый логин — самоотметка пропускается).
- Сторно пишется в текущую открытую смену; закрытая смена неизменяема.
- Ф10 правит `edit.ts`: смена типа ТС у закрытой брони — только владелец (денежная дыра).
- Ф8 урезана: без рассылок, без журнала согласий; CSV только владельцу (DECISIONS §4 п.3 говорит «администратор» — записано как толкование).
- Уведомления ревьюеров, которых запускает агент дорожки, приходят **оркестратору** — пересылать агенту `SendMessage`, иначе он ждёт вечно.

## 6. Вопросы пользователю (ждут ответа; до ответа — по рекомендации)

**Коммуникация (нужно для показа):**
1. Wazzup: можно ли подключить **реальный аккаунт заказчика** (5154-8781) к stage? Сначала только посмотреть каналы (`GET /v3/channels`), потом тестовые отправки **только на разрешённые номера**. ⚠️ Кнопка «Подключить вебхук» перезапишет вебхук всего аккаунта — есть ли у заказчика другая интеграция?
2. Какие каналы куплены (WhatsApp / Telegram / MAX), с какого номера пишем, какой тариф (при Max без лимита порог 500 диалогов даст ложные предупреждения)?
3. **Тестовые номера** для списка разрешённых (allowlist) — какие и в каких мессенджерах?
4. Тексты: сообщения при заезде (Влад) и ссылки на отзывы (Яша) нет — для показа ставим **наши черновики**, заказчик заменит в настройках?
5. Клиент выбрал Telegram, а канала нет — писать ли ему в WhatsApp? (рекоменд.: нет, сообщение ждёт)

**МФ-1 (автоподтверждение):** блокировать включение, пока нет отправщика и текста отказа (рек.: да); при перегрузке базы бронь «Новая» с пояснением (рек.: да); уведомлять ли админа, когда заявка осталась «Новой» при полной стоянке (рек.: нет); антибот — создавать заявку «возможен бот» вместо молчаливой потери (рек.: создавать).
**Ф10:** досрочный выезд при ступени тарифа — «не дороже плана» (рек.: оставить) или по цене суток брони.
**Ф2б:** фура «по запросу», уехавшая из перестоя — отдельный сигнал админу (рек.: нет).
**Ф13 (табель):** часы смен (рек.: день 08–20, ночь 20–08, сутки охраны с 08:00, до 02:00 — вчерашняя ночь); график на будущее (рек.: не вести, только факт — но ТЗ 6.2 говорит «контролировать график»); может ли администратор ставить/снимать смены себе (рек.: нет).

## 7. Подводные камни (узнали в сессии)

- `tests/e2e/cleanup.*` удаляет клиентов с номерами `+7999…` — это реальный диапазон мобильных номеров; на бою с живыми клиентами cleanup не запускать (сменить префикс тестовых номеров — задача МФ-3б).
- Лимит сессии API: около 16:45 Джакарта агенты падали с 429; после сброса — продолжать `SendMessage` агенту (контекст сохраняется).
- Параллельные агенты дорожек: dev-сервер только на время e2e, иначе память (24 ГБ) кончается.

## 8. Стартовый промпт нового чата

```
Продолжаем Parkovka24. Прочитай ~/Parking24/docs/SESSION_2026-09-24_LANES_REPORT.md целиком
и блок СТАРТ в ~/Parking24/HANDOFF.md. ГЛАВНОЕ ПРАВИЛО: не усложнять.
Проверь git status main и всех дорожек (~/Parking24-lanes), stage (/opt/parking24/DEPLOYED_COMMIT).
Затем: слей Ф4ш0 и Ф14 в main (конфликты — по §3), полный e2e, выкатка пакета на stage (§4).
Параллельно доведи дорожки: Ф8 и Ф13 (e2e + подтверждающее ревью), Ф3 и Ф11 (2 раунда ревью),
синхронизация цен (доделать). Дальше — Ф5 (сообщения по статусам), Ф6, Ф7, затем показ заказчику.
Не спрашивай подтверждений, параллель процессы, вопросы ко мне — из §6.
```
