# Часть 4. Сквозное: хвосты, ревью, аудит, фазы (проверка по коду HEAD 9f9fe7c, 22.09)

Проверено по коду и схеме, плюс read-only на stage (VPS2): crontab, `/opt/parking24/backups`, ключи `.env` (только имена), `ufw`, `sshd`, заголовки ответа, таблицы `Outbox`, `AutomationRule`, `Setting`, `CapacityConfig`. На stage образ собран из `c33717b`, после него менялись только документы.

Обозначения: **закрыто** · **частично** · **открыто**. «МФ» — предлагаемая новая мини-фаза (список внизу).

## Таблица

| Откуда | Что | Сейчас | Где в коде | Что сделать | Фаза |
|---|---|---|---|---|---|
| COVERAGE п.8 (8.1, 4.2), ложное «сделано», стык | Жёсткого потолка 405 в CRM нет: быстрая бронь, возврат из «Отклонена», правка дат идут без проверки пула и без блокировки | открыто | `bookings.ts:26-91` (createBooking без lockOccupancy/fits), `:124-195` (transition REJECTED→AWAITING_PAYMENT), `:371-423` (updateBooking) | проверять пик пула против `capacityTotal` под `lockOccupancy`, сверх лимита — только явным «подтверждаю» с записью в ленту | Ф3 (создание, из резерва), Ф6 (правка дат) |
| COVERAGE 8.1, ложное «сделано» 4.2, ревью `occupancy.ts:105`, `ParkingSummary:25`, `capacity/page:23` | «Всего мест» = 415 «с грузовыми»; «Свободно сейчас», «занято на странице ёмкости» и плашка порога считают пул вместе с фурами | открыто | `occupancy.ts:137-143`, `ParkingSummary.tsx:22-25,64`, `settings/capacity/page.tsx:23` | heldPool/heldTruck отдельно; «Всего 405» + строка «Грузовые 10»; порог сравнивать с пулом тем же `fits` | Ф3 |
| COVERAGE 8.2 | Ёмкости 60/30/10/5 в seed и в сетке; у фур в `CapacityConfig` 5, в настройках 10 (на stage так и есть) | открыто | `seed.ts:30-41`, `occupancy.ts:27-32,154-166`, `occupancy/page.tsx:87-99` | либо категории без знаменателя, либо правка `CapacityConfig` из формы; один лимит — пул + фуры | Ф3 |
| COVERAGE 8.2 | Сохранение 405/395 гасит баннер «ёмкость — плейсхолдер», хотя 60/30/10/5 остались (на stage `capacityIsPlaceholder=false`) | открыто | `actions/settings.ts:34` | не сбрасывать, пока категории не переведены | Ф3 |
| COVERAGE 8.2, ревью `actions/bookings.ts:147` | Подсказка «свободно N» в быстрой брони и правке — по категориям 60/30/10/5, а не по пулу | открыто | `actions/bookings.ts:156-160` → `occupancySummary` | `poolLoad(POOL/TRUCK, …, excludeBookingId)` | Ф3 |
| COVERAGE стык, ревью `occupancy.ts:127` | Три разных «занято сейчас»: полоса дня (`occupancyToday`) без «Ожидает оплаты» и по 60/30/10/5, строка пула, панель 415 | открыто | `occupancy.ts:154-166` (статусы оставлены «как были»), `TodayStrip` | один источник — `poolSpans`; `occupancyToday` перевести или убрать | Ф3 |
| COVERAGE стык, ревью `occupancy.ts:99`, ложное «сделано» 4.2 | Окна «24 ч»: заезды — две календарные даты, выезды — `dateTo ≤ завтра` для всех стоящих; табло «Отправление» — `dateTo ≤ сегодня`; в строке выезда печатается время заезда; `plannedMoment` в src не вызывается | открыто | `occupancy.ts:131-133`, `today/page.tsx:22-24`, `TodayBoard.tsx:101` | одно окно `now…now+24ч` через `plannedMoment`, просрочку отдельно, в «Отправлении» `timeTo` | Ф3 (панель), Ф9 (табло) |
| COVERAGE п.8, старые брони | Бронь парковки без `vehicleType` в `held` попадает, в пул — нет | открыто | `occupancy.ts:92-94` против `:135`; `validation/booking.ts` — `vehicleType` необязателен | обязательный тип ТС для парковки или явное правило | Ф3 |
| Ложное «сделано» 1.1 «менее 395» | Буквальное условие «на парковке сейчас < 395» не проверяется, только пик на датах заявки. Комментарий `fits` исправлен; после Ф2а перестой держит место на все дни | частично | `autoconfirm.ts:27-45`, `occupancy-math.ts:48-52` | зафиксировать трактовку (вопрос пользователю в архитектуре Ф3) | Ф3 |
| Ложное «сделано» тесты | Тесты — чистая математика (+ `effectiveSpan`, e2e overstay/autoconfirm с порогом 1). Тестов выборки (статусы, TRUCK вне пула, `excludeBookingId`, граница 394/395 в базе) нет | частично | `tests/unit/*`, импортов `services/occupancy` нет | сервисные тесты вместе с потолком в CRM | Ф3 |
| Ревью мелочь `occupancy-math.ts:41` | `fits` при `to < from` отвечает «место есть» | открыто | `occupancy-math.ts:50-52` | первой строкой `if (to < from) return false` — до переиспользования в CRM | Ф3 |
| Ревью `actions/settings.ts:22` | Вместимость сохраняется без сверки с занятостью: опечатка 45 вместо 405 молча закрывает приём с сайта | открыто | `actions/settings.ts:23-26` | отказ или подтверждение при `total < peak` | Ф3 |
| COVERAGE стык, ревью ×3 `NoticeBell:18` | «Прочитано» гасит все непрочитанные, включая непоказанные (take 20, пришедшие после открытия) | открыто | `NoticeBell.tsx:18`, `notices.ts:23-27` | `ids` обязателен, ветки «все» нет | Ф2б |
| Ревью `settings.ts:47` | `markNoticesReadAction` без проверки роли — охрана может погасить уведомления | открыто | `actions/settings.ts:48` `requireActor()` | `requireActor(STAFF)` | Ф2б |
| Ревью `AdminShell:9` | Бейдж колокольчика в layout застывает на всю сессию | открыто | `AdminShell.tsx:9`; `/api/admin/notices` нет | опрос раз в 60 с по одобренной архитектуре §4.7 | Ф2б |
| Ревью мелочь `settings.ts:49` | `revalidatePath("/admin","layout")` не попадает в layout группы `(app)`; тот же вызов в `actions/bookings.ts:24`, `clients.ts:145,158` | открыто | см. слева | `"/admin/(app)"` или убрать после опроса | Ф2б |
| Ревью `autoconfirm.ts:48` | Настройки читаются вторым соединением изнутри транзакции — при 3–5 одновременных заявках пул исчерпан, 500, заявка потеряна (и при выключенном АП) | открыто | `autoconfirm.ts:31` → `settings.ts:32-34` (глобальный prisma) | `parkingSettings(tx)`, явный `timeout` у транзакции | МФ-1 |
| Ревью `autoconfirm.ts:52` | Нет условия «цена > 0»: тип без тарифа подтверждается как бесплатный | открыто | `autoconfirm.ts:32-35` | передавать `amount`, при 0 — «Новая» | МФ-1 |
| Ревью `leads.ts:45` | Дубль ищется до транзакции и вне блокировки: две вкладки — два места и два подтверждения; при выключенном АП вторая падает P2002 → 500 | открыто | `leads.ts:45-55`, `clients.ts:18-25` | лок для любой заявки с сайта и поиск дубля внутри, либо уникальный индекс | МФ-1 |
| Ревью `leads.ts:100/101` | Уведомление об автоотклонении вне транзакции брони (может не создаться, клиенту 500); в тексте сырые ISO-даты | открыто | `leads.ts:100-102` | `notify(…, tx)` в транзакции `createBooking`, даты `fmtRange` | МФ-1 |
| Ревью `BookingCalculator:196` | При повторной заявке заголовок «уже у администратора» при подтверждённой брони | открыто | `BookingCalculator.tsx:196` | заголовок по `state`, `duplicate` — уточнение (HANDOFF относил к Ф9; проявляется только при АП) | МФ-1 |
| Ревью (опровергнутый блокер) `lead/route.ts:42` | Антибот по часам клиента: при спешащих часах — «ok» без заявки, клиент видит «Заявка принята» | открыто | `api/public/lead/route.ts:42` | серверная отметка времени формы, отрицательную разницу не считать ботом | МФ-1 |
| COVERAGE стык | Автоподтверждение можно включить раньше, чем появится сообщение об отказе: правила/шаблона на «Отклонена» нет, `saveCapacityAction` включить не мешает | открыто | `seed.ts:95-102`, `templates.ts`, `actions/settings.ts:12-44` | предохранитель: не включать АП без активного правила REJECTED и отправщика; сами тексты — Ф5 | МФ-1 (предохранитель), Ф5 |
| COVERAGE стык 1/3/5 | «Отклонена» видна везде — закрыто; но при возврате из резерва и `correctStatus` `rejectedAt/rejectKind` стираются — счёт отклонённых (ТЗ 5.2) задним числом уменьшится | частично | `bookings.ts:167-171,213` | считать отклонения по ленте/журналу или хранить историю | Ф12 (учесть в Ф8) |
| COVERAGE п.8 8.3 | Неоплаченная бронь держит место до конца дат: авто «Не приехал» 48 ч нет (переход есть, сканов нет) | открыто | `labels.ts:57`, `scheduler.ts:21` `SCANS = []` | скан по контракту Ф1 | Ф7 |
| STAGE2 «Безопасность выкатки» | Холодный старт сканов: `startFrom`, лимит на тик, не трогать оплаченные; обратного перехода «Не приехал» → «Ожидает оплаты» нет | открыто | `labels.ts:61` `NO_SHOW: []` | по разбору; systemTransition с `userId = null` | Ф7 |
| Ревью `dates.ts:64` | `plannedMoment` зависит от TZ процесса (в «дыре» летнего времени ±1 ч) | открыто | `dates.ts:72-79` | смещение через `timeZoneName: "longOffset"`, тест с `TZ=America/New_York` | Ф7 |
| Ревью мелочь `dates.ts:62` | «25:00» проходит проверку: уходит клиенту в тексте, `plannedMoment` → Invalid Date | открыто | `dates.ts:66,74`, `validation/booking.ts:4` (и в `leadSchema`) | одна строгая проверка (часы 00–23, минуты 00–59) во всех трёх местах | Ф4 (до первого текста), не позже Ф7 |
| COVERAGE п.8 8.3; мелочь раунда 2 №1; HANDOFF §4 | Двойное подтверждение в коде закрыто (`dedupGroup: "confirmation"`) | закрыто (код) | `dispatcher.ts:27-31`, `seed.ts:96,101` | — | — |
| Мелочь раунда 2 №1, HANDOFF §4/§8 | На stage 5 старых PENDING (08–22.09) со старыми ключами и текстами; у брони `cmtoa2kve…` в очереди **и** `on_awaiting_payment`, **и** `on_confirmed` — клиент получит два подтверждения | открыто (проверено в базе stage) | таблица `Outbox` на stage | отменить устаревшие или переписать ключи в `confirmation:<id>` | Ф4, шаг 0 |
| HANDOFF §8 | Правила вне ТЗ `before_checkout_2d` и `after_checkout_7d` (скидка 10 %) активны на stage; seed при создании ставит `isActive: true` | открыто (проверено) | `seed.ts:97-99,109` | выключить в базе, в seed создавать выключенными | Ф4 шаг 0 (не позже Ф7) |
| Мелочь раунда 2 №2 | Фура с ценой 0: любая оплата переводит в «Подтверждена», текст «стоимость подскажет администратор» без «оплачено» | открыто | `bookings.ts:291`, `render.ts:48-51` | при цене 0 не подтверждать оплатой или писать «Оплачено: N ₽» | Ф4 |
| Мелочь раунда 2 №3 | Оживление отменённой записи очереди не покрыто e2e | открыто | `dispatcher.ts:31,45` | e2e вместе с отправщиком | Ф4 |
| COVERAGE §2 (сквозное для отправки) | Канал по умолчанию WHATSAPP для заявок без мессенджера; `doNotDisturb` и согласие в очереди не проверяются | открыто | `dispatcher.ts:40` | неизвестный канал → SKIPPED; проверка в отправщике | Ф4 |
| PLAN §6 | Предохранители отправщика: не слать старше N часов, список разрешённых номеров на время теста | открыто (кода нет) | — | в отправщике | Ф4 |
| COVERAGE «не записано» | Привязка Telegram `chat_id`: поля нет, в плане §4 его тоже нет | открыто | `schema.prisma` Client — только `telegram` | `Client.tgChatId @unique` + токен заявки для `/start` | Ф4 |
| STAGE2 «Экран очереди» | В карточке PENDING подписан «запланировано», `SKIPPED_NO_PROVIDER` нигде не ставится | открыто | `bookings/[id]/page.tsx` (блок «Сообщения клиенту») | честная полоса «отправка не подключена», человеческие названия | Ф4 |
| Сайт (сквозное) | Сайт уже обещает «Ничего писать не нужно — мы напишем вам сами», «Подтверждение придёт в …» — отправки нет | открыто | `BookingCalculator.tsx:180-186,202,395` | закрывается отправщиком; до него — админ пишет вручную | Ф4 (блокирует бой) |
| Ложное «сделано» 2.3 | Сообщения при заезде нет: ни шаблона, ни правила на «Заехал», `contractNumber` в схеме нет | открыто | `templates.ts`, `seed.ts:95-102`, `schema.prisma` | шаблон, правило, номер договора | Ф5 |
| Ложное «сделано» 3.1 | На КПП нет «предстоящих выездов 24 ч»: «Выезд» = просроченные + все стоящие | открыто | `GuardScreen.tsx:24`, `today/page.tsx:23-24` | блок «Планируют выезд · 24 ч» | Ф9 |
| COVERAGE п.8, ложное «сделано» 3.3, аудит P1-2 | Время не по Москве: журнал аудита (сервер в UTC), дата платежа в поясе браузера и без времени, часы в шапке — время устройства; TZ контейнера не задан | открыто | `audit/page.tsx:21`, `PaymentPanel.tsx:77`, `Clock.tsx:13-14`, `docker-compose.yml` | всё через `fmtDateTime`; часы — время Москвы с сервера | Ф9 (TZ в compose — МФ-3) |
| Ревью `labels.ts:68` | «Подтвердить место» (→ Ожидает оплаты) и «Подтвердить» (→ Оплачена) рядом, главной выделена вторая | открыто | `labels.ts:67-70`, `TransitionButtons.tsx:10` | «Подтвердить место» — главная, CONFIRMED → «Отметить оплату» | Ф9 |
| PHASE_02 §12, HANDOFF §8 | Дата по умолчанию в быстрой брони — по UTC браузера (00–03 МСК = вчера) | открыто | `QuickBookingDrawer.tsx:28-32` | дата Москвы с сервера | Ф9 |
| PHASE_02 §12, HANDOFF §8 | Экран охраны показывает «Заехал» для «Новой заявки», а переход запрещён | открыто | `GuardScreen.tsx:81`, `labels.ts:55` | кнопку по `TRANSITIONS` | Ф9 |
| HANDOFF §8 | 3 старые ошибки eslint `set-state-in-effect` | открыто | `Clock.tsx`, `QuickBookingDrawer.tsx` ×2 | эти файлы и так правятся в Ф9 | Ф9 |
| Аудит P1-3 | Канбан на 1440 px: теперь 7 колонок по 272 px | открыто | `KanbanBoard.tsx:24,150` | узкие колонки, сворачивать «Выехал»/«Отменена»/«Отклонена» | Ф9 |
| Аудит P1-12 | Клавиатура охраны: из букв только «А» | открыто | `GuardScreen.tsx:42` | ряд букв госномера | Ф9 |
| Аудит P1-11 | «Заехал» раньше даты теперь держит место (Ф2а), но предупреждения нет; овербукинг — см. потолок | частично | `bookings.ts:136` | предупреждение при раннем заезде | Ф9 (овербукинг — Ф3) |
| PHASE_02 §12, HANDOFF §8 | Баннер «Стоянка по факту» считает `actualDays × цена суток`, а действие — `quote(actualDays)` (от 30 суток разные суммы) | открыто | `PriceTools.tsx:13`, `bookings.ts:329` | одно правило | Ф10 |
| COVERAGE п.8 8.5 | Возврат больше оплаченного молча обрезается до 0; причина необязательна | открыто | `bookings.ts:267-268` | ошибка, причина обязательна, наличные по умолчанию | Ф10 |
| COVERAGE п.8 8.4 | Закрытие смены кнопкой, напоминание при выходе — только enum `SHIFT_OPEN` | открыто | `schema.prisma:53-58` | по плану | Ф11 |
| COVERAGE «не записано» | Администратор должен видеть закрытые отчёты смен (в плане — только владелец) | открыто | — | экран истории смен: владелец — все, админ — свои | Ф11 |
| COVERAGE «не записано» | Правило даты ночной смены (20:00→08:00) не записано — табель и касса разойдутся на стыке месяцев | открыто (документ) | PLAN §1 | «дата смены = дата открытия» для CashShift и WorkShift | Ф11 / Ф13 (архитектура) |
| Ложное «сделано» права | «Разграничение доступа к отчётности и кассе» — разделов нет, нечего разграничивать | открыто | нет `/admin/reports`, `CashShift` | `requireUser(["OWNER"])` для отчёта и ставок | Ф11, Ф12 |
| COVERAGE п.8 итог, «не записано» | Финансовый отчёт без выгрузки и печати в плане | открыто | — | CSV + печать в Ф12 | Ф12 |
| Аудит P1-9 | Дашборд §11 (конверсия по источникам, средний чек, повторные, возвраты) | частично | `dashboard/page.tsx` | вместе с отчётом | Ф12 |
| COVERAGE п.8 6.1 | «Сотрудник отмечает свою смену» (ТЗ) против «логины только у администраторов» (решение пользователя) | открыто (не письменно у заказчика) | PLAN §1 | подтвердить у заказчика | Ф13 / ждём |
| COVERAGE п.8 | Ф13 шире ТЗ (ставки, зарплата, 5 должностей, настраиваемые столбцы) | открыто (объём) | PLAN этап 5 | решить: в договоре или допработа | Ф13 / пользователь |
| COVERAGE п.8 | Этапы 2а/2б вне ТЗ | закрыто (решение пользователя: Telegram первым, окно чатов — в Ф14, в рамках договора) | PLAN §5а | — | — |
| COVERAGE «не записано», HANDOFF §8, STAGE2 шапка | Редактора шаблонов нет (`/admin/settings/templates`, `/automations` — 404); тексты ведёт seed с `sync: true` — после появления редактора затрёт правки заказчика; `seedBody` не сделан | открыто | `settings/page.tsx:13-14`, `templates.ts`, `seed.ts:92,108` | страница «Сообщения»: вкл/выкл, текст, предпросмотр; признак ручной правки вместо `sync` | МФ-2 |
| COVERAGE «не записано», ревью мелочь `dispatcher.ts:28` | Ссылки в сообщениях: подстановка закрыта (`siteLinks(tx)`, маршрут по умолчанию `<сайт>/#directions`); формы ввода `links.route/review` нет, на stage ключи не заданы, у отзыва запасного значения нет | частично | `settings.ts:15-18,53-62` | поля ссылок в настройках | МФ-2 (не позже Ф5) |
| HANDOFF §8, аудит P0-4 | `/admin/settings/users`, `/tariffs`, `/policy` — 404 (смена паролей, тарифы, политика отмены) | открыто | `settings/page.tsx:9-11` | страницы или убрать ссылки до готовности | МФ-2 |
| COVERAGE п.8, аудит P0-1 | Бэкапов по крону нет: crontab на VPS2 пуст, в `backups/` только 4 ручных дампа перед выкатками; копии вне сервера и проверки восстановления нет | открыто (проверено на VPS2) | `docker/backup.sh` (крон только в комментарии) | крон, копия вне VPS, тренировка восстановления с записью в HANDOFF | МФ-3 (не позже Ф11) |
| Аудит P0-3, STAGE2 «мины» | Seed: `demo()` на пустой базе создаст 9 демо-броней (флага `SEED_DEMO` нет); пароли по умолчанию `owner12345` при пустых `SEED_*`; сбой seed глотается («seed failed (continuing)») при каждом старте | открыто | `seed.ts:7-14,114-116`, `entrypoint.sh:7` | `SEED_DEMO=1` для демо, без паролей по умолчанию, громкий сбой | МФ-3 |
| Аудит P0-5 | Бой: `robots` — `Disallow: /`, sitemap/canonical/OG нет, `NEXT_PUBLIC_SITE_URL` — sslip | открыто | `robots.ts` | после решения по домену | МФ-3 / ждём |
| Аудит P0-6 | Хостинг в РФ (152-ФЗ), юрист по политике/оферте/согласию на рассылки | открыто | — | решение заказчика | ждём |
| Аудит P0-7, HANDOFF §8 | На сайте WhatsApp пользователя `6281219010408`, боевой `79055250660` | открыто | `tariffs.ts:30` | вернуть в день запуска | МФ-3 |
| Аудит P1-1 | Блокировки перебора паролей нет (только 400 мс), неудачные входы не пишутся | открыто | `login/actions.ts:23-25` | 5 ошибок → 15 мин по логину+IP, запись в журнал | МФ-3 |
| Аудит P1-5 | Заголовков безопасности нет (stage: только `x-powered-by: Next.js`) | открыто (проверено curl) | `next.config.ts` | HSTS, X-Frame-Options, nosniff, Referrer-Policy, `poweredByHeader: false` | МФ-3 |
| Аудит P1-6 | `ufw` выключен, `PermitRootLogin yes`, вход по паролю работает | открыто (проверено) | VPS2 (общий сервер) | для боевого сервера; на VPS2 — только с разрешения пользователя | МФ-3 |
| Аудит P1-4 | CSV броней доступен администратору, в журнал `EXPORT` не пишется | открыто | `BookingsTable.tsx:34,70` | только владелец + запись в журнал | МФ-3 |
| Аудит P1-10 | 2FA для владельца/админа | открыто | — | по желанию заказчика | МФ-3 (опц.) |
| Аудит P1-13 | Мониторинг: uptime-пинг, алерт в Telegram, ротация логов | открыто | — | после Ф4 (бот есть) | МФ-3 |
| Аудит P0-2 | Планировщик — ✅ Ф1; канала отправки нет | частично | `instrumentation.ts`, `scheduler.ts` | — | Ф4 |
| Аудит P1-7 | Заявка с `/rooms` не создаётся в CRM (только wa.me) | открыто | `rooms/page.tsx:110` | вне ТЗ 21.09 (договор, поз. 1) | решение пользователя |
| Аудит P1-8 | Страницы `/tariffs /faq /contacts /about`, JSON-LD, sitemap | открыто | — | вне ТЗ 21.09 (договор, поз. 1) | решение пользователя |
| STAGE2 шапка | Тело разборов этапа 2 описывает старые решения (`client.hello`, `payLine`, `automation.*`, `defaults.ts`) и не сверено между собой | открыто (документ) | `docs/STAGE2_DESIGN_2026-09-22.md` | каждая архитектура Ф4–Ф7 сверяет свой раздел с кодом, а не наоборот | Ф4–Ф7 |
| HANDOFF §8 | Заявка №41 «Sergeyyy» на stage | не код | — | спросить пользователя | ждём от пользователя |
| Ревью, 6 блокеров этапа 1 | «Отклонена» на доске/в таблице, extras в рендер, тексты ред. 3 в seed, трансфер от 4 суток | закрыто | `b2e3ed7`, `baaf46c` | — | — |
| Ревью `bookings/[id]:47`, `clients/[id]:163` | «Отклонена» в полосе этапов и без «долга» у клиента | закрыто | `bookings/[id]/page.tsx:51`, `clients/[id]/page.tsx:170-171` | — | — |
| Ревью `render.ts:41` | Суммы «1 050» везде | закрыто | `render.ts:45-51` | — | — |
| Ревью мелочи `TransitionButtons:82`, `BookingsTable:12` | «Отклонить» красная и спрашивает причину, чип «Отклонена» | закрыто | `TransitionButtons.tsx:45,82`, `BookingsTable.tsx:13` | — | — |
| Ревью мелочи `occupancy-math:38`, `autoconfirm:18` | Комментарий `fits` верный; `ACTIVE` и выборка пула больше не дублируются | закрыто | `occupancy-math.ts:48`, `autoconfirm.ts:39` → `poolSpans` | — | — |
| Ревью `settings.ts:57` | `NEXT_PUBLIC_SITE_URL` вшит пустым | закрыто (опровергнуто, читается в рантайме) | `settings.ts:60` | — | — |
| COVERAGE стык, ревью `occupancy.ts:70/103`, п.8 старые брони | Перестой и ранний заезд выпадали из занятости, `onSite` спорил с `held` | закрыто Ф2а | `occupancy.ts:13-19,135`, `occupancy-math.ts:43-46` | уведомление — Ф2б | — |
| COVERAGE стык | Переход на ред. 3 ухудшил бы тексты (extras, `sync`) | закрыто | `dispatcher.ts:34-35`, `render.ts:79`, `seed.ts:92` | — | — |

**Итого строк: 86 — закрыто 10, частично 7, открыто 69.** Из открытых 3 — не код: два документа (разборы этапа 2, правило даты ночной смены) и заявка №41 на stage.

По фазам, открытые и частичные вместе (строка с двумя фазами считается в обеих): Ф2б — 4 · МФ-1 — 7 · Ф3 — 12 · Ф4 — 11 · Ф5 — 2 · Ф6 — 1 · Ф7 — 3 · Ф9 — 10 · Ф10 — 2 · Ф11 — 4 · Ф12 — 4 · Ф13 — 3 · МФ-2 — 3 · МФ-3 — 10 · Ф4–Ф7 (документ) — 1 · вне ТЗ, ждём, решение пользователя — 4.

## Предлагаемые мини-фазы

- **МФ-1 «Автоподтверждение: надёжность»** — после Ф2б, до включения АП заказчиком (можно перед Ф3): `parkingSettings(tx)` + таймаут транзакции; условие «цена > 0»; дубль заявки под блокировкой или по уникальному индексу; уведомление об отказе в транзакции брони с датами по Москве; заголовок калькулятора по `state`; антибот не по часам клиента; предохранитель «АП не включается без правила на „Отклонена“ и отправщика». Тесты: 5 одновременных заявок, две вкладки, цена 0.
- **Ф4, шаг 0** (внутри Ф4, до кода отправщика): отменить 5 старых PENDING на stage, выключить `before_checkout_2d`/`after_checkout_7d`, строгая проверка времени, громкий сбой seed.
- **МФ-2 «Настройки CRM (M4)»** — до передачи CRM заказчику, ссылки — не позже Ф5: страница «Сообщения» (вкл/выкл, текст, предпросмотр, ссылки маршрута и отзыва, признак ручной правки вместо `sync: true`), пользователи и смена пароля, тарифы, политика отмены; убрать ссылки на 404.
- **МФ-3 «Подготовка к бою»** — бэкапы (крон, копия вне VPS, тренировка восстановления) **не позже Ф11**, остальное — перед боевым запуском: `SEED_DEMO`, пароли без умолчаний, `TZ=Europe/Moscow` в compose, блокировка перебора паролей, заголовки безопасности, CSV только владельцу с записью в журнал, мониторинг, домен/robots/sitemap/`NEXT_PUBLIC_SITE_URL`, боевой WhatsApp, 152-ФЗ, ufw/ssh на боевом сервере, 2FA по желанию.
- Москва в показе времени (3.3) — не отдельная фаза, а часть **Ф9** (там же ручное время и «Планируют выезд»).

## Блокирует включение автоподтверждения заказчиком

1. **МФ-1 целиком.** Главное: чтение настроек вторым соединением изнутри транзакции (`autoconfirm.ts:31`) и дубль вне блокировки (`leads.ts:45`). При наплыве заявок это даёт ответ 500, потерянные заявки и два места на одного клиента.
2. **Правило и текст на «Отклонена» (Ф5) и отправщик (Ф4).** Без них автоотказ молчаливый: клиент видит его только на экране. При подтверждении сайт обещает «подробности придут», а не приходит ничего.
3. **Ф2б.** Бейдж колокольчика застывает, а «Прочитано» гасит отказы, которых никто не видел. Без уведомления о перестое забытый «Выехал» навсегда занимает место из 405, и АП начинает отказывать.
4. **Ф3 — желательно.** Плашка порога и «Свободно» сравнивают не ту величину. Брони из CRM пул не проверяют, поэтому резерв из 10 мест можно переполнить вручную. Опечатка во вместимости закрывает приём заявок.
5. **Ждём:** подтверждение порога 395 и правила 48 ч.

Не блокирует: Ф7. Неоплаченные брони держат место до конца дат — это запас, а не овербукинг.

## Блокирует отправку (Ф4)

1. **Данные stage:** 5 старых PENDING с текстами и ключами до 22.09. У брони `cmtoa2kve…` в очереди два подтверждения. Их надо отменить или переписать ключи.
2. Правила со скидкой 10 % и «продлением» активны. Выключить в базе, а seed должен создавать их выключенными.
3. Предохранители отправщика: не слать сообщения старше N часов, на время теста — список разрешённых номеров.
4. Канал: сейчас по умолчанию WHATSAPP. Нужны проверка `doNotDisturb` и согласия, поле `chat_id` для Telegram (в схеме его нет), кнопка «Открыть бота» и токен бота.
5. Карточка брони пишет «запланировано», хотя отправки нет, а `SKIPPED_NO_PROVIDER` нигде не ставится.
6. Seed падает молча (`entrypoint.sh:7`), и правила могут пропасть незаметно.
7. Тексты: у фуры с ценой 0 после оплаты не написано «оплачено». Время «25:00» проходит проверку и уходит клиенту.
8. Нет e2e на оживление отменённой записи очереди.

Не блокирует: страница шаблонов (тексты пока ведёт seed) и ссылки (есть запасной `#directions`).

## Ждём от заказчика (и пользователя)

- **Заказчик:** текст сообщения при заезде (Влад); ссылка на отзывы (Яша); видео «как проехать»; подтверждение порога 395; письменно: освобождение места через 48 ч, уведомления администратору (колокольчик, `admin.notifyPhone`), логины только у администраторов вместо «сотрудник отмечает смену сам» (6.1); «парковщик» — это охрана?; как клиент добирается от терминала после прилёта и цена платного трансфера (< 4 суток); ссылка на MAX; домен и DNS; хостинг в РФ или письменное согласие (152-ФЗ); юрист (политика, оферта, согласие на рассылки); номер для АТС (телефония — отдельно).
- **Пользователь:** токен Telegram-бота (@BotFather, Ф4); аккаунт Wazzup (Ф14); объём Ф13 (ставки и зарплата — в договоре или допработа); новый срок сдачи (не зафиксирован); имя обязательно и в CRM? (ответа нет); что делать с заявкой №41 на stage; разрешение на ufw/ssh, если бой остаётся на VPS2.
