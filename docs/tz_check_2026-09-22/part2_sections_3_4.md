# ТЗ 21.09, разделы 3 и 4 — сверка с кодом на HEAD `9f9fe7c` (22.09, после Ф1 и Ф2а)

Обозначения: ✅ работает · 🟡 частично · ⬜ нет. Фазы — PLAN §5а. Все строки проверены по коду, документам не верил.

**Итог: 38 пунктов — ✅ 13 · 🟡 12 · ⬜ 13.**

| Пункт ТЗ | Требование | Сейчас | Где в коде | Что осталось | Фаза |
|---|---|---|---|---|---|
| 3.1 | Отдельное табло «Планируют выезд» (ближайшие 24 ч) | ⬜ | Есть только блок «Отправление» на `/admin/today`: `TodayBoard.tsx:55`, выборка `today/page.tsx:28` («Заехал», `dateTo ≤ сегодня`) | Табло или блок на «Сегодня» и у охраны | Ф9 |
| 3.1 | Окно «ближайшие 24 часа» от текущего момента | ⬜ | `today/page.tsx:28` — календарный день плюс все перестои; `plannedMoment` (`dates.ts:72`) в `src/` не вызывается ни разу (только тесты) | Окно `now…now+24ч` по `plannedMoment(dateTo, timeTo)`, перестой отдельной группой | Ф9 |
| 3.1 | Номер бронирования и автомобиля | ✅ | `TodayBoard.tsx:103,106`; охрана `GuardScreen.tsx:87,90` | — | — |
| 3.1 | Дата и запланированное время выезда | ⬜ | `TodayBoard.tsx:101` `{r.timeFrom ?? r.timeTo}` — в «Отправлении» показывается время **заезда**; даты выезда в строке нет. У охраны дата без времени (`GuardScreen.tsx:94`) | Показывать `timeTo` и дату выезда | Ф9 |
| 3.1 | Информация о клиенте | ✅ | `TodayBoard.tsx:106,109` (имя, ссылка на карточку, телефон) | — | — |
| 3.1 | Текущий статус бронирования | 🟡 | `TodayBoard.tsx:116` — `hidden xl:block`: на экранах уже 1280px статуса нет; у охраны статуса нет вовсе | Статус виден всегда | Ф9 |
| 3.1 | Предстоящие выезды видны у охраны | 🟡 | `GuardScreen.tsx:25` — вкладка «Выезд» = выезды ≤ сегодня плюс **все** стоящие; окна 24 ч и времени нет | Тот же блок 24 ч на КПП | Ф9 |
| 3.1 (план) | Просроченные выезды помечены | ✅ | Ф2а: `TodayBoard.tsx:25,112`, `GuardScreen.tsx:83,94` — «перестой N сут. · долг» | — | — |
| 3.2 | Фильтр «Планируют выезд» в таблице заявок | ⬜ | `BookingsTable.tsx:17-29` — только статус, тип ТС, источник, поиск; в `KanbanItem` нет `timeTo` (`KanbanBoard.tsx:16-21`, `boards/[kind]/page.tsx:42-63`) | Признак «выезд в 24 ч» с сервера + чип фильтра | Ф9 |
| 3.2 (сопутств.) | «Отклонена» видна на доске и в таблице | ✅ | `boards/[kind]/page.tsx:29`, `KanbanBoard.tsx:24`, `BookingsTable.tsx:13`, карточка `bookings/[id]/page.tsx:51,62-69` | — | — |
| 3.3 | Исключить ручной ввод времени заезда и выезда | ⬜ | Форма `datetime-local`: `TransitionButtons.tsx:50-53,60-71` (карточка брони и «Сегодня» — `askTime` по умолчанию `true`, `TodayBoard.tsx:117`); `window.prompt` на доске `KanbanBoard.tsx:81-89`; параметр `at` — `actions/bookings.ts:45-48`, `services/bookings.ts:132-133` | Убрать форму, prompt и параметр `at` на сервере | Ф9 |
| 3.3 | Система фиксирует точное время действия | 🟡 | Сервер ставит `now`, если `at` нет (`bookings.ts:132`) — так только у охраны (`GuardScreen.tsx:35`) и в «Исправить статус» (`bookings.ts:215-216`). В карточке, на «Сегодня» и на доске пишется значение формы: момент **открытия** формы, до минуты (`TransitionButtons.tsx:52`, `KanbanBoard.tsx:82-86`) | Время — только серверное, в момент действия | Ф9 |
| 3.3 (риск) | Время нельзя подделать | ⬜ | `bookings.ts:133` проверяет только «не в будущем»: выезд можно поставить раньше заезда. С Ф2а начисление перестоя идёт от `moscowIso(at)` (`bookings.ts:142`) — администратор, указав выезд датой плана, **снимает ДОЛГ** в обход правила Ф2а «деньги закрытой брони — только владелец» | Закрывается удалением `at`; до Ф9 дыра на stage | Ф9 |
| 3.3 | Московское время: расчёт и показ в истории брони | ✅ | `dates.ts:12-18` (`moscowIso`, `todayIso`), `:32-34` (`actualParkingDays`), `:49-51` (`fmtDateTime`); карточка и лента — `bookings/[id]/page.tsx:50,93,219`; ввод трактуется как +03:00 (`TransitionButtons.tsx:20-22`) | — | — |
| 3.3 | Московское время в остальных показах | 🟡 | Журнал аудита `audit/page.tsx:21` — `toLocaleString` без `timeZone`, в `docker-compose.yml`/`Dockerfile` нет `TZ` → время UTC; платежи `PaymentPanel.tsx:77` — пояс браузера; часы `Clock.tsx:13-14` — время устройства | Через `fmtDateTime` / строки с сервера | **нет** |
| 3.3 | Дата и время заезда и выезда в истории | ✅ | `bookings.ts:136,141` (`checkedInAt`/`checkedOutAt`), `:174-184` (лента `meta.at` + аудит); этапы в карточке `page.tsx:58-59` | — | — |
| 3.3 | Фактическая продолжительность | ✅ | `bookings.ts:149-153` (`actualDays` через `actualParkingDays`), показ `page.tsx:69,118`; при перестое дата выезда и сутки переносятся (`bookings.ts:142-147`); юнит `dates.test.ts`, `overstay.test.ts` | — | — |
| 3.3 | Время ставится и при «Исправить статус» | ✅ | `bookings.ts:215-216` — серверное время, поля ввода нет | — | — |
| 4.1 | Просмотр занятости по каждой категории | ✅ | `occupancy/page.tsx:87-100` (21 день, сегодня подсвечен); с Ф2а перестой и ранний заезд считаются (`occupancy.ts:43-55`) | — | — |
| 4.1 | Ёмкость категорий согласована с решением (пул 405, фуры 10) | 🟡 | Знаменатели 60/30/10/5 из `CapacityConfig` (`seed.ts:33-36`, `occupancy.ts:27-32`); из интерфейса не правятся — `capacityConfig` в `src/` только читается; «Груз /5» против `capacityTruck = 10`; строки краснеют по 60/30 при свободном пуле (`page.tsx:91-92`) | Убрать капы категорий или сделать правку; фуры 10 | Ф3 |
| 4.1 | Предупреждение «ёмкость — плейсхолдер» честное | ⬜ | `actions/settings.ts:35` гасит флаг при сохранении общих настроек, баннер `occupancy/page.tsx:51` пропадает, а 60/30/10/5 остаются | Не гасить, пока категории не решены | Ф3 |
| 4.1 | Полоса «занято» на доске и «Сегодня» | 🟡 | `occupancyToday` (`occupancy.ts:154-166`): только «Подтверждена» + «Заехал», без «Ожидает оплаты»; ёмкость 60/30/10/5 → расходится с сеткой и панелью | Один источник — пул | Ф3 |
| 4.2 | Панель из пяти показателей | ✅ | `ParkingSummary.tsx:18-24`, подключена `occupancy/page.tsx:39-49` | Числа — см. ниже | — |
| 4.2 | «Авто на парковке» — фактически на территории | ✅ | `occupancy.ts:129` — все «Заехал» (с перестоем) | Фуры внутри числа; при разделении пула в Ф3 решить, как показывать | Ф3 (мелочь) |
| 4.2 | «Заезды за 24 ч» | 🟡 | `occupancy.ts:131` — даты «сегодня + завтра» (до 48 ч), время брони не учитывается, опоздавшие со вчера не видны (а «Сегодня» их держит, `today/page.tsx:27`) | Окно 24 ч по `plannedMoment(dateFrom, timeFrom)` | Ф3 |
| 4.2 | «Выезды за 24 ч» | 🟡 | `occupancy.ts:133` — `dateTo ≤ завтра` без нижней границы: все перестои + сегодня + завтра; не совпадает с «Отправлением» и полосой доски (`≤ сегодня`, `boards/[kind]/page.tsx:38`) | То же окно, что у табло 3.1 | Ф3 |
| 4.2 | «Свободно сейчас» = вместимость − машины на парковке | 🟡 | `occupancy.ts:135,137,143` — `(405 + 10) − брони на сегодня` («Ожидает оплаты», «Подтверждена», «Заехал», фуры вместе); перестой с Ф2а учтён | `405 − на парковке` по пулу; «занято бронями» — подписью | Ф3 |
| 4.2 | «Всего мест» — максимум 405 | ⬜ | `occupancy.ts:137` = 405 + 10 = **415**, подпись «с грузовыми» (`ParkingSummary.tsx:23`); строки фур на панели нет | 405 + фуры отдельно | Ф3 |
| 4.2 | Эталон ТЗ: 1 на парковке / 404 свободно / 405 | ⬜ | При одной машине и без броней панель даст **1 / 414 / 415** | Воспроизвести эталон e2e-тестом | Ф3 |
| 4.2 | Предупреждение о пороге автоподтверждения | 🟡 | `ParkingSummary.tsx:25,64` — `held` (пул + фуры, только сегодня) против порога пула; решение принимается по пику дат заявки (`autoconfirm.ts:39-41`) | Сравнивать ту же величину, что автоподтверждение | Ф3 |
| 4.2 | Строка «Всего в пуле» в сетке | ✅ | `occupancy/page.tsx:69-86` через `poolLoad` (`occupancy.ts:117-121`), подсветка порога (только при включённом автоподтверждении) | — | — |
| 4 | Не подтверждать больше, чем вместит — заявки с сайта | ✅ | `autoconfirm.ts:27-45` — пик пула по датам под `pg_advisory_xact_lock`, общий `poolSpans` с перестоем (`occupancy.ts:98-109`) | Надёжность: `parkingSettings()` вне транзакции (`autoconfirm.ts:31`) | мини-фаза «надёжность» |
| 4 | То же при брони из CRM | ⬜ | `createBooking` (`bookings.ts:26`) мест не проверяет; подсказка `quoteAction` (`actions/bookings.ts:157`) по категориям 60/30 → ложный «перегруз!» (`QuickBookingDrawer.tsx:226-229`) | Пул + потолок 405 под блокировкой | Ф3 |
| 4 | Правка дат в CRM не уводит за вместимость | ⬜ | `updateBooking` (`bookings.ts:371`) мест не проверяет; `EditBooking.tsx:70` «свободно N/30» | Проверка пула при смене дат | Ф3, Ф6 |
| 4 | Ручное подтверждение (новая заявка, резерв из «Отклонена») не выше 405 | ⬜ | `transition` (`bookings.ts:124-196`) занятость не смотрит; переходы `labels.ts:55,62` | Проверка 405 при переходе в «Ожидает оплаты» / «Подтверждена» | **нет** (Ф3 описан как «быстрая заявка, правка дат») |
| 4 | Страница ёмкости: «Сейчас занято мест» | 🟡 | `settings/capacity/page.tsx:23` → `dash.held` (пул + фуры, по броням) | Занятость пула | Ф3 |
| 4 | Вместимость сохраняется со сверкой с занятостью | ⬜ | `actions/settings.ts:23-26` — только «> 0» и «порог ≤ вместимости» | Отказ или подтверждение при `total < пик` | Ф3 |
| 4 (сквозное) | Одно определение «занято сейчас» | 🟡 | Пять разных чисел: полоса доски/«Сегодня» (`occupancyToday`, без «Ожидает оплаты», /60…), строка пула (/405), панель (`held`, /415), страница ёмкости (`held`), дашборд «Сейчас на стоянке» (`dashboard/page.tsx:20` — «Заехал» **без фильтра по виду**, считает и комнаты) | Одна функция занятости | Ф3 (дашборд — нет) |

## Не входит ни в одну фазу

1. **Московское время вне карточки брони:** журнал аудита (`audit/page.tsx:21`, контейнер без `TZ`), дата платежа (`PaymentPanel.tsx:77`), часы в шапке (`Clock.tsx:13-14`). ТЗ 3.3 — московское время; Ф9 про это молчит.
2. **Ручное подтверждение без потолка 405:** «Новая заявка» → «Ожидает оплаты»/«Подтверждена» и возврат из резерва «Отклонена» → «Ожидает оплаты» (`bookings.ts:124`, `labels.ts:55,62`). Ф3 в PLAN перечисляет только быструю заявку и правку дат. Предложение — явно дописать в Ф3.
3. **Дашборд «Сейчас на стоянке» считает комнаты** (`dashboard/page.tsx:20`, нет `kind: "PARKING"`) — ещё одно число «на стоянке», мелочь.

Не пробел, а порядок фаз: честные окна 24 ч в Ф3 и Ф9 требуют `plannedMoment`, а его починка (зависимость от TZ процесса, `dates.ts:76`) отнесена к Ф7, которая идёт позже. Чинить надо в Ф3.
Ф3 и Ф9 сломают действующие e2e: `crm-pipeline.mjs:73` и `overstay.mjs:52,141` ищут поле `datetime-local`, `overstay.mjs:60` читает подпись «занято бронями N». Переписывать тесты вместе с фазами.

## Где прежняя сверка ошибалась или устарела

| Строка сверки 22.09 | Как на самом деле |
|---|---|
| 3.2 (сопутств.) ❌ «Отклонена» не видна на доске, в таблице, в карточке | Закрыто в `b2e3ed7`: `boards/[kind]/page.tsx:29`, `KanbanBoard.tsx:24`, `BookingsTable.tsx:13`, `bookings/[id]/page.tsx:51` |
| Шапка: «3.3 … ✅; ручной ввод ещё не убран» | ✅ неверно: главное требование 3.3 («исключить ручной ввод») не выполнено, время по умолчанию — момент открытия формы. С Ф2а ручное время ещё и двигает деньги: выезд датой плана снимает ДОЛГ |
| Шапка: «4.2 — 3 числа не по ТЗ» | Не по ТЗ 4 из 5 (заезды, выезды, свободно, всего); верно только «Авто на парковке» |
| 4.2 «Выезды за 24 ч: `dateTo` от сегодня до завтра, два дня» | С Ф2а нижней границы нет (`occupancy.ts:133`): все перестои + 2 дня — окно стало шире |
| 4.2 «Свободно сейчас» и «Не учтено»: незакрытые заезды не уменьшают свободные; «Сквозное · старые брони ❌»; «Просроченные CHECKED_IN бьют по четырём разделам» | Для занятости закрыто в Ф2а (`occupiesWhere`/`effectiveSpan`, `occupancy.ts:13-19,135`, `occupancy-math.ts:43-46`, автоподтверждение тоже). Остались фуры в числе и расчёт от броней, а не от машин |
| 3.1 окно ⚠️ | По сути ⬜: окна 24 ч нет ни в одном месте, `plannedMoment` в `src/` не вызывается |
| 3.1 экран охраны ✅ | 🟡 (критик был прав): все стоящие, без окна 24 ч и без времени выезда; пометка теперь «ПЕРЕСТОЙ N СУТ. · ДОЛГ» |
| «Дыры на стыках»: три числа «занято сейчас» | Их пять (плюс страница ёмкости и дашборд с комнатами) |
| Ссылки на строки | Сдвинулись: `TodayBoard.tsx:97→101`, `:112→116`; `today/page.tsx:23→28`; `bookings.ts:106→132`, `:111-112→136,141`, `:136→173`, `:171-172→215-216`; `occupancy.ts:97/99/101/103/105→129/131/133/135/137`, `:127→159`; `actions/bookings.ts:147→157`; `autoconfirm.ts:44-60→27-45` (`poolPeak` слит в `poolSpans`); `dates.ts:60→72` |

## Открытые находки ревью этапа 1 по разделам 3–4

| Находка (`REVIEW_2026-09-22_STAGE1.md`) | Сейчас | Фаза |
|---|---|---|
| `occupancy.ts:105` «Свободно сейчас» складывает пул и фуры | открыта (`:137`) | Ф3 |
| `occupancy.ts:99` «Заезды/выезды за 24 ч» по двум датам | открыта (`:131`, `:133`; выезды теперь с перестоем) | Ф3 |
| `occupancy.ts:127` полоса дня без «Ожидает оплаты» | открыта (`:159`) | Ф3 |
| `ParkingSummary.tsx:25` порог сверяется не с той величиной | открыта | Ф3 |
| `settings/capacity/page.tsx:23` «занято» с фурами | открыта | Ф3 |
| `actions/bookings.ts:147` проверка мест в CRM по 60/30/10/5 | открыта (`:157`) | Ф3 |
| `actions/settings.ts:22` вместимость без сверки с занятостью | открыта (`:23`) | Ф3 |
| `lib/crm/labels.ts:68` «Подтвердить место» / «Подтвердить» — главная кнопка ведёт в «Оплачена» | открыта (`labels.ts:68-70`, `TransitionButtons.tsx:10`) | Ф9 |
| `server/lib/dates.ts:64` `plannedMoment` зависит от TZ процесса | открыта (`:76`) | Ф7 по HANDOFF, но нужна уже в Ф3/Ф9 |
| `BookingCalculator.tsx:196` заголовок при повторной заявке | открыта; это сайт (п. 1.2), не интерфейс администратора | Ф9 по HANDOFF |
| Попутно из Ф2: дата по умолчанию в быстрой брони по UTC (`QuickBookingDrawer.tsx:28-32`) | открыта | Ф9 |
| Попутно из Ф2: «Заехал» активна у охраны для «Новой заявки», переход запрещён (`GuardScreen.tsx:82`, `labels.ts:55`) | открыта | Ф9 |
| `occupancy.ts:70` и `:103` перестой выпадает из занятости | ✅ закрыта в Ф2а | — |
| `bookings/[id]/page.tsx:47` «Отклонена» не терминальная; мелочи `BookingsTable.tsx:12`, `TransitionButtons.tsx:82` | ✅ закрыты в `b2e3ed7` | — |

Колокольчик (`NoticeBell.tsx:18`, `AdminShell.tsx:9`, `actions/settings.ts:47`) — интерфейс администратора, но не раздел 3 ТЗ. Открыт, фаза Ф2б.
