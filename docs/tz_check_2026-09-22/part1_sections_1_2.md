# ТЗ 21.09, разделы 1 и 2: сверка с кодом на HEAD `9f9fe7c` (22.09)

Проверено по коду, документам не верил. ✅ работает · 🟡 частично · ⬜ нет. Фаза — из PLAN §5а; «ни в одну» — пункта нет ни в одной строке таблицы фаз.

**Итог: 49 пунктов: ✅ 12 · 🟡 14 · ⬜ 23.** Отправки сообщений нет вообще: нет отправщика, адаптера Telegram, сканов (`scheduler.ts:21` `SCANS = []`). Весь раздел 2 заканчивается на записи в Outbox.

## Раздел 1. Автоматизация бронирований

| Пункт ТЗ | Требование | Сейчас | Где в коде | Что осталось | Фаза |
|---|---|---|---|---|---|
| 1.1 | Проверка свободных мест на выбранные даты при заявке с сайта | ✅ | `autoconfirm.ts:27-45`, `leads.ts:95-98` | — | — |
| 1.1 | «Менее 395», вместимость 405, оба числа в настройках, выключатель | ✅ | `settings.ts:7-12,41` (порог ≤ вместимости), `occupancy-math.ts:50-52` (394 проходит, 395 нет), `CapacityForm.tsx:13-27`, `actions/settings.ts:19` (только владелец) | На stage выключено, включает заказчик. «Меньше 395» считается как пик на даты брони, стоящие машины в него входят; отдельной проверки «сейчас на парковке < 395» нет, по смыслу ТЗ это верно | — |
| 1.1 | Статус «Ожидает оплаты» | ✅ | `autoconfirm.ts:42`, `bookings.ts:36,44` | — | — |
| 1.1 | Учитывать машины, которые уже на парковке (в т.ч. перестой и ранний заезд) | ✅ | `occupancy-math.ts:43-46` `effectiveSpan`, `occupancy.ts:13-19,98-109`, `autoconfirm.ts:39` | — | Ф2а ✅ |
| 1.1 | Учитывать подтверждённые брони на эти даты | ✅ | `occupancy.ts:8` (Ожидает оплаты + Подтверждена + Заехал) | «Новая заявка» место не держит (фуры, брони из CRM, все заявки при выключенном автоподтверждении). Решения нет | ни в одну |
| 1.1, PLAN §3 | Решение и создание брони под блокировкой, без дыр | 🟡 | Блокировка: `autoconfirm.ts:20-22,37`. Дыры: настройки читаются вторым соединением, а не из транзакции (`autoconfirm.ts:31` → `settings.ts:34`); нет условия «цена > 0» (`autoconfirm.ts:32-35`); проверка дубля вне блокировки (`leads.ts:45-55`); уведомление об отклонении вне транзакции (`leads.ts:100-102`) | 4 правки до того, как заказчик включит автоподтверждение | **ни в одну**: мини-фаза «надёжность» есть только в HANDOFF §8, в §5а её нет |
| 1.1 | Сообщение клиенту в выбранный им мессенджер | 🟡 | Запись в Outbox с `channel = client.messenger`: `dispatcher.ts:11-23,40`. Отправщика нет | Отправщик и Telegram; WhatsApp | Ф4, Ф14 |
| 1.1 | В тексте дата, время и инструкция, как доехать (ссылка) | 🟡 | Ред. 3 в базе: `templates.ts:8-27` (`{{booking.arrival}}`, «Маршрут: {{links.route}}»), `seed.ts:92` (sync). Ссылка: `dispatcher.ts:34` → `settings.ts:53-62`, по умолчанию `<сайт>/#directions` | Текст утверждает Влад; видео. Поля для ссылки в CRM нет: ключ `links.route` есть только в `settings.ts:15-18`, менять его можно только в базе | «—» (видео); поле ссылки — ни в одну |
| 1.1 | Подтверждение уходит клиенту один раз, без повтора при оплате | ✅ | `dispatcher.ts:25-31` (`dedupGroup`), `seed.ts:96,101` | На stage 5 старых PENDING со старыми ключами `on_*:<id>` и старыми текстами (HANDOFF §4, §8) | Ф4 (фильтр по возрасту, PLAN §6) |
| 1.1, решение 22.09 | Перестой: место занято, ДОЛГ отдельно, начисление при выезде, «Продлить» | ✅ | `src/lib/overstay.ts`, `bookings.ts:140-155` (начисление), `:345-364` (`extendStay`) | — | Ф2а ✅ |
| 1.1, решение 22.09 | Перестой: уведомление администратору сразу, один раз | ⬜ | В `NoticeKind` нет `OVERSTAY` (`schema.prisma:53-58`), в `AdminNotice` нет `dedupKey` (`:487-500`), сканов нет (`scheduler.ts:21`) | Скан `overstay`, 2 миграции | Ф2б |
| PHASE_02 §4.6 | «Машина выехала, не оплачено N ₽» | ⬜ | В `transition` нет (`bookings.ts:124-195`) | — | Ф2б |
| 1.1, решение 22.09 | Инструкция — видео-таймлапс под телефон | ⬜ | — | Ждём видео | «—» |
| 1.2 | Без мест заявка не подтверждается (автоотклонение, `NO_SPACE`) | ✅ | `autoconfirm.ts:41-44`, `bookings.ts:64-65` | — | — |
| 1.2, решение 22.09 | Администратору: «заявка №N отклонена: нет мест» | 🟡 | Создаётся: `leads.ts:100-102`, `notices.ts:6-8`. Колокольчик: `NoticeBell.tsx:17` зовёт `markNoticesReadAction()` без `ids` → гасит все уведомления, проверки роли нет (`actions/settings.ts:48`, `notices.ts:23-27`); бейдж читается один раз в layout (`AdminShell.tsx:9`) | Опрос, `ids`, роль | Ф2б |
| 1.2 | Клиенту: «не подтверждена, на эти даты нет мест» | ⬜ | Шаблона и правила на `REJECTED` нет (`templates.ts:59-68`, `seed.ts:95-102`); `onStatusChanged` вызывается, но ничего не ставит (`bookings.ts:88,192`) | Шаблон, правило, выбор текста по `rejectKind` | Ф5 |
| 1.2 (сверх ТЗ) | Отказ виден сразу на экране сайта | ✅ | `api/public/lead/route.ts` (`state`), `BookingCalculator.tsx:196-201` | — | — |
| 1.2, решение 22.09 | Отказанные — в базе как «не смогли попасть» (для рассылок) | 🟡 | Данные сохраняются: бронь `REJECTED`/`NO_SPACE` с клиентом (`bookings.ts:37,64-65`). Сегмента и фильтра нет (`NO_SPACE` встречается только в `bookings/[id]/page.tsx:61`). Согласие на рассылки сайт не берёт, только на ПД (`leads.ts:104-106`) | Сегмент; согласие на рекламные рассылки | Ф8; согласие — ни в одну |
| 1.2, PLAN §1 8.1 | 10 мест резерва подтверждает администратор вручную | 🟡 | Отклонена → Ожидает оплаты: `labels.ts:62`, `bookings.ts:167-171` | Потолка 405 при ручном «Подтвердить место» (Новая/Отклонена → Ожидает оплаты) нет: `transition` места не проверяет | **ни в одну**: в Ф3 только «быстрая заявка, правка дат» |
| 1.3 / 2.6 | Сообщение «бронь изменена» с новыми датой и временем | ⬜ | `updateBooking` автоматизации не вызывает (`bookings.ts:371-423`); в `AutomationTrigger` нет `BOOKING_CHANGED` (`schema.prisma:116-121`); шаблона нет | Триггер, шаблон, ключ с датой | Ф6 |
| 1.3 | Изменение «даты ИЛИ времени» распознаётся | 🟡 | `spanChanged` время учитывает (`bookings.ts:389`), а запись в ленте — нет: только сумма, даты, номер (`:413-416`). При правке одного времени в ленте «Изменено: данные брони» | — | Ф6 |
| 1.3 | Изменения сразу учитываются в занятости | ✅ | Даты пишутся в бронь (`bookings.ts:397-400`), занятость считается на лету (`occupancy.ts:98-121`) | — | — |
| 1.3 | Новые даты помещаются (проверка мест, потолок) | ⬜ | В `updateBooking` нет ни `lockOccupancy`, ни `fits`; подсказка «свободно» в форме — по категориям 60/30/10/5: `actions/bookings.ts:158` → `occupancy.ts:27-39` | — | Ф3 (подсказка, потолок), Ф6 (проверка) |
| 1.3 | Старые сообщения при смене дат отменяются или пересчитываются | ⬜ | Текст рендерится при постановке (`dispatcher.ts:35`); `updateBooking` PENDING не трогает | — | Ф6 |
| 1.3 | Клиент меняет бронь сам на сайте («Изменить заявку») | ⬜ | `BookingCalculator.tsx:234-236` только сбрасывает форму. С новыми датами создаётся **вторая бронь**: второе место и второе подтверждение, первая бронь остаётся. Те же даты с другим временем за 10 минут считаются дублем, и время **молча не обновляется** (`leads.ts:45-63`) | Решить, как клиент меняет бронь | **ни в одну** |
| PLAN §1 1.3 | Проверить реальной доставкой в Telegram | ⬜ | — | — | Ф4 / Ф6 |

## Раздел 2. Автоматические уведомления

| Пункт ТЗ | Требование | Сейчас | Где в коде | Что осталось | Фаза |
|---|---|---|---|---|---|
| 2 | Писать в мессенджер, выбранный клиентом | 🟡 | Выбор на сайте → `leads.ts:92`, Outbox `dispatcher.ts:40` | Отправка | Ф4, Ф14 |
| 2 | Отправщик Outbox + Telegram (бот, `chat_id`, «Открыть бота», вебхук) | ⬜ | Кода нет (поиск `api.telegram` и `chat_id` пуст); поля `chat_id` в `Client` нет (`schema.prisma:238-271`) и в PLAN §4 оно не записано | — | Ф4 |
| 2 | WhatsApp | ⬜ | — | — | Ф14 |
| 2 | MAX | ⬜ | На сайте выбор выключен (`tariffs.ts:32,37`), отправки не планируется | — | ни в одну |
| 2 | Канал не выбран, «не беспокоить», смена канала | 🟡 | `client?.messenger ?? "WHATSAPP"` (`dispatcher.ts:40`): брони из CRM без канала молча уходят в WhatsApp; `doNotDisturb` не проверяется; смена мессенджера в карточке клиента не меняет канал у PENDING (меняется только при дубле с сайта, `leads.ts:61`); у заявки без телефона сообщение ставится без клиента | Правила пропуска | **ни в одну явно** (в Ф4 предохранители — только возраст и белый список) |
| 2 | Тексты ред. 3 в базе | 🟡 | 4 шаблона ред. 3 с `sync` (`templates.ts:60-67`). Нет: «Автомобиль принят», «Спасибо и отзыв», 2 отказа, «Бронь изменена», «Не приехал» | — | Ф5, Ф6, Ф7 |
| 2, MESSAGE_TEMPLATES | Заказчик правит тексты и ссылки в CRM | ⬜ | В `settings/` только `capacity` и `page.tsx`; seed перезаписывает тексты с `sync` (`seed.ts:92`) | Страница шаблонов и ссылок | **ни в одну** |
| 2 | Подстановка маршрута, ссылки на отзыв, времени заезда | ✅ | `dispatcher.ts:34`, `render.ts:6-61`, `tests/unit/render.test.ts` | Номер договора — см. 2.3 | — |
| 2.1 | Подтверждение сразу после подтверждения (автоматического и администратором), с датой, временем и маршрутом | 🟡 | `onStatusChanged` из `createBooking` (`bookings.ts:88`), `transition` (`:192`), оплаты (`:296`); правила `seed.ts:96,101` | Сейчас только в очереди | Ф4 |
| 2.2 | Текст напоминания со «за 3 часа до вылета» | ✅ | `templates.ts:33-45,61` | Утверждение Влада | — |
| 2.2 | Отправка за 24 ч; не ставить, если бронь создана позже | ⬜ | Правило `before_checkin_24h` (`seed.ts:97`) никто не читает: диспетчер берёт только `STATUS_CHANGED` (`dispatcher.ts:13`); сканов нет (`scheduler.ts:21`); `plannedMoment` (`dates.ts:72`) не используется и зависит от TZ процесса | Скан | Ф7 |
| 2.2 / 2.6 | Повторное напоминание или «изменено» после новой смены дат | ⬜ | Ключ `<группа или правило>:<бронь>` без даты (`dispatcher.ts:28`): отправленное или ждущее держит ключ навсегда, свободен только отменённый | Ключ с датой (PLAN §2 п. 8) | Ф6 / Ф7 |
| 2.3 | Сообщение при «Заехал» | ⬜ | Вызов есть (`bookings.ts:192`), время заезда передаётся (`dispatcher.ts:34`); шаблона и правила нет | Текст Влада | Ф5 |
| 2.3 | Номер договора 001, присваивается при «Заехал» | ⬜ | Поля нет (`schema.prisma:288-347`); `render.ts:35` берёт его из `extras`, а `dispatcher.ts:33-34` не передаёт. `correctStatus` сбрасывает `checkedInAt` (`bookings.ts:209`); что тогда с номером — не решено | Миграция, нумерация | Ф5 |
| 2.4 | Отклонение: два текста (нет мест / отклонено администратором) | ⬜ | Шаблонов нет. Диспетчер фильтрует только по `status` и `source` (`dispatcher.ts:17-19`), выбрать текст по `rejectKind` нечем. При подтверждении из резерва PENDING-отказ не отменяется (`bookings.ts:191` отменяет только при переходе в CANCELLED/NO_SHOW/REJECTED) | — | Ф5 |
| 2.5 | «Вы не воспользовались бронью» через 24 ч после планового заезда | ⬜ | Нет `AFTER_PLANNED_CHECKIN` (`schema.prisma:116-121`), шаблона и скана | — | Ф7 |
| Решение 22.09 | Автопереход в «Не приехал» через 48 ч | 🟡 | Переход «Ожидает оплаты» → «Не приехал» есть (`labels.ts:56`); `autoNoShowAfterHours` по умолчанию 24 (`schema.prisma:410`), а решено 48; скана нет | — | Ф7 |
| 2.7 | «Спасибо + отзыв» после выезда | ⬜ | Шаблона нет. Есть `thanks_discount` со скидкой 10 % (`templates.ts:64`, sync) и правило через 7 дней (`seed.ts:99`), решено — через 2 ч. `links.review` пустой (`settings.ts:17`), без ссылки строка с просьбой об отзыве выпадает целиком (`render.ts:79`) | Ссылка от Яши | Ф5 |
| 2, вне ТЗ | Выключить `before_checkout_2d` и `after_checkout_7d` (скидка не утверждена) | ⬜ | Создаются с `isActive: true` (`seed.ts:109`), `update` флаг не трогает (`:108`) — правила активны | — | **ни в одну** (HANDOFF §8: «до Ф4») |
| PLAN §4, этап 2 | Уведомление администратору в мессенджер (`admin.notifyPhone`) | ⬜ | Ключа нет (`settings.ts:7-18`) | — | **ни в одну** (PHASE_02 §2 отсылает к Ф4, в строке Ф4 этого нет) |
| 2 (дефект) | Время «запланировано» в карточке брони | ⬜ | `bookings/[id]/page.tsx:206`: `fmtDate` с `timeZone: "UTC"` (`dates.ts:40-43`) — показывает UTC, а не Москву | Заменить на `fmtDateTime` | ни в одну |
| 2, REVIEW р. 2 п. 2 | Фура с ценой 0: любая оплата сама переводит в «Подтверждена», в сообщении нет «оплачено» | 🟡 | `bookings.ts:291` (`paidAmount >= 0`), `render.ts:48-51` | — | ни в одну |
| Тесты | Сообщения покрыты тестами | 🟡 | `render.test.ts`; `site-lead.mjs:65-67` (очередь для заявки без автоподтверждения) | Нет тестов на `dedupGroup`, Outbox после автоподтверждения, отказ, `updateBooking` | в своих фазах |

## Не входит ни в одну фазу PLAN §5а

1. «Надёжность» автоподтверждения: настройки из транзакции, «цена > 0», дубль под блокировкой, уведомление внутри транзакции (`autoconfirm.ts:31-35`, `leads.ts:45,100`). В HANDOFF §8 это названо мини-фазой, но в таблице §5а её нет, хотя она нужна до включения автоподтверждения.
2. Потолок 405 при ручном подтверждении (Новая или Отклонена → Ожидает оплаты). Ф3 закрывает только быструю заявку и правку дат.
3. Изменение брони клиентом с сайта: новые даты создают вторую бронь, смена времени теряется (`leads.ts:45-63`).
4. Страница правки шаблонов и ссылок (`links.route`, `links.review`). Без неё видео и ссылку от Яши можно подключить только правкой базы.
5. Выключение правил `before_checkout_2d` и `after_checkout_7d` (скидка 10 %). В HANDOFF — «до Ф4», в §5а нет.
6. Уведомление администратору в мессенджер (`admin.notifyPhone`).
7. Правила отправки при неизвестном канале и «не беспокоить»; перенос канала PENDING при смене мессенджера клиента.
8. Согласие на рекламные рассылки для сегмента «не смогли попасть» (Ф8 делает сегмент, но согласия не собирает).
9. MAX: отправки нет в планах.
10. «Новая заявка» не держит место — нужно решение.
11. Мелочи: время очереди в карточке брони показывается в UTC; фура с ценой 0 подтверждается любой оплатой.

## Где прежняя сверка (22.09 ночь) теперь неверна

**Закрыто с тех пор (была ⬜/❌/⚠️):**
- 1.1 «Стоящие машины учтены полностью» ❌ → ✅ Ф2а (`effectiveSpan`).
- 1.1 и 2.1 «Инструкция, как добраться: ссылки нет, extras не передаются» ❌ → ✅: ред. 3 плюс `siteLinks` (`dispatcher.ts:34`), по умолчанию `#directions`.
- 1.1 и 2.1 «В базе старый текст `seed.ts:94`», 2 «4 из 6 без sync» → ред. 3 в `prisma/templates.ts` с `sync` (старым остался только `extension_offer`, он вне ТЗ).
- 1.1 и 2.1 «Подтверждение один раз — `dedupGroup` в коде нет» → есть (`dispatcher.ts:28`, `seed.ts:96,101`); отменённая запись ключ больше не держит.
- 2.1 «Нет времени заезда» ⚠️ → ✅ `{{booking.arrival}}`.
- 2.2 «Текст без „за 3 часа“» → фраза есть.
- 2.3 «`checkedInAt` рендерится пустым» ❌ → передаётся (`dispatcher.ts:34`).
- 2 «dispatcher рендерит без extras, `siteLinks` не вызывается» ❌ → вызывается; пусты только `contract` и `review` (ссылки нет).
- 2.2 и 2.5 «Нет `instrumentation.ts` и `/api/cron`, `RUN_SCHEDULER` никто не читает», вердикт критика «отправки нет: нет планировщика» → планировщик есть (Ф1); нет сканов и отправщика.
- «Мелкие ловушки» п. 1 (комментарий `occupancy-math.ts:38`) и п. 2 (`{{client.name}},`) → исправлены (`occupancy-math.ts:48`, `greeting.*`).

**Осталось как было, хотя можно подумать, что закрыто:**
- «Мелкие ловушки» п. 3: время в списке изменений `updateBooking` по-прежнему не пишется (`bookings.ts:413-416`).
- 2.6 «ключ навсегда блокирует повтор»: верно наполовину. Отменённая запись ключ больше не держит, отправленная и ждущая держат.
- `before_checkout_2d` и `after_checkout_7d` активны; `markNoticesRead` без `ids` гасит всё; канал по умолчанию WHATSAPP. Ничего из этого не исправлено.

**Сводка в шапке прежней сверки** (22.09 вечер) по разделам 1–2 верна. Но в строке 1.1 «✅ автоподтверждение» не упомянуты открытые дыры надёжности (п. 1 выше), а пункта про изменение брони с сайта (п. 3) нет вовсе.
