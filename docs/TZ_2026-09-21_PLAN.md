# ТЗ на доработку от 21.09.2026 — решения и план

Исходный текст заказчика: `docs/TZ_2026-09-21_SOURCE.md`. Этот файл — рабочий: что решено, где ТЗ расходится с уже сделанным и в каком порядке делать. Статусы: ⬜ не начато · 🟨 в работе · ✅ на stage.

Работа идёт **в рамках того же договора** (решение пользователя 22.09).

## 1. Решения (ответы пользователя 22.09)

| Вопрос | Решение |
|---|---|
| Ручная правка времени заезда и выезда | Убираем совсем. Время ставит сервер в момент действия, хранится в UTC, показывается по Москве. Поле ввода времени из карточки и из перетаскивания удаляется |
| Номер договора | Отдельная нумерация 001, 002, … Пока присваивает система; позже номер будет вести бухгалтерия. Присваиваем при переходе в «Заехал» (договор хранения подписывается при сдаче машины) |
| Касса | Одна на всех. Администраторы работают посменно, двух одновременно не бывает. Значит открытая смена в системе всегда одна |
| Логины водителей и парковщиков | ~~Нет. Логины только у администраторов, они же отмечают смены остальных~~ **Отменено 24.09: входят сами, см. «Решения 24.09 (вечер)» п. 6** |
| Длительность смены | Пока 12 часов (день и ночь). Уточнится позже, поэтому часы храним в записи смены |
| Мест нет | Заявка отклоняется автоматически, клиент получает сообщение из п. 1.2 ТЗ, администратор получает уведомление «заявка №N отклонена: на парковке нет мест» |
| Освобождение места, если клиент не приехал | Через 24 часа после сообщения «вы не приехали», то есть через 48 часов после планового заезда бронь сама переходит в «Не приехал» |
| Напоминания ночью | Можно, тихих часов нет |

### Решения пользователя 22.09 (после разбора ТЗ по пунктам)

| Пункт ТЗ | Решение |
|---|---|
| 1.1 Машина стоит дольше даты выезда (перестой) | Место считается занятым. Администратору сразу уходит уведомление: немедленно разобраться, выяснить причину и освободить место. Пока ситуация не решена, стоимость продолжает начисляться и подсвечивается отдельно как **ДОЛГ** |
| 1.1 Перестой — детали (Ф2, ответы 22.09) | Перестой — с 00:00 по Москве следующего дня после даты выезда. Сутки долга — по текущему тарифу типа машины (легковая от 30 суток — 250 ₽), у фуры «по запросу» долг не считается. При выезде долг сразу входит в сумму брони, снять — «Изменить цену» с причиной. Уведомление администратору — один раз на перестой. Охрана видит долг и выпускает, «Выехал» не блокируется. Подробно — `docs/phases/PHASE_02_OVERSTAY.md` §11 |
| 1.1 Инструкция, как доехать | Будет ссылка на видео, как проехать к парковке. Видео в формате таймлапса, должно хорошо смотреться с телефона. До его появления — ссылка на блок «Как добраться» на сайте |
| 1.2 Отказ, мест нет | Такие клиенты записываются в базу как «не смогли к нам попасть» — потом по ним будут рассылки |
| 1.3 Сообщение об изменении брони | Делаем, проверяем реальной доставкой в Telegram |
| 1.3 Изменения в занятости | Как сейчас: новые даты сразу учитываются |
| Возврат и сумма брони (22.09 вечер, уточнено после критики) | Больше оплаченного нельзя, причина обязательна. **У «Выехал»** возврат сначала гасит переплату, остаток уменьшает сумму брони («не оплачено» не меняется); **администратор после выезда возвращает только переплату** (досрочный выезд — «Пересчитать по факту», затем возврат), уменьшить сумму закрытой брони возвратом — владелец. **До выезда, у «Отменена» и «Не приехал»** сумма брони не меняется — после возврата долг возвращается (ошибочная оплата не делает бронь бесплатной). `docs/phases/PHASE_SP_URGENT_FIXES.md` |
| Ручное время при выезде в перестое (22.09 вечер) | **По дате нажатия**: в перестое выезд — только сегодняшним числом по Москве, для всех ролей; забытый выезд — «Исправить статус» с причиной (без начисления) |
| VPS2 (22.09 вечер) | **Не трогать**: боевой сервер будет другой, защита сервера — МФ-3б |
| Льготный час при выезде (22.09 вечер) | **Сутки перестоя — с 01:00 по Москве**: выезд до 01:00 не начисляет; позже начисляется сам, **администратор может снять начисление с причиной**. Перестой бывает редко — решения простые. `docs/phases/PHASE_SP_URGENT_FIXES.md` §3.4 |
| Порядок работы | **Каждый раздел — отдельная фаза, по одному, тщательно:** архитектура (продумать всё) → код → тесты → ревью → stage. Лучше больше фаз, но каждая проработана до конца. Не делать всё сразу |

### Ответы на пять вопросов раздела 8 ТЗ (пользователь, 22.09)

1. **Вместимость — 405 мест** (подтверждено). Порог автоподтверждения 395 пользователь отдельно не подтвердил, берём из текста ТЗ: система подтверждает сама, пока занято меньше 395, последние 10 мест — резерв, их подтверждает администратор вручную. Оба числа в настройках.
2. **Категории.** Общий пул 405 мест для легковых, кроссоверов и мотоциклов, без деления по категориям. **Фуры пока отдельно: 10 мест, в 405 не входят.** Фуры автоматически не подтверждаются (цена 0, «по запросу»), их ведёт администратор. На stage у фур сейчас стоит временное значение 5 — поправить через страницу настроек ёмкости в этапе 1.
3. **Предоплаты нет.** «Ожидает оплаты» держит место, клиент платит на въезде. Окончательно подтверждённой бронь становится после оплаты («Подтверждена»).
4. **Закрытие смены — только отдельной кнопкой «Закрыть смену» с повторным подтверждением.** Выход из аккаунта смену не закрывает. Если при выходе смена открыта, показываем напоминание со ссылкой на форму закрытия.
5. **Возврат при досрочном выезде** считается посуточно по тому же правилу, что и цена (дни заезда и выезда включительно). Администратор может поправить сумму, причина обязательна.

### Решения пользователя 23.09 (перед запуском дорожек)

Отвечены после повторной сверки с ТЗ (`docs/TZ_STATUS_2026-09-22_night.md` §5). Агенты дорожек берут решения отсюда и не придумывают своих.

1. **Работаем всеми семью дорожками сразу** (Д1–Д7), а не волнами.
2. **«Новая заявка» держит место ограниченное время.** Срок пользователь не назвал — **по умолчанию сутки**, значение выносится настройкой в CRM (`parking.newLeadHoldHours`, по умолчанию 24), чтобы заказчик менял сам. Совпадает с уже принятым правилом «место освобождается через 24 часа». Ждём подтверждения, если срок другой.
3. **Порог 395 — это пик занятости на датах заявки**, как сейчас в коде. Не «занято на парковке сейчас». Менять не надо.
4. **VPS2 не трогаем вообще.** Порт 8500 наружу и крон бэкапа уходят в МФ-3б на боевой сервер. **Следствие, принято осознанно: бэкапов по расписанию не будет до переезда**, остаются ручные дампы перед выкаткой; если база stage умрёт между выкатками, восстанавливать нечем. МФ-3а сводится к подготовке скрипта и runbook без применения. Д7 в работе первой волны фактически пустая.
5. **Забытый заезд или выезд** отмечается через «Исправить статус» **датой без времени**. Время не ставим, сутки считаются по датам — как везде с 21.09.
6. **Повторная заявка с сайта на другие даты** создаёт **новую бронь** и уведомление администратору «у клиента уже есть бронь на другие даты». Существующую бронь не трогаем.
7. **Финансовый отчёт — только парковка.** Комнаты в отчёт не входят.
8. **Табель — только отметки смен.** Место под ставки закладываем в схему, расчёт зарплаты делаем позже отдельной работой.

### Решения пользователя 24.09 (вечер): интерфейс и роли

1. **Сначала интерфейс, потом код Ф9а.** Интерфейс CRM сверяется со всеми функциями ТЗ и получает окончательную структуру до того, как фазы начнут дописывать в него свои экраны. Отдельная фаза «МФ-UI: меню, роли, сверка интерфейса с ТЗ».
2. **Меню CRM — только самое нужное** для управления бронированием и удобства администратора.
3. **Главная страница CRM — доска «Парковка»** (как сейчас).
4. **Ф9а, «Отменить» на доске у закрытой брони — только владелец** (вариант «а»). Исправление статуса из «Выехал», «Отменена», «Не приехал» — только владелец.
5. **Роли и доступы — по реальному штату:** владелец, три администратора, водитель, парковщик, плюс охрана (КПП) — **охрана остаётся отдельной ролью**, парковщик — отдельная должность.
6. **Водитель и парковщик входят в систему сами** (со своего телефона). **Это отменяет решение 22.09 «логинов у водителей и парковщиков нет».** Каждый видит только свой экран и отмечает свою смену (ТЗ 6.1 «сотрудник сам отмечает смену»). Денег и карточек броней не видят.
7. **Деньги у ворот принимает администратор, не охрана.** Охрана видит пометку «НЕ ОПЛАЧЕНО» (как сейчас), но кассу не открывает и оплату не проводит — это остаётся у `addPaymentAction` (роль STAFF). Вопрос из проверки `docs/ui_check_2026-09-24/C_money.md`.
8. **Водитель и парковщик — две отдельные роли** (`DRIVER`, `PARKER`), не одна общая «сотрудник».
9. **Парковщику кнопок «Заехал»/«Выехал» не даём** — он только смотрит список машин, отметку по-прежнему ставит охрана на воротах.
10. **«Дашборд» и будущие «Отчёты» — один пункт меню**, не два про одни и те же деньги.
11. **Срок входа (сессии) — 30 дней для всех ролей**, включая охрану, водителя и парковщика на личных телефонах. Не сокращаем.
12. **Остальные мелкие вопросы проверки D** (отмечает ли администратор смену сам; отмечает ли охрана свою смену сама; кнопка «позвонить клиенту» у водителя) **решаю по рекомендации проверяющего**, как того требует общее правило (DECISIONS §1): администратор и охрана — да, сами отмечают приход/уход в табеле (Ф13, когда дойдёт); у водителя кнопки звонка пока нет (см. `docs/phases/PHASE_MFUI_MENU_ROLES.md` §9). Это не блокирует МФ-UI — Ф13 ещё не переписана под новые роли, займётся этим отдельно.

### Ждём от заказчика

Текст сообщения при заезде (Влад), ссылка на отзывы (Яша), ссылка на инструкцию проезда.

## 2. Где ТЗ расходится с уже сделанным

1. **Ручное время (п. 3.3).** Поле «фактическое время» добавлено 09.09 по просьбе заказчика (очередь 2). Теперь оно убирается: `TransitionButtons.tsx` (форма `datetime-local`), `KanbanBoard.tsx` (`window.prompt` при перетаскивании), параметр `at` в `transitionAction`. Поля `checkedInAt`, `checkedOutAt`, `actualDays` и баннер пересчёта остаются как есть.
2. **Двойные сообщения.** Сейчас заявка с сайта сразу ставит в очередь «администратор проверяет место» (`on_new_lead`). При автоподтверждении клиент получил бы его и следом «бронь подтверждена». Решение: заявка создаётся сразу в нужном статусе (`createBooking` уже принимает `status`), поэтому срабатывает только одно правило. `on_new_lead` остаётся для заявок, которые не решились автоматически (фуры, автоподтверждение выключено).
3. **Правило `on_confirmed`.** При оплате на въезде бронь переходит в «Подтверждена», и клиент у ворот получил бы «бронь подтверждена, до встречи». Решение: у правил `on_awaiting_payment` и `on_confirmed` общий ключ дедупликации (`dedupGroup: "confirmation"` в `triggerParams`), подтверждение уходит один раз за бронь.
4. **Статуса «Отклонена» нет.** Есть только «Отменена». Отчёт считает отклонённые отдельно, поэтому добавляем значение `REJECTED` в `BookingStatus`. Переходы: «Новая заявка» → «Отклонена»; «Отклонена» → «Ожидает оплаты» (администратор подтвердил из резерва).
5. **Цепочка статусов против оплаты на месте.** Из «Ожидает оплаты» нельзя поставить «Не приехал» (только из «Подтверждена»), поэтому неоплаченная бронь без заезда держит место до конца своих дат. Добавляем переход «Ожидает оплаты» → «Не приехал» и системный автопереход через 48 часов. Порог уже есть в схеме: `CancellationPolicy.autoNoShowAfterHours`.
6. **Ёмкость.** В базе временные 60/30/10/5 = 105 мест, страницы настроек нет (M4). Нужны ключи в `Setting`: общая вместимость, порог автоподтверждения, выключатель автоподтверждения. Проверка мест в CRM (`quoteAction`, `QuickBookingDrawer`) сейчас идёт по категории и тоже переводится на общий пул, иначе CRM будет предупреждать «мест нет» при 30 кроссоверах.
7. **«Занято сейчас».** `occupancyToday` считает «Подтверждена» + «Заехал». По ТЗ «на парковке» — только «Заехал». Показываем два числа: по факту и по броням. `occupancyToday` не меняем молча, добавляем новую функцию сводки.
8. **Повторные сообщения.** Ключ дедупликации `правило:бронь` не даст отправить второе «бронь изменена». Для изменений и напоминаний в ключ входят дата и время (`правило:бронь:2026-10-01T12:00`). Напоминания создаёт периодический сканер, а не постановка заранее: смена дат учитывается сама.
9. **Возврат больше оплаченного** сейчас молча обрезается до нуля (`bookings.ts`, `Math.max(0, …)`). Должна быть ошибка.
10. **Должности и роли.** «Водитель» и «парковщик» — должности без входа в систему. `Role` не трогаем, заводим отдельный справочник сотрудников.
11. **Планировщик нужен и без провайдера сообщений.** Отправка в WhatsApp отложена до подключения провайдера, но минутный тик (`src/instrumentation.ts` + `/api/cron/automations`) нужен самому ТЗ: напоминания, сообщение «не приехали», автопереход в «Не приехал». Сообщения, как и сейчас, копятся в Outbox; отправщик подключается к тому же тику позже.
12. В макете кассы стоит «Rp 0» — в системе рубли.

## 3. Правила, чтобы ничего не сломать

- **Миграции только добавляющие:** новые таблицы, необязательные поля, новые значения enum. Старые брони и платежи не пересчитываются.
- **Новые значения enum — отдельной миграцией**, без использования в ней же: Postgres не даёт применить новое значение до фиксации транзакции.
- **Каждое новое автоматическое поведение имеет выключатель** в `Setting`. На stage выкатывается выключенным, включается после проверки. У автоподтверждения обязательна кнопка «вернуть ручной режим».
- **Одно правило в одном месте:** проверка мест — `src/server/services/occupancy.ts`; сообщения — `src/server/automations/dispatcher.ts`; деньги — `addPayment` в `src/server/services/bookings.ts`.
- **Автоподтверждение под блокировкой:** решение и создание брони в одной транзакции с `pg_advisory_xact_lock`, чтобы две заявки не заняли последнее место.
- **Системные действия** (автоподтверждение, автоотклонение, авто «Не приехал») выполняются без пользователя: `userId = null` в ленте и журнале, в интерфейсе подпись «автоматически».
- **Бэкап базы на stage перед каждой миграцией** (`docker/backup.sh`).
- **Код-ревью перед выкаткой — отдельный шаг, не самопроверка** (правило пользователя, действует и здесь). Независимое ревью свежим контекстом → список находок → правки → подтверждающее ревью, что находки закрыты и остальное не сломано. Минимум два раунда, без чистого подтверждающего на stage не выкатываем. Этап 1 уехал на stage в обход правила — ревью делается задним числом.
- **Сначала тесты.** Автотестов в репозитории нет; проверочные скрипты Playwright прошлых сессий лежали во временных папках и не сохранились.

## 4. Изменения в схеме (все добавляющие)

- `BookingStatus` + `REJECTED`. `AutomationTrigger` + `BOOKING_CHANGED`, `AFTER_PLANNED_CHECKIN`.
- `Booking`: `contractNumber Int? @unique` (показываем как 001), `rejectedAt DateTime?`, `rejectKind String?` (`NO_SPACE` или `OTHER`), `rejectReason String?`.
- `Payment`: `cashShiftId String?`.
- `CashShift`: администратор, время открытия и закрытия, остаток на начало, итоги смены снимком (наличные, карты, переводы, возвраты, инкассации, расчётный и фактический остаток, расхождение), примечание, статус `OPEN`/`CLOSED`. Одна открытая смена на систему — проверка в коде под блокировкой.
- `CashCollection`: смена, сумма, время, кто забрал (текст), кто передал (пользователь), кто записал, примечание.
- `Employee`: имя, должность (`ADMIN`, `DRIVER`, `PARKER`), необязательная связь с `User`, активен или нет.
- `WorkShift`: сотрудник, дата, слот (`DAY`, `NIGHT`), часы (по умолчанию 12), кто отметил. Уникальность: сотрудник + дата + слот.
- `AdminNotice`: вид, бронь, текст, время, кто и когда просмотрел. Лента уведомлений администратору в CRM (колокольчик в шапке). Первое применение — «заявка отклонена: нет мест»; дальше — расхождение в кассе, незакрытая смена.
- `Setting`, новые ключи: `parking.capacityTotal` (405), `parking.autoConfirmLimit` (395), `parking.autoConfirm`, `cash.requireShift`, `links.route`, `links.review`, `admin.notifyPhone`.

## 5а. Фазы (порядок работы с 22.09)

> **Обновлено 22.09 вечером по итогам полной сверки с ТЗ** — `docs/TZ_STATUS_2026-09-22.md` §3: добавлены «срочные правки» (дыры в деньгах на stage), мини-фазы МФ-1 «Автоподтверждение: надёжность» (после Ф2б, до включения АП заказчиком), МФ-2 «Настройки CRM» (до Ф5), МФ-3а «Бэкапы» (до Ф11) и МФ-3б «Подготовка к бою» (перед запуском); в Ф3, Ф6, Ф8, Ф9, Ф10–Ф13 дописано найденное сверкой. Порядок и содержимое — там; таблица ниже — исходная.

Этапы ниже остаются картой содержания, а работа идёт мелкими фазами. Каждая фаза проходит одни и те же шаги и только после этого начинается следующая:

1. **Архитектура** — документ `docs/phases/PHASE_NN_*.md`: цель, что входит и что нет, решения, данные, файлы, риски, критерии приёмки, тесты. Бизнес-вопросы уходят пользователю до кода.
2. **Критика архитектуры** — независимый разбор документа до кода.
3. **Код.**
4. **Тесты** — юнит и e2e; сообщения клиенту проверяются реальной доставкой в Telegram.
5. **Ревью** — независимое → правки → подтверждающее, минимум два раунда.
6. **Stage** — бэкап, выкатка, e2e на stage, запись в HANDOFF.

| Фаза | Что | Разделы ТЗ | Состояние |
|---|---|---|---|
| Ф1 | Планировщик: минутный тик, запасной вход, «пульс» в CRM. Ничего не отправляет | фундамент для 1.1, 2 | ✅ на stage 22.09 (`2453a92`), `docs/phases/PHASE_01_SCHEDULER.md` |
| Ф2а | Перестой: занятость (перестой и ранний заезд держат место), ДОЛГ по тарифу, начисление при выезде, «Продлить», деньги закрытой брони — только владелец | 1.1, 4 | ✅ на stage 22.09 (`c33717b`), `docs/phases/PHASE_02_OVERSTAY.md` |
| Ф2б | Перестой: уведомление администратору (скан `overstay`), «выехал, не оплачено», колокольчик, режимы сканов в «Планировщике» | 1.1 | ⬜ следующая, архитектура одобрена (§4.6–4.8) |
| Ф3 | Панель занятости по ТЗ (405, «свободно» от машин на парковке, честные 24 ч, фуры отдельно, категории) и потолок мест в CRM (быстрая заявка, правка дат) | 4.1, 4.2, 1.3 | ⬜ |
| Ф4 | Telegram: бот, привязка клиента кнопкой «Открыть бота», отправщик с предохранителями. Первое реальное сообщение — подтверждение брони | 2 | ⬜ |
| Ф5 | Сообщения по статусам: отказ (нет мест / отклонено), заезд с номером договора, выезд и отзыв | 1.2, 2 | ⬜ |
| Ф6 | Изменение брони: сообщение, отмена старых сообщений, проверка мест при смене дат | 1.3, 2 | ⬜ |
| Ф7 | Сообщения по времени: напоминание за 24 ч, «не приехали» через 24 ч, автопереход в «Не приехал» через 48 ч | 2 | ⬜ |
| Ф8 | «Не смогли попасть»: отклонённые клиенты отдельным сегментом базы под будущие рассылки | 1.2 | ⬜ |
| Ф9 | Интерфейс: табло и фильтр «Планируют выезд», убрать ручной ввод времени | 3 | ⬜ |
| Ф10 | Возврат при досрочном выезде: расчёт системой, «Оформить возврат», причина, наличные, не больше оплаченного | 5.3, 8.5 | ⬜ |
| Ф11 | Касса: смены, инкассация, закрытие смены | 7 | ⬜ |
| Ф12 | Финансовый отчёт | 5.1, 5.2 | ⬜ |
| Ф13 | Табель смен сотрудников | 6 | ⬜ |
| Ф14 | WhatsApp через Wazzup и окно чатов | 2, 2б | ⬜ |
| — | Видео «как проехать» — подключить, когда пришлёт заказчик (ссылка в настройках, при необходимости страница под телефон) | 1.1 | ждём видео |
| — | Общий прогон всех автотестов | — | в конце |

## 5. Этапы

### Этап 0. Страховка ✅ (22.09)
- `tests/unit/` — 18 юнит-тестов на чистые расчёты (сутки парковки и комнат, даты и московское время, телефоны и госномера, шаблоны сообщений). Запуск `npm test`, база и сервер не нужны.
- `tests/e2e/` — два сценария Playwright: `site-lead.mjs` (заявка с калькулятора → экран «Заявка принята» → карточка в CRM с каналом в очереди) и `crm-pipeline.mjs` (быстрая заявка → оплата → заезд → выезд → возврат). Запуск `npm run test:e2e`, параметр `--base` для stage. Прогнаны локально и на stage, оба зелёные.
- Очистка тестовых данных: `tests/e2e/cleanup.mjs` локально, `tests/e2e/cleanup.sql` на stage (признаки: госномер `Т000…`, телефон `+7999…`).
- Как запускать и какие есть подводные камни — `tests/README.md`.
- Что нашли тесты: `/api/public/lead` молча отвечает «ок» без создания заявки, если форму отправили быстрее 1,5 с (антибот); быстрая заявка не открывает карточку, а показывает тост со ссылкой; на stage поля калькулятора отвечают не сразу после загрузки страницы.
- Осталось в этапе 0: тесты на проверку мест и формулу кассы пишутся вместе с этими модулями (этапы 1 и 4); бэкап базы на stage не проверялся.

### Этап 1. Занятость и автоподтверждение ✅ (22.09, на stage) — п. 4, 1.1, 1.2
- Настройки ёмкости: минимальная страница `/admin/settings/capacity` (общая вместимость, порог, выключатель, категории).
- Сводка из пяти показателей на `/admin/occupancy`: на парковке (только «Заехал»), заезды за 24 ч («Ожидает оплаты» + «Подтверждена» с плановым заездом в окне), выезды за 24 ч, свободно сейчас (вместимость минус на парковке), всего мест. Строка «Всего» в сетке на 3 недели с подсветкой порога.
- Статус «Отклонена»: enum, подписи, колонка на доске, переходы.
- Автоподтверждение в `createSiteLead`: парковка, не фура, цена больше нуля, телефон распознан, выключатель включён. Пик занятости по дням брони меньше порога — заявка создаётся в «Ожидает оплаты»; иначе в «Отклонена» с `rejectKind = NO_SPACE` и `AdminNotice`. В остальных случаях «Новая заявка», как сейчас.
- Сайт: ответ API получает поле состояния; финальный экран калькулятора показывает «Бронь подтверждена», «На эти даты мест нет» или прежний «Заявка принята».
- Колокольчик уведомлений в шапке CRM.

### Этап 2. Уведомления ⬜ (2 дня до очереди) — п. 2, 1.3
- Тик планировщика: `src/instrumentation.ts` + `/api/cron/automations` (`CRON_SECRET`, заголовок `X-Cron-Secret`).
- Сканер по времени: напоминание за 24 ч до планового заезда (не ставим, если бронь создана позже этого момента); «вы не приехали» через 24 ч после планового заезда; автопереход в «Не приехал» через 48 ч. Плановое время = дата + время брони по Москве, без времени — 12:00.
- Правила и шаблоны: основная цепочка из четырёх сообщений (подтверждение, напоминание, заезд с номером брони и договора, спасибо с отзывом) — черновики, новые переменные и правка `renderTemplate` в `docs/MESSAGE_TEMPLATES_2026-09-22.md`, финальную редакцию утверждает заказчик, отклонение (два текста: «нет мест» и общее), «не приехали», изменение брони, выезд с просьбой об отзыве. Правила вне ТЗ (`before_checkout_2d`, `after_checkout_7d` со скидкой 10 %) выключаются: скидку заказчик не утверждал.
- `updateBooking`: при смене дат или времени — сообщение об изменении, отмена неотправленных сообщений по старым датам, повторная проверка мест.
- Присвоение номера договора при «Заехал», переменная `{{booking.contract}}` в шаблонах.
- Уведомление администратору в мессенджер (`admin.notifyPhone`) — через тот же Outbox, когда заработает отправка; до этого только колокольчик.

### Этап 2а. Отправка ⬜ (отправщик + адаптер Wazzup)

**Отменено 23.09: отдельного Telegram-бота не делаем.** Решение 22.09 «первым каналом Telegram» пересмотрено пользователем: «зачем нам телеграм бот, мы же используем Wazzup». Разбор подтвердил — бот не нужен:
- Wazzup сам возит Telegram, это один из трёх типов каналов. Свой бот — вторая интеграция для той же работы.
- **Бот не может написать первым нигде**, в том числе через Wazzup (транспорт `telegram` — это бот). Первым по номеру пишет только `tgapi`, личный Telegram по QR, — отдельный платный канал. Наш флоу «первым пишет Питстоп», значит бот к задаче не подходит в принципе, и ветка с кнопкой «Открыть бота» на сайте не нужна.
- Довод «отладить отправщик бесплатно, не сжигая пробный период» закрывает шаг 0: отправщик не знает про каналы и имеет режим «пробно» — пишет в журнал вместо отправки.

Дорожка Д3 теперь: **шаг 0 (отправщик с предохранителями) → адаптер Wazzup, WhatsApp**. Telegram при необходимости — ещё один канал Wazzup: значение транспорта в настройке, не новый код. Токен от @BotFather больше не нужен, блокер снят.

Следствия для заказчика:
- **Платят за канал.** WhatsApp на Pro — 4 000 ₽/мес; Telegram и MAX — ещё по подписке каждый. Три мессенджера на сайте = три счёта.
- **Сайт обещает больше, чем сможет.** В калькуляторе выбор из трёх, но писать первыми мы сможем только в купленные каналы. Предложено: оставить в выборе WhatsApp, остальные показывать как «напишите нам сами», иначе клиент выбирает Telegram и ждёт сообщения, которого не будет.

Ниже — прежний разбор ограничения Telegram, оставлен как обоснование отмены.

**Ограничение Telegram, которое меняет флоу.** Бот не может написать человеку первым: пока клиент не нажал «Старт», у нас нет его `chat_id`, и отправить сообщение некуда. Обойти это нельзя — так устроен Telegram, и на любом тарифе любого провайдера бот остаётся ботом. (В Wazzup есть отдельный канал «личный Telegram» по QR, он писать первым умеет, но это уже платно и не бот.)

Как с этим живём:
- На экране «Заявка принята» для выбравших Telegram — заметная кнопка «Открыть бота» со ссылкой `t.me/<бот>?start=<токен брони>`. Одно нажатие, и бот сразу присылает подтверждение: для клиента это выглядит как обещанное сообщение, а не как лишний шаг.
- Ссылка живёт и дальше: дублируется в CRM, администратор может отправить её вручную.
- Если клиент так и не нажал «Старт», сообщения остаются в очереди с пометкой «клиент не подключил Telegram», а администратор видит это в карточке и звонит.
- Телефон клиента при этом остаётся ключом: после «Старта» `chat_id` привязывается к его карточке по токену заявки.
- WhatsApp через Wazzup этого ограничения не имеет: там мы пишем первыми по номеру. Поэтому в бою WhatsApp остаётся основным каналом, а Telegram — для тех, кто сам его выбрал.

**Что делаем в Telegram-части.**
- Отправщик Outbox в минутном тике: аренда записи, повторы с паузой, отмена устаревших, список разрешённых номеров на время теста.
- Адаптер Telegram Bot API по `TELEGRAM_BOT_TOKEN`: отправка сообщения, разбор ошибок (нет `chat_id`, бот заблокирован клиентом).
- Вебхук бота: `/start <токен>` привязывает `chat_id` к клиенту и сразу отправляет накопившиеся сообщения; входящие сообщения клиента попадают в ленту брони и в колокольчик.
- Токен и адрес вебхука — только в серверном `.env`. Бота заводит пользователь через @BotFather.

**Что делаем потом в Wazzup-части** (всё изучено, см. `docs/WAZZUP_INTEGRATION.md`): адаптер WhatsApp (`POST /v3/message`), вебхук статусов, окно чатов для ответов администратора. Пробный период там всего 3 дня — начинать только когда код готов и проверен на Telegram.

### Этап 2б. Входящие сообщения и ответ из CRM ⬜ (1,5 дня)
Требование пользователя 22.09: администратор видит новое сообщение клиента и может ответить.
- Решение: встраиваемое окно чатов Wazzup (`POST /v3/iframe`) вместо собственного мессенджера. Панель «Чат с клиентом» в карточке брони и клиента, страница «Чаты» со счётчиком неотвеченных (`GET /v3/unanswered/<id>`).
- Входящие и ответы администратора дублируются в ленту клиента и брони через вебхук; новое сообщение — в колокольчик.
- Синхронизация пользователей CRM в Wazzup (`POST /v3/users`) при старте.
- Телефония прорабатывается отдельно (решение пользователя).

### Этап 3. Интерфейс администратора ⬜ (1 день) — п. 3
- Блок «Планируют выезд · 24 часа» на экране «Сегодня» и на экране охраны: номер брони, госномер, дата и время выезда, клиент, статус; просроченные выезды помечены.
- Фильтр «Планируют выезд» в табличном виде. Признак считается на сервере, чтобы не ломать гидратацию.
- Удаление ручного ввода времени (см. раздел 2, п. 1).

### Этап 4. Деньги ⬜ (4 дня) — п. 5, 7
- Возврат: причина обязательна, способ по умолчанию наличные, сумма не больше оплаченной, привязка к смене. Из баннера пересчёта при досрочном выезде — кнопка «Оформить возврат» с готовой суммой.
- Касса: открытие смены при входе администратора (остаток подставляется из прошлой смены, расхождение при приёме фиксируется); платежи привязываются к открытой смене; при включённом `cash.requireShift` без смены платёж не проводится. Владелец может закрыть чужую смену принудительно.
- Инкассация: отдельная форма и история.
- Закрытие смены: отдельная кнопка «Закрыть смену», форма по ТЗ и повторное подтверждение перед сохранением. Расчётный остаток = начало + наличные − возвраты наличными − инкассации, ввод фактического остатка, расхождение, снимок итогов в записи смены, «Скопировать отчёт», уведомление владельцу при расхождении. Выход из аккаунта смену не закрывает; при открытой смене показываем напоминание.
- Финансовый отчёт `/admin/reports` (владелец): период с первого числа месяца по сегодня и произвольный, границы по московскому времени. Показатели ТЗ плюс возвраты отдельной строкой и список смен за период.

### Этап 5. Табель и расчёт смен ⬜ (3 дня) — п. 6

Пользователь прислал 22.09 скриншот действующей таблицы (Google Sheets, вкладки по месяцам «Июль 26»). Делаем ту же таблицу, но кликами вместо ручного ввода, и со счётом денег.

**Как ведётся сейчас.** Лист на месяц: строки — числа 1…31, столбцы — «администратор ночь», «администратор день», «водитель ночь», «водитель день», «охрана». В ячейке — имя сотрудника. Справа отдельная табличка зарплаты: сотрудник, должность, число смен, ставка за смену, сумма; внизу «итого» (в июле 790 000 ₽).

**Что это добавляет к ТЗ.**
- Должностей не три, а пять: администратор, водитель, охрана, уборщица, «доп. сотрудник». Список должностей и набор столбцов должны настраиваться, а не быть прошиты в коде.
- У охраны смена одна на сутки, у администратора и водителя — день и ночь. Значит набор слотов задаётся для каждой должности.
- Ставка за смену у каждого сотрудника своя (4 000 ₽ у администраторов и водителей, 5 000 ₽ у охраны), и по ней считается зарплата. В ТЗ это только упомянуто («использовать данные для расчёта заработной платы») — теперь понятно, что нужен готовый расчёт.
- Уборщица в июле отмечена 30 сменами: не все должности работают по сменам день и ночь.

**Что делаем.**
- `Employee`: имя, должность, ставка за смену, активность. Необязательная связь с `User` (у администраторов есть логины, у остальных нет).
- `WorkShift`: сотрудник, дата, слот (`DAY`, `NIGHT`, `FULL`), часы, кто отметил. Уникальность: сотрудник + дата + слот.
- Календарь `/admin/staff`: месяц, строки — числа с днями недели, столбцы — должность и слот, как в таблице заказчика. Клик по ячейке открывает список сотрудников этой должности, последние выбранные сверху. Выходные подсвечены, сегодня выделено.
- Удобства, которых нет в таблице: «повторить вчерашний день» и «продолжить до конца месяца» (в таблице один человек стоит неделями подряд), предупреждение, если сотрудник поставлен на ночь и следующий день подряд, пустые ячейки видно сразу.
- Панель расчёта рядом с календарём: сотрудник, должность, смен за месяц, ставка, сумма, итого. Печать и выгрузка.
- Открытие кассовой смены автоматически отмечает смену администратора (слот по времени открытия, можно поправить).
- Права: смотрит и правит владелец и администраторы; ставка и справочник сотрудников — только владелец.

Порядок задан зависимостями: сообщение о подтверждении запускается автоподтверждением; отчёт читает данные кассы; табель независим и идёт последним. Этапы 2а и 2б можно поднять выше этапа 1, если отправка нужна раньше: они зависят только от минутного тика из этапа 2. Всего 16–17 рабочих дней с отправкой и окном чатов.

## 6. Отправка сообщений: состояние

Провайдер определён 22.09 — Wazzup24, подробности в `docs/WAZZUP_INTEGRATION.md`. Код отправщика не начат. На stage `RUN_SCHEDULER=1`, в очереди 7 неотправленных сообщений за 08–21.09, поэтому в отправщике обязательны два предохранителя: сообщения старше нескольких часов не уходят; на время теста отправка только на номера из списка разрешённых. Черновики четырёх сообщений клиенту — `docs/MESSAGE_TEMPLATES_2026-09-22.md`.
