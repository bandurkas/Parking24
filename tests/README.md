# Тесты Parking24

Страховка перед доработкой по ТЗ от 21.09 (`docs/TZ_2026-09-21_PLAN.md`, этап 0). Задача тестов — ловить поломки расчётов, конвейера статусов и денег при дальнейших правках.

## Быстрый запуск

```bash
npm test          # юнит-тесты, 2 секунды, база и сервер не нужны
npm run test:e2e  # сценарии в браузере против локального сервера на :3100
```

## Юнит-тесты — `tests/unit/`

Встроенный тест-раннер Node, отдельных зависимостей нет. Покрывают чистые расчёты, которые нельзя ломать:

- `periods.test.ts` — сутки парковки по датам включительно (правило 21.09), 24-часовые периоды комнат с льготой 60 минут;
- `dates.test.ts` — `bookingDays` по видам ресурса, фактические сутки по московскому времени, формат дат;
- `phone.test.ts` — нормализация телефонов и госномеров, дедупликация клиентов держится на ней;
- `render.test.ts` — подстановка переменных в шаблоны сообщений, боевые шаблоны из `prisma/templates.ts` без имени и ссылок;
- `scheduler.test.ts` — ядро минутного тика (Ф1): изоляция сканов, «занят», режимы «выкл / пробно / вкл», пауза, журнал ошибок, состояние карточки, проверка секрета.
- `overstay.test.ts` — перестой (Ф2а): сутки, тариф, ДОЛГ, начисление при выезде и продлении, занятость; СП: льготный час до 01:00 МСК, остаток начисления после снижения суммы владельцем (правило «выезд в перестое только текущими сутками» удалено в Ф9а — задать другую дату нечем);
- `correction.test.ts` — «Исправить статус» датой (Ф9а): какие даты спросить (`neededDates`), проверки дат — формат, не в будущем, выезд не раньше заезда, не раньше создания брони;
- `dates.test.ts` — ещё и фактические сутки с льготным часом после плановой даты выезда (СП);
- `refund.test.ts` — возврат (СП): у «Выехал» сначала переплата, остаток уменьшает сумму, у остальных сумма не меняется; предел администратора после выезда; перебором — «не оплачено» у «Выехал» не меняется.
- МФ-2 (настройки CRM): `seed-sync.test.ts` — seed не затирает текст, правленый в CRM, и не пишет `isActive` правил; `template-vars.test.ts` — список переменных из рендера, опечатки, предпросмотр; `settings-validate.test.ts`, `user-rules.test.ts`, `tariff-rules.test.ts`, `rule-when.test.ts` — проверки страниц настроек.

## Сценарии в браузере — `tests/e2e/`

Playwright берётся из глобального `~/node_modules` (в проекте его нет специально, чтобы не тянуть браузеры в образ). Если не установлен: `npm i -g playwright && npx playwright install chromium`.

```bash
npx next dev -p 3100                                  # в отдельном окне
node tests/e2e/site-lead.mjs                          # заявка с сайта → CRM
node tests/e2e/crm-pipeline.mjs                       # оплата → заезд → выезд → «по факту» → возврат переплаты, отказы
node tests/e2e/autoconfirm.mjs --login owner --password owner12345   # автоподтверждение и автоотклонение
node tests/e2e/scheduler.mjs --login owner --password owner12345    # планировщик: вход по секрету, пульс, пауза
node tests/e2e/overstay.mjs --login owner --password owner12345     # перестой: занятость, ДОЛГ, начисление при выезде, «Продлить»
node tests/e2e/roles.mjs                                            # МФ-UI: роли driver/parker (сиды driver12345/parker12345), мобильное меню, «← CRM»; на stage пароли через E2E_DRIVER_PASSWORD/E2E_PARKER_PASSWORD/E2E_GUARD_PASSWORD/E2E_OWNER_PASSWORD
node tests/e2e/f9a-no-manual-time.mjs --login owner --password owner12345   # Ф9а: без ручного времени, «Исправить статус» датой; второй вход admin — на stage E2E_ADMIN_PASSWORD или --admin-password
node tests/e2e/mf2-settings.mjs --login owner --password owner12345         # МФ-2: шаблоны, правила, ссылки, тарифы, пользователи и пароли; admin/guard — E2E_ADMIN_PASSWORD/E2E_GUARD_PASSWORD
node tests/e2e/site-lead.mjs --base https://parking24.168-231-118-173.sslip.io --password '<пароль admin>'
node tests/e2e/site-lead.mjs --headed                 # смотреть глазами
```

Параметры: `--base` (по умолчанию `http://localhost:3100`), `--login`, `--password`, `--headed`, `--secret` (секрет планировщика). Те же значения читаются из `E2E_BASE`, `E2E_LOGIN`, `E2E_PASSWORD`, `E2E_CRON_SECRET`; секрет без параметра берётся из `CRON_SECRET` локального `.env`.

На stage — только со stage-логинами (`--login admin --password <пароль admin>`, для автоподтверждения и планировщика — владелец) и, для планировщика, с секретом stage (`--secret`, лежит в `/opt/parking24/.env`).

**Что проверяет `site-lead.mjs`:** цена на калькуляторе по правилу «даты включительно» (3 суток = 1 050 ₽), экран «Заявка принята» с номером, заявка в CRM с именем и источником «Сайт», сообщение в очереди в выбранном клиентом мессенджере.

**Что проверяет `crm-pipeline.mjs`:** расчёт в быстрой заявке, полная оплата переводит бронь в «Подтверждена», «Заехал» и «Выехал» одним нажатием — поля времени нет, диалогов нет, время события серверное и стоит в строке ленты «Заехал → Выехал», после выезда в тот же день — баннер «Стоянка по факту» → «Пересчитать по факту» (350 ₽) → «Возврат» подставляет переплату 700, без причины кнопка недоступна, возврат 350 с причиной (сумма та же, «оплачено»), отказы сервера: 800 — «больше оплаченного», 400 — «только переплату»; в карточке клиента «К оплате» — «—». Входит администратором.

**Что проверяет `scheduler.mjs`:** запасной вход `POST /api/cron/automations` без секрета и с чужим — 404, `GET` не принимается, с секретом — результат тика; пять вызовов подряд без 500; карточка «Планировщик» в настройках показывает время последнего тика; пауза останавливает тик и снимается обратно (в `finally`). Требует входа владельцем.

**Что проверяет `autoconfirm.mjs`:** заявка подтверждается сама, когда места есть; следующая на те же даты отклоняется; администратор получает уведомление; с выключенной настройкой заявка снова ждёт администратора. Требует входа владельцем. Сценарий сам опускает порог до «занято в пуле на эти даты + 1» (по сетке `/admin/occupancy`: машины в перестое держат место и на дальние даты), берёт случайные дальние даты и возвращает настройки как были, в том числе при сбое.

**Что проверяет `overstay.mjs` (Ф2а):** легковая бронь на позавчера–вчера по Москве, оплачена и «Заехал» → плашка «ПЕРЕСТОЙ · 1 сут. · ДОЛГ 350 ₽» и строка долга в карточке, «занято бронями» +1, подписи на «Сегодня», у охраны и на доске; «Выехал» → 1 050 ₽, 3 сут., «не хватает 350 ₽», запись «Начислен перестой», без баннера «по факту»; исправление статуса назад и повторный выезд второй раз не начисляют; долг, принятый заранее, гасит строку; два одновременных «Выехал» из двух вкладок (нажаты разом, confirm в обеих) — одно начисление; «Продлить» до завтра — 1 400 ₽; «Исправить статус» в «Выехал» с датой выезда (сегодня) долг не начисляет и пишет в ленту «Исправление: … · выезд <дата>, без времени · причина» и «Перестой 1 сут. не начислен (350 ₽) · по дате выезда <дата> · причина». Ф9а: «Выехал» в перестое — одно нажатие после confirm «…ДОЛГ 1 сут. × 350 ₽ = 350 ₽. Отметить выезд?» (сценарий соглашается и считает диалоги), подсказки «текущими сутками» нет. СП: в перестое «Изменить бронь» не меняет дату выезда (поле недоступно, сервер отказывает и в обход формы), сумма в обход формы — отказ; после выезда «Снять начисление за перестой» с причиной (1 050 → 700 ₽); после «Продлить» уменьшить сумму в «Изменить бронь» нельзя; возврат владельца после выезда уменьшает сумму (1 050 → 700 ₽) и гасит начисление, которое можно снять. Не запускать с 00:00 до 01:00 МСК (льготный час — перестоя ещё нет). Требует входа владельцем.

**Что проверяет `f9a-no-manual-time.mjs` (Ф9а, `docs/phases/PHASE_09A_MANUAL_TIME.md`):** «Ожидает оплаты» → «Заехал» → «Выехал» в карточке — по одному нажатию, подсказка «Время события поставит система», в полосе этапов сегодняшняя дата и текущее московское время, диалогов нет; владелец возвращает закрытую бронь в «Заехал» без дат и снова закрывает датой — баннер «Стоянка по факту» от снятого выезда не всплывает; на доске перетаскивание в «Заехал» и «Выехал» без prompt и confirm, у владельца «Отменить» возвращает «Выехал» → «Заехал»; у администратора тост закрытого статуса без «Отменить» («вернуть может владелец — „Исправить статус“»), в закрытой брони серый текст вместо «Исправить статус»; «Исправить статус» → «Заехал»: поле «Дата заезда» (min — дата создания, max — сегодня), подпись «Время не ставим: в карточке будет только дата.», кнопка заблокирована без даты (красного текста у пустого поля нет), будущая дата и дата раньше создания брони — ошибка под полем и отказ сервера в обход формы; «Заехал» → «Выехал»: только «Дата выезда», вчерашняя при сегодняшнем заезде — «Выезд не может быть раньше заезда» (дата заезда из сохранённой отметки), успех — «<дата> · без времени» в полосе, шапка «Выехал <дата> · без времени (отмечено …)», строки «по факту N ч» и баннера нет, сумма и «оплачено» те же, одна запись в ленте и `STATUS_CHANGE` с признаком, причиной и датой в журнале; признак «без времени» не залипает (датой → «Подтверждена» → кнопкой — снова время); заезд датой + выезд кнопкой — баннер «Стоянка по факту: 1 сут. (по плану 3)» без часов и «по датам: 1 сут.»; «Ожидает оплаты» → «Выехал» спрашивает обе даты; перестой — confirm с суммой в карточке, на «Сегодня» и на доске (отказ ничего не меняет, согласие начисляет), исправление в «Выехал» (из «Заехал» и из «Подтверждена» двумя датами) — сумма та же, «Перестой 1 сут. не начислен (350 ₽) · по дате выезда <дата> · причина» одной строкой, экран охраны — одно нажатие без диалогов; поле `datetime-local` не появляется ни разу. Брони создаются сегодня, а дата исправления не бывает раньше создания брони: успешные исправления — сегодняшней датой, прошедшая дата в успешном пути — только в юните `correction.test.ts`. Входит владельцем, второй контекст — `admin` (`E2E_ADMIN_PASSWORD` или `--admin-password`, по умолчанию `admin12345`). Не запускать с 00:00 до 01:00 МСК.

**Что проверяет `mf2-settings.mjs` (МФ-2, `docs/phases/PHASE_MF2_CRM_SETTINGS.md`):** шесть плиток «Настроек» без 404; неправленый шаблон с номером брони (правленый заказчиком не трогается) — правка, предпросмотр на демонстрационной брони №128 без `{{`, опечатка `{{links.rout}}` не сохраняется (названа переменная), пропажа номера брони — предупреждение и «Сохранить всё равно», «Вернуть текст по умолчанию»; выключатель правила переживает перезагрузку; ссылки на отзывы и видео сохраняются, `javascript:` отклоняется, обе ссылки видны в предпросмотре; цена тарифа сохраняется, единственный стартовый тариф легковых не выключается; владелец заводит водителя, парковщика и администратора `e2e_*` — каждый входит на свой экран; пароль, заданный владельцем, закрывает сессию в другом браузере; свой пароль в «Мой пароль» оставляет текущее окно и закрывает другое; выключенный не входит; администратор и охрана не попадают ни на одну страницу настроек и по прямому адресу; в журнале есть записи, паролей нет. Локально (`--base` на localhost) ещё: `npx prisma db seed` посреди сценария (правленый текст и выключатель не затёрты, неправленый шаблон вернулся к тексту по умолчанию) и временное правило на «Отклонена» через Prisma — предупреждение при включённом автоподтверждении. Всё изменённое возвращается в `finally`, тестовые пользователи выключаются (удаляет `cleanup.mjs`/`cleanup.sql` по логину `e2e_*`).

### Подводные камни

- **Считать строки на странице — по `innerText`, не по `textContent`.** После полной загрузки страницы в `body` лежат скрипты Next с теми же данными, и `textContent` находит каждую строку ленты дважды.
- **Даты перестоя — `moscowPlus`, не `isoPlus`.** `isoPlus` считает по UTC и с 21:00 до 24:00 UTC отстаёт от Москвы на день.

- **Антибот.** `/api/public/lead` молча отвечает «ок» и не создаёт заявку, если форму отправили быстрее 1,5 секунды после загрузки. Сценарий выдерживает паузу; при отладке помнить об этом.
- **Быстрая заявка не открывает карточку** — после создания появляется тост «Заявка №N создана · открыть», сценарий переходит по этой ссылке.
- **Ручного времени нет (Ф9а).** «Заехал» и «Выехал» — одно нажатие, время ставит сервер. В перестое перед «Выехал» в CRM (не у охраны) — `window.confirm`: Playwright без обработчика `dialog` молча его отклоняет, и выезд не проходит — сценарии перестоя вешают обработчик и считают диалоги.
- **Гидратация кнопок.** Нажатие до гидратации молчит, а повтор может провести переход дважды. Новые сценарии ждут, пока у самого элемента появятся обработчики React (`__reactProps…`), и только потом нажимают.
- **Дата «Исправить статус» не раньше создания брони.** Брони e2e создаются сегодня — прошедшей датой исправить нельзя; отказы проверяются снятием `min`/`max` у поля.
- **Ограничение частоты.** Сайт принимает 8 заявок за 10 минут с одного адреса. Несколько прогонов подряд упираются в него, и сценарий скажет об этом прямо. Счётчик хранится в памяти процесса: перезапуск сервера его сбрасывает.
- **Планировщик локально выключен.** В `.env` `RUN_SCHEDULER=0`: таймера нет, тик дёргается только через `/api/cron/automations`, поэтому e2e детерминированы. Таймер в dev работает на коде, загруженном при старте сервера (`register()` после правок не перевызывается), а HTTP-вход — уже на новом коде: после правки скана перезапустить dev или звать тик через вход.
- **Гидратация.** Поля форм управляются React: если заполнить их сразу после загрузки страницы, значение откатится. Для ввода используется `fillReliably` из `lib.mjs`, который проверяет результат и повторяет.

## Тестовые данные

Сценарии создают брони с госномером `Т000…` и клиентов с телефоном `+7999…`. Эти признаки не пересекаются с реальными данными.

```bash
node tests/e2e/cleanup.mjs                                         # локально
docker exec -i parking24-db psql -U parking24 -d parking24 < tests/e2e/cleanup.sql   # на stage
```

**На stage чистить обязательно:** тестовые брони занимают места в сетке занятости и попадут в автоподтверждение и отчёты.

## МФ-1: автоподтверждение — надёжность и предохранитель

```bash
node tests/e2e/mf1-autoconfirm.mjs --base http://localhost:3111 --login owner --password owner12345
```

**Что проверяет `mf1-autoconfirm.mjs`** (`docs/phases/PHASE_MF1_AUTOCONFIRM.md`): в форме ёмкости галочки автоподтверждения нет, панель отдельно; пока нет отправщика и правила на «Отклонена», кнопка «Включить автоподтверждение» неактивна даже с «Понимаю», непройденные проверки подписаны; без «Понимаю» не включить; устаревшую форму (отправщик выключили, пока она открыта) сервер отклоняет; «включено. Порог N, резерв M мест», «Включил <кто>, <когда>» и запись журнала; «Сохранить» ёмкости выключатель не трогает; две одновременные заявки на последнее место — одна «Место забронировано», вторая «Мест нет», у отклонённой `NO_SPACE`, ровно одно уведомление «Заявка №N отклонена автоматически: на <даты по-русски> мест нет (занято N, порог M)», записанное той же транзакцией, что и бронь (`xmin` строк совпадает), отказ поставлен в очередь, уведомление видно в колокольчике; мест нет, а отправщик выключен — «Новая заявка» с пояснением, без уведомления; три одинаковые заявки одновременно — один номер, одна бронь, одно сообщение (и то же при выключенном автоподтверждении); цена 0 (тариф мото выключен) — «Новая заявка» с «Стоимость не рассчитана»; 15 разных заявок одновременно — ни одного 5xx, все в базе; сценарий сам держит замок занятости 12 с (дольше срока транзакции 10 с) — две заявки в это время отвечают без 5xx и создаются «Новой заявкой» с пояснением «Автоподтверждение не сработало (перегрузка)» (путь одного повтора без автоматики); часы браузера на час вперёд — заявка создана, отправка через 300 мс — «ок» без заявки; «Вернуть ручной режим» одним нажатием, «Выключил …» и запись журнала; броней «Отклонена · нет мест» без уведомления — 0.

- **Заявки — прямыми POST с отдельным `X-Forwarded-For` на каждый запрос** (или на группу одинаковых): у каждого адреса свой лимит 8 за 10 минут, поэтому сценарий не съедает лимит браузерных сценариев и не упирается в него сам.
- **Предохранитель в e2e открывается данными, а не переменной окружения** (`tests/e2e/autoconfirm-fixture.mjs`): правило `e2e_on_rejected_no_space` с шаблоном на «Отклонена» и `Setting messaging.senderEnabled = true`; в `finally` всё возвращается (правило и шаблон удаляются, выключатели — как были). Это же делает `autoconfirm.mjs`.
- **На stage** базы у сценария нет: оба сценария проверяют только, что включить нельзя, и пишут «Пропуск». Счастливый путь автоподтверждения на stage вернётся, когда там появятся отправщик (Ф4) и правило отказа (Ф5).
- `cleanup.mjs` убирает брони сценария (телефоны `+7999…`), уведомления уходят каскадом.
## Ф2б — уведомления о перестое, колокольчик, режимы сканов

- Юнит `f2b-notices.test.ts`: ключи `overstay:<бронь>:<дата выезда>` и `unpaid-out:<бронь>:<дата выезда>`, разбор 4-го аргумента `notify` (tx или `{ tx, key }`), `planOverstay` (известные пропускаются, старые по дате выезда первыми, лимит), условие само закрытия (только вид `OVERSTAY`, при пустом списке ключей `notIn` не ставится — в Prisma 6.19 пустой `notIn` гасит всё), тексты, `withScanMode` (меняет только свой ключ), реестр кодов сканов, тик с несколькими сканами в разных режимах.
- E2E: `node tests/e2e/f2b-notices.mjs --login owner --password owner12345` (секрет тика — как у `scheduler.mjs`). **Что проверяет:** `/api/admin/notices` без входа 401, охрана/водитель/парковщик 403, администратор 200; у администратора нет режимов сканов в настройках; скан «Перестой» в «Пробно» — `dry.overstay` в результате тика и «было бы: N» в карточке, уведомления и строки в ленте нет; в «Вкл» — одно уведомление на бронь (текст, время по Москве) и одна строка в ленте, повторный тик — 0 новых; бейдж колокольчика обновляется опросом через минуту без перезагрузки (часы вкладки перематываются `page.clock`); «Выехал» с долгом — сразу «Машина выехала, не оплачено 350 ₽ — бронь №…», следующий тик закрывает уведомление о перестое; «Прочитано» не гасит «не оплачено», пришедшее после открытия панели; «Исправить статус» назад и повторный выезд второго «не оплачено» не создают; смена режима в журнале. Режим скана запоминается и возвращается в `finally`. **На stage** шаг «Прочитано» пропускается (погасил бы настоящие уведомления; включить — `E2E_READ=1`), а режим «Вкл» на время теста уведомит обо всех настоящих перестоях — это ожидаемо (PHASE_02 §4.6 п. 26). Пароли ролей на stage — `E2E_ADMIN_PASSWORD`/`E2E_GUARD_PASSWORD`/`E2E_DRIVER_PASSWORD`/`E2E_PARKER_PASSWORD`. Не запускать с 00:00 до 01:00 МСК.

## Ф10 «Деньги» (дописано 24.09)

**Юнит:** `recalc.test.ts` — сутки к оплате (поздний заезд не уменьшает, ранний добавляет; перебором равно «сутки от min(заезд, плановая дата) до выезда»), когда открыт баннер «Стоянка по факту», цена по тарифу (`priceFor`, общая с `quote`), пересчёт: тариф / ручная цена пропорционально / «по запросу» и ручная цена после перестоя — без пересчёта, смена ступени тарифа не делает досрочный выезд дороже плана; `refund.test.ts` дописан — откат «Подтверждена» → «Ожидает оплаты» при оплате 0, правило сторно (владелец, только оплата, один раз, не больше оплаченного).

**e2e `f10-money.mjs`** (`node tests/e2e/f10-money.mjs --login owner --password owner12345`; второй вход — `admin`, на stage `E2E_ADMIN_PASSWORD` или `--admin-password`; не запускать с 00:00 до 01:00 МСК): владелец работает в браузере с поясом `America/New_York` — время платежа обязано быть московским. М1 администратор: досрочный выезд → расчёт с сервера → «Пересчитать по факту» (350 ₽) → «переплата 700 ₽», «Оформить возврат 700 ₽» → возврат 400 (остаток «Оформить возврат 300 ₽») → 300 картой; строка платежа «дата, время МСК · способ · сотрудник», причина, сумма. М2 бронь 31 сут. — сумма в баннере и после нажатия одна, запрос несёт сумму из баннера. М3 ручная цена — пропорционально. М4 фура с ценой 0 — оплата 1 ₽ в обход формы не делает «Подтверждена», пересчёта нет (и в обход формы). М5 поздний заезд — баннера нет. М6 «Исправить статус» датой — баннер, пересчитывает только владелец (отказ администратору в обход формы), сумма не из баннера — «Бронь изменилась». М7 владелец открывает закрытую бронь — строка в ленте. М8 сторно — отдельная запись, «оплачено» ↓, сумма та же, статус назад, повтор и администратор — отказ сервера, журнал. М9 перестой → «Отменена» — «Перестой не начислен». М10 снятие начисления при открытом баннере — отказ. М11 полный возврат до заезда — «Ожидает оплаты». М12 «Отклонение снято» в ленте.

**Отказ сервера в обход формы:** сценарий запоминает запросы серверных действий (`Next-Action` + тело) из настоящих нажатий и повторяет их из другой сессии или с другими аргументами (`callAction` в `f10-money.mjs`) — так проверяются права и пределы без поиска id действия в сборке.

`crm-pipeline.mjs` — после «Пересчитать по факту» строка «переплата 700 ₽» и кнопка «Оформить возврат 700 ₽», после возврата 350 — «переплата 350 ₽». `f9a-no-manual-time.mjs` Н1/Н4 — исправление в «Выехал» датой при досрочном выезде теперь открывает баннер по датам (сутки считает Ф10).

## Отправщик сообщений (Ф4 шаг 0)

```bash
node tests/e2e/f4-sender.mjs --login owner --password owner12345   # только локально, на stage — пропуск
```

**Юнит `sender.test.ts`:** выбор канала `channelForClient`, предохранители `decide` по порядку (возраст от `scheduledAt`, пустой текст, нет получателя, «не беспокоить» только для рекламы, не мессенджер, мессенджер не выбран, список разрешённых и `OUTBOX_ALLOWLIST`, нет провайдера / не отвечает, длина текста), план прохода (потолки за тик и час, одно сообщение клиенту), паузы повторов, раскладка ответа адаптера, `senderEnabled`, подписи очереди, шаги тика, `mergeMode`, строгое «ЧЧ:ММ» (`isHHMM`) и чтение кривого «25:00» как 12:00.

**Что проверяет `f4-sender.mjs`:** карточка «Отправка сообщений» у владельца, по умолчанию «Выключено», «только на номера из списка» и порог 6 ч; выключенный отправщик тик не запускает. Заявка с сайта с Telegram встаёт в очередь с каналом Telegram. Провайдер «Заглушка» и список разрешённых (номер «8 …» сохраняется в E.164) — через карточку. «Пробно»: в ответе тика `dry.sender = 1`, в базе ничего не изменилось, в карточке журнал «ушло бы». «Включено»: сообщение уходит через заглушку — `SENT`, `providerMessageId` первого вызова, в карточке брони «отправлено <время по Москве>», канал «Telegram», в ленте «Отправлено клиенту в Telegram»; сообщение старше 48 ч — «устарело, не отправлено»; без телефона — «пропущено: нет получателя»; номер не в списке — остаётся в очереди с причиной, попытка не тратится. Два тика разом и третий — ни одной второй отправки. Чужая аренда не трогается; зависшая (процесс упал после отправки) — «статус отправки неизвестен» без повтора и уведомление. Сбой сети — повтор через минуту, отказ провайдера — «не доставлено», уведомление «Позвоните клиенту». «Убрать устаревшие из очереди». Список выключен — уходит всем и `messaging.senderEnabled = true`. Две ошибки канала подряд при пороге 2 — отправщик сам выключается слиянием карты режимов (чужой ключ цел) и пишет `CHANNEL_DOWN`. Заявка с «25:00» мимо формы — 400 «ЧЧ:ММ».

- **Только локально.** Заглушка провайдера есть лишь вне production, а записи для крайних случаев (старая, без телефона, чужая аренда) расставляются через базу из `.env`. На stage сценарий сам выходит с пропуском; там отправщик проверяется руками по `docs/phases/PHASE_04_SENDER.md` §8.
- **Чужие неотправленные записи** на время сценария паркуются (`scheduledAt` уносится в будущее) и возвращаются в `finally`; настройки `sender.*`, `messaging.*` и `scheduler.scans` тоже возвращаются как были. Сценарий оборвали силой — проверить очередь в карточке настроек.
- **Нужен `RUN_SCHEDULER=0`**: таймерный тик посреди сценария меняет записи раньше проверок.
- **Сбой seed на сервере** (`docker/entrypoint.sh`) теперь останавливает старт контейнера со строкой «seed не прошёл»; аварийный выход — `SEED_STRICT=0` в `.env`.

## Ф8 — «Не смогли к нам попасть»

- Юнит `segments.test.ts`: правило сегмента (`src/lib/segments.ts`) — в сегменте только клиенты с отказом `NO_SPACE`; «потом забронировал» — бронь в «Ожидает оплаты»/«Подтверждена»/«Заехал»/«Выехал», созданная после последнего отказа, или сам последний отказ, возвращённый в работу (после Ф10 отметка не стирается); бронь до отказа и между отказами не считается; новая/отменённая/не приехал/отклонённая — не считаются; пустой `rejectedAt` — время создания; сортировка свежих отказов сверху; CSV — кавычки, «;», апостроф перед `= + - @` (имя с сайта и телефон `+7…` Excel не примет за формулу), время отказа по Москве, календарные даты заявки.
- E2E: `node tests/e2e/f8-segment.mjs --login owner --password owner12345`. **Что проверяет:** две заявки с сайта при пороге 0 (предохранитель открыт фикстурой МФ-1) — «мест нет», `REJECTED/NO_SPACE`; порог 395 — второй клиент бронирует на другие даты («Место забронировано»); вкладка «Не смогли к нам попасть · N» в «Клиентах» (N = клиентов с отказом в базе), подпись про ст. 18 «О рекламе»; строка клиента — имя, телефон, мессенджер, время отказа по Москве, № заявки, «потом забронировал: нет» / «да · №N · Ожидает оплаты»; «Выгрузить CSV» у владельца — файл `ne-smogli-popast-<дата>.csv` с BOM и заголовком, строки обоих клиентов, одна запись `EXPORT` в журнале от владельца с числом строк; администратор видит сегмент, кнопки нет, выгрузка в обход — 403 «Выгрузка доступна только владельцу»; охрана, водитель, парковщик — редирект на свой экран и 403; без входа — 401; чужие попытки в журнал не пишут; клиент из «Чёрного списка» из сегмента уходит. Владелец определяется по адресу `/admin/settings/capacity` (как в `mf1-autoconfirm.mjs`), а не по кнопке выгрузки — пропажа кнопки роняет набор, а не даёт «Пропуск». Выключатели и порог возвращаются в `finally`. **На stage** (без базы) — только вкладка, выгрузка и права; второй вход — `E2E_ADMIN_PASSWORD`/`--admin-password`, роли — `E2E_GUARD/DRIVER/PARKER_PASSWORD`. Каждая выгрузка на stage оставляет строку `EXPORT` в журнале (так и должно быть).

## Ф11 «Касса» (дописано 24.09)

**Юнит `cash.test.ts`:** расчётный остаток смены (ТЗ 7.3) = начало + наличные оплаты − наличные возвраты − инкассации; карта, перевод, онлайн и возвраты не наличными остаток не меняют; сторно уменьшает оплаты своего способа, а не «возвраты наличными» (сторно оплаты из прошлой смены уводит наличные текущей в минус — честно); непроведённые платежи не считаются; расхождение со знаком, расчёт не обрезается в ноль; дата смены `shiftDateOf` — открытие до 02:00 МСК относится ко вчерашней дате; текст «Скопировать отчёт» — все строки ТЗ 7.2, рубли без «Rp», у инкассации кто забрал и кто передал.

**e2e `f11-cash.mjs`** (`node tests/e2e/f11-cash.mjs --login owner --password owner12345`; второй вход — `admin`, на stage `E2E_ADMIN_PASSWORD` или `--admin-password`; охрана и водитель — `E2E_GUARD_PASSWORD`/`E2E_DRIVER_PASSWORD`): «Касса» в меню — ссылка; администратор открывает смену (подсказка остатка, своё имя) → инкассация 1 000 ₽ «E2E-инкассатор» (метка смены для очистки) → вторая смена со старой страницы владельца — отказ «Смена №N уже открыта (Администратор, …)»; оплата наличными 700, картой 350, возврат наличными 200, сторно оплаты картой (владелец) — после каждого шага строки отчёта и расчётный остаток, операции смены с № брони; «Выйти» на экране КПП у владельца (`?guard=1`) — то же напоминание; «Выйти» при открытой смене — напоминание со ссылкой «Закрыть смену», «Всё равно выйти» выходит, после входа смена та же; закрытие: расхождение на экране, повторное подтверждение с суммами, «Назад» — смена открыта; оплата, пришедшая при открытом подтверждении, — отказ «Суммы изменились»; закрытие 4 500 при расчётном 4 600 — снимок (все строки), «Скопировать отчёт», в колокольчике `CASH_MISMATCH` «закрыта с расхождением −100 ₽»; после закрытия — «Смена не открыта», подсказка 4500, строка истории; инкассация со старой страницы — «Смена уже закрыта»; платёж после закрытия в снимок и операции не попал; охрана и водитель по `/admin/cash` и по ссылке на отчёт уходят на свой экран. **Если в базе уже открыта смена — сценарий пишет «ПРОПУСК» и ничего не трогает** (чужую смену не закрывает). При сбое посреди сценария тестовая смена закрывается в `finally` свежим входом владельца и только по своему номеру — чужую открытую смену уборка не закроет. На stage запускать при закрытой кассе: платежи других сценариев, прошедшие при открытой настоящей смене, попадут в неё (очистка их удалит вместе с тестовыми бронями).

**Очистка:** `cleanup.mjs` / `cleanup.sql` удаляют смены с инкассацией `takenBy` на «E2E…» (и их инкассации, и их уведомления `CASH_MISMATCH` «Смена №N …»); чужие платежи в такой смене не удаляются — только отвязываются (`cashShiftId = NULL`).

`roles.mjs` — «Касса» в листе «Ещё» теперь ссылка; при открытой смене «Выйти» проходит через напоминание («Всё равно выйти»).
