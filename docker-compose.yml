services:
  web:
    build: .
    container_name: parking24-web
    ports:
      - "8500:3000"
    # Basic Auth выключена (убрана по решению от 18.08.2026).
    # Чтобы включить: раскомментировать и задать значения.
    # environment:
    #   - BASIC_AUTH_USER=...
    #   - BASIC_AUTH_PASS=...
    restart: unless-stopped
